# MVP Application Requirements

## Purpose

This document translates [MVP Scope](MVP%20Scope.md) into the application capabilities required to deliver it. It defines the deployable applications, internal modules, external integrations, installation behavior, and observable proofs that together constitute the MVP application environment.

The cryptographic and protocol rules remain authoritative in [cryptography.md](cryptography.md). [MVP Scope](MVP%20Scope.md) remains authoritative for what the MVP includes and defers. This document is authoritative for how that scope is divided into applications and composable implementation modules. If an application requirement conflicts with either upstream document, the conflict must be resolved there rather than silently implemented here.

The intended adopter is an individual JavaScript developer. The normal entry points are installing the ChainTorrent Visual Studio Code extension and running `npx chaintorrent`, one command that installs the bootstrap package and deliberately runs its `bin` with no install-time script. After either path completes, and after any unavoidable operating-system or custody consent, the developer must have a working system rather than a list of components to install manually.

## Normative Language and Proof Model

The terms **MUST**, **MUST NOT**, **SHOULD**, and **MAY** are normative.

Every requirement in Self-Installation Requirements, every subsection in Composable Module Catalogue, and every MVP Acceptance Scenario is simultaneously:

1. an operational requirement for the application; and
2. a high-level integration or end-to-end test obligation.

Passing isolated unit tests does not satisfy a requirement whose proof column crosses a process, adapter, storage, network, custody, or chain boundary. The release evidence must exercise the named boundary using the packaged application path.

Requirement identifiers are stable references. They describe behavior, not filenames or a fixed crate layout. A later workplan may split or combine implementation units without changing a requirement, provided the same boundary and proof remain observable.

## Implementation Language and Runtime Policy

All deployable ChainTorrent applications MUST be implemented in Rust. A deployable application with a graphical user interface MUST use Tauri for that interface. Headless services and command-line applications MUST use Rust without adding Tauri solely for consistency.

Shared protocol rules, application workflows, cryptography, storage, networking, credential delivery, and adapter contracts MUST be Rust libraries so that the CLI, desktop application, local daemon, and test harness execute the same behavior rather than reimplementing it.

TypeScript or JavaScript is permitted only where an external host imposes it:

- the Visual Studio Code extension uses TypeScript because the extension host requires a JavaScript-compatible extension;
- the npm bootstrap package may contain the minimum JavaScript or TypeScript required by npm lifecycle and package execution conventions;
- generated TypeScript bindings may exist when a host API requires them; and
- EVM contracts use Solidity because the target execution environment requires contract bytecode rather than a Rust application.

The TypeScript and JavaScript surfaces MUST remain thin host shells. They MUST NOT own protocol rules, cryptographic decisions, entitlement logic, package resolution, or workflow state. Any proposed additional TypeScript module must identify the external host constraint that prevents its implementation in Rust.

## Composition Boundary

The implementation is divided into three dependency rings. Dependencies point inward.

### Protocol and Domain

Pure Rust types and rules represent canonical identities, authenticated deployment suites and hash-cards, attempt contexts, delivery statements, entitlement and interval state, custody state, manifest and sidecar bounds, parameter-set state, claims, and lifecycle transitions. This ring MUST NOT depend on Tauri, Visual Studio Code, npm, a particular chain SDK, a wallet product, a transport implementation, or a storage engine.

### Application Workflows

Rust use cases coordinate installation, package serving, cache resolution, encrypted acquisition, First Finder ingestion, credential delivery, per-attempt authorization, decryption, seeding, publishing, claiming, transfer, repair, and recovery. A workflow depends on capability contracts and domain types, not on the identity of an adapter implementation.

Long-running operations MUST be represented as durable, restartable jobs. In particular, the initiating npm install MUST be allowed to complete after the upstream tarball is verified and committed to the plaintext CAS; First Finder encryption, parameter-set and sidecar registration, and seeding continue asynchronously.

### Adapters and Host Shells

Rust adapters connect workflows to package-manager protocols, chains, pairing-curve backends, wallets, key custody, ingest sources, swarm transports, discovery systems, seed hosts, claim verifiers, local storage, and telemetry. Thin TypeScript or JavaScript shells connect Rust application surfaces to Visual Studio Code and npm where those hosts require it.

Every adapter MUST declare capabilities, version, and compatibility. Resolution MUST validate the complete composition before an operation begins and MUST fail closed before any credential is exercised, any chain mutation is sent, or any swarm transfer occurs.

## Capability Disposition

“Existing” means an external system or standard exists for integration. It does not mean that a conforming ChainTorrent adapter, installer, or test already exists.

| Capability | Disposition for MVP | Application consequence |
| --- | --- | --- |
| npm dependency resolution, lockfiles, workspaces, and install behavior | Existing external system | Integrate through npm's registry protocol; do not replace the package manager. |
| Visual Studio Code extension host | Existing external system | Build a thin TypeScript onboarding and control shell over the Rust application interface. |
| Operating-system service managers, credential stores, firewall controls, and application directories | Existing platform facilities | Build signed installation, lifecycle, custody, and repair adapters per supported platform. |
| EVM-compatible chain, transaction settlement, and view calls | Existing external platform; Base is the launch chain | Build a chain adapter, contract bindings, settlement adapter, and entitlement-state adapter. |
| User wallets and signing providers | Existing external systems and standards | Integrate EIP-1193 and WalletConnect providers, EIP-712 typed signatures, and an ERC-4337 paymaster where supported; provide a working default local identity path. |
| npm registry and integrity metadata | Existing external system | Build the MVP ingest-source adapter and preserve upstream bytes and integrity attestations. |
| BitTorrent clients and protocol | Existing optional compatibility systems | Build transport, discovery, and delegated seed-host adapters without making them protocol requirements. |
| BLAKE3 and Bao, AES-256-CTR, Ed25519, secp256k1, and pairing-curve (BN254, BLS12-381) libraries | Existing cryptographic primitives | Build suite-constrained Rust wrappers, validation, domain separation, and lifecycle handling. |
| Tauri | Existing UI framework | Use it for ChainTorrent graphical applications; keep workflows in shared Rust libraries. |
| ChainTorrent protocol/domain model | New | Build the authoritative Rust types, validation rules, and state machines. |
| Package-serving and resolution orchestration | New | Build the local registry endpoint, CAS orchestration, encrypted acquisition, and fallback workflow. |
| First Finder lifecycle | New | Build immediate foreground serving plus durable asynchronous bootstrap. |
| Credential KEM, envelope, delivery proof, and on-chain verifier | New | Build the suite-constrained Rust implementations behind `ICredentialKemAdapter`, `IKeyAgreementAdapter`, `IDeliveryProofAdapter`, and `IPairingAdapter`, and the verifier the contract suite calls through the chain's precompiles; a validation harness measures their costs before the piece-group size is fixed and the curve confirmed. |
| Entitlement, registry, escrow, identity-binding, envelope-key, parameter-set, delivery, and claim contracts | New | Build and deploy the contract suite plus application adapters. |
| Claim verification, relaying, and application observability services | New | Build Rust deployables and their operational controls. |
| General monetization, seeder compensation, per-entitlement variance, and other V2+ items | Deferred | Preserve the suite and adapter boundaries described by MVP Scope; do not implement them as hidden MVP dependencies. |
| Default key-custody implementation (a local keystore under the OS credential store with version-headed blobs upgraded in place, the holder seed on root devices, and every other device admitted under its own key on the identity's contract account), launch chain (Base) and pairing curve (BLS12-381 primary, BN254 retained), and no escrow-salt custodian | Resolved selections | Prove the chosen composition before release. |
| Custody recovery UX and the published attempt-rule parameters and piece-group size | Unresolved selections | Resolve through the MVP workplan and the validation harness's measurements and prove the chosen composition before release. |

## Deployable Applications

Logical applications MAY share a packaged Rust binary when doing so simplifies installation, but their module contracts and independent lifecycle responsibilities MUST remain testable.

| Application | Language/runtime | Responsibility |
| --- | --- | --- |
| Visual Studio Code extension | Thin TypeScript extension host backed by Rust | Mandatory primary onboarding, status, consent, adapter configuration, health, repair, and links to richer controls. |
| npm bootstrap package | Minimal JavaScript/TypeScript host shim backed by signed Rust artifacts | Makes `npx chaintorrent` a complete alternative onboarding path, installing and deliberately running the bootstrap `bin` in one command with no `postinstall`, and starts or invokes the same installer used by the extension. |
| ChainTorrent desktop application | Rust with Tauri | Graphical configuration, identity and custody workflows, adapter selection, daemon and job status, diagnostics, repair, and recovery. |
| ChainTorrent CLI | Rust | Scriptable consent, configuration, diagnostics, package-host control, identity operations, jobs, repair, and uninstall. It does not replace npm's resolver. |
| Local daemon and package host | Rust service | Machine-level single instance providing local registry endpoints, CAS orchestration, encrypted acquisition, credential custody, per-attempt authorization, decryption, durable jobs, seed-host control, and local IPC/API. |
| Cryptographic validation harness | Rust | Implements the credential KEM, envelope, and delivery proof on the resolved pairing adapter, exercises the deployed verifier on a test chain, and produces the measured sizes, timings, and gas that the piece-group size and curve selection depend on. |
| Relayer or paymaster | Rust service plus chain-specific contract support where required | Sponsors the free path and initial identity binding, enforces policy, and reports cost. |
| Claim verifier | Rust service | Implements the MVP publisher-proof flow and emits contract-verifiable, claimant-bound results. |
| Contract suite | Solidity for an EVM launch adapter | Implements canonical registry, authenticated deployment records, parameter-set registry, envelope-key registration, entitlements with interval state, issuance and transfer with delivery-proof verification through the pairing precompiles, escrow, identity binding, authority transfer, and claim settlement. |
| MVP demonstration harness | Rust | Creates controlled participants, services, wallets, chain state, and failures so packaged end-to-end scenarios are reproducible. |
| Project seed host and site | Rust service plus static site | Persistent first seeder of the core packages and their dependency closure, optional relayer and discovery host, and the protocol's public explanation; a convenience no client depends on. |

## Self-Installation Requirements

Both supported entry points MUST converge on one Rust installation coordinator and the same post-installation state. Neither path may be a reduced demonstration path.

| ID | Operational requirement | Required integration or end-to-end proof | Test class |
| --- | --- | --- | --- |
| SI-01 | Installing the Visual Studio Code extension MUST discover or install the complete supported local environment. | On a clean supported machine, install the published extension, grant only unavoidable consent, then install a supported package through ChainTorrent. | End-to-end |
| SI-02 | Running `npx chaintorrent` MUST discover or install the same complete environment without requiring Visual Studio Code, in that one command, with no install-time script execution and no unstated second command. | On a clean supported machine, run exactly the documented command and complete the same package-install scenario as SI-01. | End-to-end |
| SI-03 | The bootstrapper MUST detect the operating system, architecture, service capabilities, package managers, and available custody and wallet integrations. | Feed supported and unsupported platform combinations and verify deterministic adapter selection or an actionable fail-closed result. | Integration |
| SI-04 | Downloaded native artifacts and application updates MUST be authenticated before execution. | Tamper with an artifact and signature metadata and verify installation halts without executing or replacing the working version. | Security integration |
| SI-05 | Installation MUST create and permission the configuration store, durable job store, plaintext CAS, owned ciphertext store, IPC endpoint, and logs/metrics store. | Install as an ordinary user and verify each store is usable only by its intended principal and survives restart. | Platform integration |
| SI-06 | Installation MUST install, start, and enable the local daemon across reboot, or use an equivalently persistent supported user service when elevation is unavailable. | Reboot or restart the service environment and prove the package host and durable jobs recover without opening an editor. | End-to-end |
| SI-07 | Installation MUST detect supported package managers and offer an explicit, visible, reversible redirect to the local package host. | Accept and reject consent in separate runs; verify only accepted package-manager configuration changes and that restoration is exact. | End-to-end |
| SI-08 | Installation MUST back up every configuration it changes and MUST roll back partial changes if any later required step fails. | Inject a failure after each mutating installation stage and compare the resulting host configuration to the pre-install snapshot. | Failure integration |
| SI-09 | The default install MUST include an owned seed host and working discovery and transport implementations. Delegation to third-party software MAY be offered but MUST NOT be required. | Complete clean installation on a machine with no compatible third-party swarm client and seed then retrieve an object. | End-to-end |
| SI-10 | Installation MUST resolve a compatible package-host, ingest, chain, pairing, credential-KEM, key-agreement, delivery-proof, wallet, identity, custody, settlement, entitlement, transport, discovery, and seed-host composition before reporting ready. | Substitute one incompatible capability declaration at a time and verify readiness fails before protected operations begin. | Composition integration |
| SI-11 | Installation MUST create or import a protocol identity, bind required keys, initialize the selected custody and recovery path, and configure the free relayer without exposing secret material. | Complete first run, restart every local process, and perform a signed, relayed identity-bound free acquisition. | End-to-end |
| SI-12 | Installation MUST configure usable default chain, discovery, relay, and ingest endpoints, while allowing later replacement through capability resolution. | Remove all prior user configuration, install, and verify health checks and a complete free acquisition against the configured environment. | End-to-end |
| SI-13 | Readiness MUST be determined by an active end-to-end health probe, not by the presence of files or processes alone. | Return syntactically valid but nonfunctional adapter endpoints and verify the installer does not report ready. | Integration |
| SI-14 | Reinstallation MUST be idempotent and MUST preserve identities, custody state, plaintext CAS contents, ciphertext obligations, and durable jobs. | Run both onboarding paths repeatedly over populated state and compare identities, stored objects, jobs, and configuration. | End-to-end |
| SI-15 | An interrupted installation MUST resume safely or restore the previous working version without duplicating identities, services, or chain bindings. | Terminate installation at each durable checkpoint, restart it, and prove one coherent final installation. | Failure end-to-end |
| SI-16 | The CLI, Tauri application, and extension MUST expose one repair operation that diagnoses and restores missing binaries, services, permissions, configuration, and adapters without discarding user state. | Corrupt each owned installation component independently and verify repair restores service while preserving custody and stored content. | Recovery end-to-end |
| SI-17 | Updates MUST be signed, atomic, schema-aware, and reversible across application and configuration migrations. | Exercise forward update, interrupted update, incompatible schema, and rollback with active durable jobs and populated stores. | Upgrade end-to-end |
| SI-18 | Uninstall MUST restore package-manager configuration and stop/remove owned services. It MUST preserve identity/recovery material and plaintext content by default unless the user explicitly requests their removal. | Uninstall from a populated machine, verify npm works against its prior registry, and verify the stated preservation and explicit-removal choices. | End-to-end |
| SI-19 | User interaction MUST be limited to unavoidable operating-system permission or firewall approval, explicit package-manager redirect consent, the request and prefetch consent items, custody user presence, recovery confirmation, and funding required for the paid dogfood proof. | Record a clean-install interaction trace and fail it for any unexplained manual download, account creation, endpoint entry, or component-install instruction. | UX end-to-end |
| SI-20 | Installation MUST offer a cache-only mode that creates no chain identity, registers no request, and prefetches nothing; MUST present automatic grant requests and ciphertext prefetch as consent items; and MUST disclose that a first run roughly doubles disk and bandwidth for the closure. | Install in cache-only mode and verify cross-project plaintext reuse with no chain, relayer, or swarm activity and nothing on chain naming the identity; install in the default mode and verify the consent trace records both items and the disclosure. | End-to-end |

## Composable Module Catalogue

Each subsection defines modules that may be split into crates or processes later. The proof is part of the module requirement, not optional test guidance.

### Installation and Configuration

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| IC-01 | A Rust bootstrap coordinator MUST execute a durable installation plan used by both onboarding shells. | Run both entry points against the same fixtures and compare their resulting capability graph and machine state. |
| IC-02 | Platform detection and signed-artifact installation MUST select only compatible binaries and MUST authenticate them before activation. | Cover every supported OS/architecture matrix member plus wrong-architecture, tampered, missing, and downgrade artifacts. |
| IC-03 | Service lifecycle management MUST install, start, stop, restart, enable, inspect, and remove the local daemon using supported platform facilities. | Exercise every lifecycle operation across process failure and host restart. |
| IC-04 | A versioned configuration registry MUST own defaults, user overrides, secret references, adapter capabilities, and endpoint configuration without storing raw custody secrets. | Round-trip configuration through CLI, Tauri, and extension surfaces and verify identical daemon behavior and redacted diagnostics. |
| IC-05 | Adapter resolution MUST validate required capabilities and immutable suite compatibility before activation. | Attempt valid and invalid compositions and prove invalid ones create no network, chain, authorization, or secret side effects. |
| IC-06 | Package-manager configuration MUST be explicit, backed up, reversible, and scoped according to the user's choice. | Configure and restore npm across user, project, workspace, proxy, and pre-existing custom-registry cases. |
| IC-07 | Migration, rollback, repair, and uninstall MUST be durable operations with checkpointed recovery. | Inject termination and storage errors at every checkpoint and prove recovery reaches the prior or new coherent state. |
| IC-08 | Health reporting MUST distinguish installed, configured, degraded, incompatible, and ready states and expose the same facts to all control surfaces. | Compare CLI, Tauri, extension, and daemon API results while adapters and services are independently failed. |
| IC-09 | Every release MUST carry a compatibility statement listing the suite identifiers and adapter versions it can resolve, and a client meeting a deployment outside that list MUST fail closed naming the statement. | Present a deployment under an unlisted suite and verify refusal that names the statement. |
| IC-10 | The desktop application and the extension's webviews MUST meet the platform accessibility baselines. | Run the platform accessibility audit on each surface on each supported operating system and record the result in release evidence. |

### Settings

Every value a user may reasonably change is a setting declared in one catalogue, stored in the configuration registry of IC-04, and exposed identically through every control surface. The MVP ships few user-facing settings and the whole mechanism, so that later settings are catalogue entries rather than retrofits.

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| ST-01 | A settings catalogue MUST declare every user-configurable value with its default, scope (machine, identity, or project), constraints, apply mode (live, next job, or restart), and MVP exposure. No value fixed by a deployment's hash-card or by the cryptographic specification is a setting; where the specification names a limit as conforming-client policy, a setting MAY make local policy stricter and MUST NOT make it looser. | Reconcile the catalogue against every hash-card field and specification constant; attempt to declare a protocol-fixed value and a looser-than-declared freshness bound and verify both are refused. |
| ST-02 | Every store root, configuration, plaintext CAS, owned ciphertext store, job store, logs and metrics, and temporary directory, MUST be a repointable default. Changing a root MUST be a checkpointed, reversible migration job that moves contents and refuses to point two stores at one location. The plaintext CAS holds verified tarballs that package managers extract per project, so no project links into a store and nothing needs relinking. | Repoint each root over populated stores, terminate mid-migration, restart, and verify one coherent result; verify a project installed before the move still installs from the CAS after it; verify coinciding roots are refused. |
| ST-03 | A settings change MUST be validated against the catalogue's constraints and, where it touches an adapter or endpoint, against the composition resolver before it takes effect; an invalid value MUST be refused with the reason and no side effect. | Submit out-of-range, wrong-type, and composition-breaking values through each surface and verify refusal with no network, chain, authorization, or secret activity. |
| ST-04 | Every change MUST be backed up before it takes effect and MUST be restorable exactly. Export MUST produce a versioned document containing no secret and only custody references; import MUST validate as ST-03. | Change, restore, export, and import across a populated installation and a second clean machine; scan the export for secrets. |
| ST-05 | The CLI, desktop application, extension, and IPC MUST expose the same catalogue with identical behavior; mutations MUST require a control principal and, for machine-scoped values and for values the catalogue marks presence-required, local presence; every change MUST be recorded in the consent trace with surface, principal, and old and new value. | Change each setting from each surface, compare daemon state, attempt mutations from an unprivileged principal and a remote surface, and verify the trace. |
| ST-06 | Seeding controls, upload and download bandwidth limits, idle CPU limit, schedule windows, pause on battery and on metered connections, listening port and interface, port mapping, seed host mode, and per-object seed or do-not-seed, MUST be honoured by the seed host as their apply mode declares, and obligated holdings MUST remain non-evictable regardless of settings. | Vary each control while seeding and measure the host's behavior; attempt to evict or unseed an obligated object and verify refusal. |
| ST-07 | Installation MUST initialize every setting from the catalogue's defaults, MUST offer the path and consent items during installation, and reinstallation MUST preserve user overrides. | Install with non-default paths, reinstall through both entry points, and compare overrides. |
| ST-08 | The client MUST NOT offer, through any surface or the configuration registry, a setting that skips the per-attempt state view, weakens a freshness bound below the deployment's declaration, persists decrypt-capable material, disables destruction at `HARD`, or serves plaintext before verification. | Audit the catalogue for such entries; attempt each through the IPC and the registry store directly and verify the daemon ignores or refuses it. |

### Package Serving and Resolution

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| PR-01 | The npm `IPackageHostAdapter` MUST serve npm-compatible metadata and tarball responses while leaving dependency resolution, peer dependency handling, workspaces, overrides, and lockfiles to npm. | Run representative npm projects through the local endpoint and compare resolved graphs and lockfile behavior with the upstream registry. |
| PR-02 | Package coordinates MUST resolve deterministically to the canonical identity hash and authenticated deployment records. | Resolve equivalent and malformed name/version requests across processes and verify identical identities or deterministic rejection. |
| PR-03 | The resolution orchestrator MUST try sources in this order, local plaintext CAS, local ciphertext under a held credential, swarm ciphertext under a held credential, the ingest source, and, only when the ingest source is unavailable, swarm ciphertext under a grant obtained within a bounded wait, starting the next source in parallel when the current one has not delivered within its configured hedge delay and serving the first plaintext to verify, so that an install is never slower than the registry alone by more than the hedge delay, and failing, when every source is exhausted, with an actionable result that leaves the request pending and acquisition running. An identity without a credential for a ledger-known asset MUST be served by the ingest source while it is available. The path taken and its reason MUST be recorded. | Populate one source at a time, with and without a held credential and with the ingest source available and unavailable; delay each source past its hedge delay; assert the chosen path, the recorded reason, the hedge start, the bounded wait, the pending request after failure, and that no source is skipped or taken out of order. |
| PR-04 | The plaintext CAS MUST use verified content addressing and atomic commit; incomplete or mismatched artifacts MUST never become serveable. | Interrupt writes, race writers, corrupt bytes, and request concurrent reads while verifying only a complete validated tarball is served. |
| PR-05 | Cache policy MUST expose capacity, pinning, quota, eviction, hit rate, and cross-project reuse without deleting ciphertext held under a seed obligation. | Fill both stores independently, exercise eviction, and verify plaintext and ciphertext lifecycles remain separate. |
| PR-06 | A plaintext CAS hit MUST serve without swarm access, chain access, entitlement acquisition, or decryption authorization. | Disable all remote systems and install the same package into a second project from a populated plaintext CAS. |
| PR-07 | Local or remote ciphertext MUST enter the authenticated authorization and decryption workflow before plaintext is committed or served. | Attempt to serve encrypted content with absent, pending, denied, mismatched, and valid authorization. |
| PR-08 | When upstream serves, verified upstream bytes MUST be committed and served to the initiating npm request without waiting for First Finder bootstrap, request registration, ciphertext prefetch, or grant pickup. | Stall encryption, chain, seeding, request, prefetch, and pickup stages after upstream verification and prove the initiating install still completes correctly with unchanged latency. |
| PR-09 | The package host MUST capture package metadata into an authenticated local store at every upstream resolution and ingest, immutable per-version metadata with its attestation and the dist-tag snapshot with its capture time; MUST serve tarball URLs on the canonical registry host, reached through npm's registry-host replacement, so committed lockfiles stay portable across machines and ports; and during an upstream outage MUST serve a pinned dependency closure from ledger and swarm alone, resolve version ranges against the ledger's per-name index of ingested versions, and mark dist-tags as best effort. The MVP outage guarantee is the pinned closure. | On a second machine with empty package-manager and daemon caches and upstream unavailable, install a pinned closure from protocol-held state; commit that lockfile and install it on a machine whose host port differs; resolve a fresh range during the outage and verify it resolves only against ingested versions with the staleness marker present. |
| PR-10 | For every ledger-known asset the identity holds no entitlement to, the daemon MUST register a durable grant request before or alongside the upstream fetch, on chain, batched per install into one sponsored operation, queued behind the identity's binding and envelope-key registration and while the chain, the relayer, or its budget is unavailable; MUST watch chain events for mints to the identity and load a credential granted in its absence on the next run; MUST NOT request for an asset it holds, for an asset absent from the ledger, or against the set of a deployment whose bytes failed the plaintext-root check; MUST route a request for an explicit-publisher asset to its issuance policy and treat refusal as discretion; and MUST NOT acquire a priced entitlement without the user's action. | Install a closure mixing held, unheld, absent, explicit-publisher, and priced assets with the chain and relayer alternately unavailable; verify one batched request naming exactly the unheld free assets, queued and submitted when its dependencies return, fulfilled by a holder while the requester is offline, and loaded on the next run; verify the priced asset produced a notice and no transaction. |
| PR-11 | While a request is pending the daemon MUST fetch and verify the deployment's ciphertext and sidecar in the background at low priority under the bandwidth, metered, and battery settings and the ciphertext store's quota, pin them until the grant lands, seed them meanwhile, and after the grant lands confirm it holds a deployment carrying the granted set, fetching one if not. | Serve a closure from upstream while tightening quota, metered, and battery settings in turn; verify prefetch obeys each, the objects are pinned and seeded, and a grant under a set other than the prefetched deployment's triggers a fetch of a matching deployment. |
| PR-12 | The daemon MUST expose per-asset independence, plaintext, ciphertext, and credential held, or pending with its reason, through every control surface, and a project's independence as every asset in its lockfile being independent. | Drive assets through each state and compare every surface. |

### Existing Encrypted Content Consumption

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| EC-01 | The client MUST authenticate the deployment record and hash-card, resolve the complete immutable suite, validate manifest bounds, and authenticate the header sidecar against its root before any credential is exercised. | Mutate every authenticated field, suite capability, counter bound, piece count, piece-group size, piece index, and sidecar capsule and verify early fail-closed behavior. |
| EC-02 | Ciphertext and sidecar acquisition MUST accept local and remote sources, verify Bao data against the authenticated roots, and share one object across compatible transport locators. | Corrupt chunks and locators across local, remote, and multi-homed retrievals and verify only authenticated bytes enter storage. |
| EC-03 | Entitlement acquisition and per-attempt authorization MUST be separate operations; an existing entitlement skips acquisition but never skips the per-attempt state view. | Repeat installs with and without an entitlement and assert chain mutations and state reads independently. |
| EC-04 | Every attempt MUST hold a state view at the deployment's declared tier no older than `τ_soft` over the complete `AttemptContext`, and a wallet-control assertion no older than `τ_wallet` by a device key the identity's contract account admits in that same view. | Omit or alter each context field, age the view past `τ_soft`, drop the assertion, and assert with a revoked device key; every case must fail closed, and the revoked device's client must purge its envelopes and envelope secrets. |
| EC-05 | `AUTHORIZED`, `PENDING_SETTLEMENT`, and `DENIED` MUST lead respectively to decryption, bounded wait/retry, and terminal refusal. | Drive all three adapter results through single and paginated batch evaluation without losing per-context bindings. |
| EC-06 | The client MUST decrypt its envelope only under its own registered envelope keys, verify the credential against the parameter set's validity equation, and verify each capsule's well-formedness before decapsulating it. | Substitute an envelope from another interval or identity, a credential for another entitlement, and malformed or substituted capsules, and verify rejection. |
| EC-07 | Decryption MUST decapsulate each piece group's capsule with the credential, derive the set's wrapping key, unwrap the piece-group key, implement the suite-declared AES-256-CTR addressing rules, verify plaintext against the authenticated root, atomically commit to CAS, and serve the exact upstream tarball. | Decrypt random ranges and complete artifacts at counter and group boundaries, then compare upstream integrity and plaintext root. |
| EC-08 | The decrypted credential, piece-group keys, expanded cipher state, buffered keystream, and other transient decryption material MUST remain memory-only; attempts MUST stop when a transfer out is observed at the declared tier, and everything decrypt-capable MUST be destroyed when that transfer reaches `HARD`. The persistent credential — envelope keys, envelope, interval index — MUST be stored only under `IKeyCustodyAdapter`. | Transfer during active use, reorganize the transfer, cancel, crash, and inspect storage and diagnostics for persisted decrypt-capable material and for premature destruction. |

### First Finder Bootstrap

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| FF-01 | On a local and remote miss, the NPM ingest adapter MUST fetch the immutable tarball as-is and, where the source provides one, validate the source's release attestation, for npm the registry signature over `name@version:integrity` against the registry's published keys and the integrity digest it covers, before CAS commit and before registration, recording its absence where the source provides none. | Supply valid, mismatched, mutable, truncated, and unavailable upstream responses, plus a forged signature, a missing signature, and a genuine signature over a different digest, and verify only exact validated bytes are committed, a forged signature and a genuine signature over a different digest are refused, and a missing signature registers with the absence recorded. |
| FF-02 | The foreground workflow MUST atomically commit and serve validated upstream plaintext to the initiating npm request before asynchronous bootstrap completes. | Hold every background dependency unavailable and verify foreground installation latency ends with the upstream response and CAS commit. |
| FF-03 | A durable idempotent job MUST continue parameter-set creation, deployment creation, encryption, registration, and seeding after the initiating process or editor exits. | Terminate npm, Visual Studio Code, the CLI, and the daemon at each checkpoint and prove eventual single completion after restart. |
| FF-04 | Deployment creation MUST generate a unique deployment identifier, a random master scalar and parameter set where none is live for the asset, a random piece-group key and random capsule randomness per piece group and a random IV, encrypt as one continuous stream keyed per piece group, construct for every live parameter set a header sidecar carrying that set's capsule and the piece-group key wrapped under it, each sidecar its own object with its own root and locator, and the authenticated commitments and hash-card data, and never derive any key from public material. | Use deterministic test vectors around the cipher and KEM while statistically and structurally validating production randomness, deployment uniqueness, and per-group key distinctness. |
| FF-05 | State-locked escrow registration MUST produce one canonical winner when finders race. A loser MUST destroy its ciphertext, sidecar, master scalar, derivatives, and buffered keystream, then resolve the winner. | Race independent finders and inspect chain state, stores, secret lifecycle, and final locator selection. |
| FF-06 | The winning First Finder MUST retain the master scalar under `IKeyCustodyAdapter` as escrow custodian, MUST author credentials only for grants the escrow contract authorizes, and MUST erase its copy only after an acknowledged handover to a claimant. | Attempt unauthorized issuance, crash and restart across grants, and verify erasure happens only after acknowledged handover and never before. |
| FF-07 | Bootstrap completion MUST require authenticated parameter-set and deployment registration, sidecar publication, persistent seeding of ciphertext and sidecar, and the finder's grant of the asset's first entitlement to itself under the escrow set with its credential delivered in the mint; partial success remains a recoverable job. | Independently fail chain, transport, seed storage, and the self-grant and verify accurate state plus eventual recovery without duplicate deployments, parameter sets, or self-grants. |
| FF-08 | A later seeker MUST discover the canonical deployment, leech authenticated ciphertext and sidecar, obtain an entitlement with its delivered credential, decrypt, and install without contacting NPM. | Disable NPM after FF-07 and complete installation from a clean second identity and machine profile, with any holder reachable for the grant. |
| FF-09 | Publishing a package through the explicit-publisher path MUST ingest its dependency closure, bootstrapping absent dependencies as First Finder and verifying present ones, so the package and its dependencies are swarm-native at publication. | Publish the project's own package with dependencies partly present and verify every dependency is in the swarm afterward. |

### Swarm, Discovery, and Storage

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| SW-01 | `ISwarmTransportAdapter` implementations MUST transfer ciphertext by authenticated root. The MVP's sole transport implementation is BitTorrent through an embedded third-party library behind the adapter; a BLAKE3-native transport is a peer implementation the interface reserves and the MVP does not ship. Every piece MUST be verified against the ciphertext or sidecar Bao root after the library's own piece check and before it enters the store; the adapter, not the library, owns the translation from ciphertext root to transport identifier. | Retrieve the same deployment through the owned seed host and through a delegated external BitTorrent client and verify identical ciphertext roots; corrupt a piece that passes the library's SHA-1 check and verify the Bao check rejects it before storage. |
| SW-02 | Multiple `IPeerDiscoveryAdapter` implementations MUST run concurrently, union and deduplicate results, and treat no discovery source as authoritative. | Return overlapping, conflicting, unavailable, and malicious discovery results and verify deterministic safe aggregation. |
| SW-03 | The owned seed host MUST run independently of editor and CLI sessions and resume persisted transfers and seeding after restart. | Close all clients and reboot or restart the host while another participant continues to discover and retrieve the object. |
| SW-04 | A delegated `ISeedHostAdapter` MUST translate ciphertext roots to external identifiers and verify external custody with random Bao authentication challenges rather than trusting reports. | Make a delegated client falsely report possession and verify the adapter detects the missing or corrupt object. |
| SW-05 | Ciphertext storage MUST be keyed by ciphertext root, shared across transports, separately budgeted from plaintext CAS, and distinguish voluntary from obligated holdings. | Multi-home one object, vary both capacity policies, and inspect one stored copy with preserved holding reason. |
| SW-06 | Transport and storage failures MUST leave resumable acquisition and seeding jobs without exposing unauthenticated partial data. | Interrupt peers, network, disk, and daemon independently and prove verified resume or safe restart. |
| SW-07 | The seeding archive — objects, sidecars, sizes, obligated versus voluntary holdings — MUST be visible to the user through the CLI, desktop application, and extension as their swarm contribution. | Seed a set of objects and compare every control surface's archive view with the seed host's verified contents. |
| SW-08 | The project seed host MUST hold an entitlement, and so a credential, for every asset it seeds, so that its availability covers grants and not only bytes for the core closure, and no client MUST depend on it. | With every other holder offline, verify a new identity obtains a grant for a core-closure asset from the seed host; with the seed host unreachable, verify resolution and grants proceed through other holders. |

### Ledger, Contracts, and Settlement

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| LC-01 | The contract suite MUST represent canonical identities, authenticated deployments and hash-cards, parameter sets and their live or retired state, envelope-key registrations, asset-bound entitlements with interval counters and envelope digests, issuance and transfer with delivery verification, escrow, identity bindings, claims, and administrative authority. | Deploy the suite and exercise each state transition through the Rust chain adapter while asserting emitted events and view state. |
| LC-02 | Entitlements MUST remain bound to one asset across issuance and transfer; no serial or identifier may authorize another asset. | Attempt cross-asset identifier reuse and verify both contract and authorization evaluation reject it. |
| LC-03 | First Finder issuance MUST remain $0.00 and escrowed; only explicitly published project-owned dogfood assets may use the nonzero MVP path. | Attempt to price First Finder content and verify refusal, then complete priced primary issuance and secondary transfer for an explicit publisher. |
| LC-04 | Settlement adapters MUST expose the deployment's declared minimum tier, which MUST be `SOFT` or above for a priced deployment and MAY be `INCLUDED` for a zero-price one, the references at which the attempt rule reads (that tier) and destroys (`HARD`), and reference age for the `τ_soft` bound. | Exercise included, pending, reorganized, stale, and insufficient-tier references through the attempt rule for a zero-price and a priced deployment. |
| LC-05 | Transfer MUST verify the seller's delivery proof against the entitlement's stored envelope digest, seller keys, and identity element, and in one transaction advance the interval counter, record the buyer's keys and envelope digest, emit the envelope, transfer the entitlement, and release payment; a lock with no valid delivery MUST refund at expiry. | Transfer during active use with valid, malformed, replayed, and cross-entitlement proofs and a lapsed lock; assert buyer access at the declared tier, seller destruction at `HARD`, and refund. |
| LC-06 | Batch and paginated chain reads MUST preserve every complete attempt context and MUST NOT broaden authorization. | Compare individual and batched evaluations across mixed assets, identities, settlements, and outcomes. |
| LC-07 | State-locked registration and claim settlement MUST be race safe, replay safe, and bound to expected contract, chain, claimant, asset, and expiry. | Submit reordered, replayed, front-run, wrong-chain, and concurrent transactions and verify the intended single outcome. |
| LC-08 | Envelope-key registration MUST require a proof of possession for each key under a wallet signature and MUST reject identity-element keys; mint MUST reject an entitlement whose identity element is trivial. | Register valid, unproven, and identity keys; mint against a colliding identifier; verify each rejection. |
| LC-09 | Parameter sets MUST be registered per asset, marked live at bootstrap and at claim, and retired only when no live entitlement remains under them; a deployment record MUST be rejected unless it carries a sidecar root for every set live at registration; and the asset's authority MAY add a sidecar root for a set made live afterward to a live deployment, whose registered sidecars are otherwise immutable. | Register deployments with missing and extra sidecars across escrow, claim, migration, and retirement, and verify acceptance only when coverage is complete; add a sidecar for a later set to a live deployment and verify it resolves under both sets. |
| LC-10 | Payment locks MUST carry a bounded, published expiry. Relayer-paid mints, batched grant requests, and zero-price grants MUST be admitted under a relayer policy that bounds exposure globally, a per-window expenditure budget and a maximum sponsored liability, as well as per identity, with per-identity limits admitting a realistic dependency closure in one batch, and exhaustion MUST yield an explicit failure state and never a false authorization. | Let locks lapse and flood free mints from many fresh identities; verify refunds, that sponsored spend never exceeds the configured budget, and that exhaustion is reported truthfully. |
| LC-11 | Entitlement ownership MUST change only through mint, grant, and delivery; the token's standard transfer and approval entry points MUST be disabled, receiver callbacks MUST run after state is final under a reentrancy guard, and no transition may leave a holder without a credential envelope recorded for its interval. | As owner, approved address, and operator, attempt the standard transfer and approval functions without a delivery and attempt reentry from a receiver callback; verify no ownership change and consistent interval state. |
| LC-12 | The registry MUST hold durable grant requests, submitted in batches, keyed by identity and asset, open until a grant to that identity settles, withdrawable by the requester, and readable by holders; an open request MUST authorize nothing by itself. | Submit batched requests, fulfil some, withdraw others, and verify state, events, and that an open request confers no authorization. |
| LC-13 | The identity MUST be a contract account verified by ERC-1271 whose signer set is its device registry, with device admission, re-scoping, and revocation as signed intents under the control branch. Every contract function that mutates identity-bound state, binding, device admission and revocation, envelope-key registration, grant requests and withdrawals, grants, mint, lock, and delivery, MUST take the acting identity and a signed intent over the call's fields, chain, contract, nonce, and expiry, verified through the account against a signer whose permissions cover the call, so that paymaster-sponsored, relayer-submitted, and self-funded calls share one entry point; the contracts MUST enforce lock expiry and no volume limit, which belongs to the relayer's policy. | Exercise each function as a paymaster-sponsored, relayer-submitted, and self-funded call and verify identical state; submit an intent signed by another identity, an expired intent, a replayed nonce, a revoked device key, and a key outside its permissions and verify refusal. |

### Identity, Wallet, and Custody

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| IW-01 | Identity, chain signing, handshake signing, envelope keys, custody, and recovery MUST be separate capabilities that can be bound to the same principal without requiring matching keys. | Compose supported distinct schemes, register their binding and envelope keys, and verify signatures and envelope decryption across process restart. |
| IW-02 | The selected MVP `IKeyCustodyAdapter` MUST declare signing, envelope-key, issuance-material, external-key protection, multi-device, device-role, device-admission, recovery, in-place-upgrade, user-presence, and publisher-seed capabilities, and its blobs MUST carry the adapter version, wrapping-key identifier, and derivation parameters. An in-place upgrade MUST retire the old blob only after the re-derived public keys match the on-chain binding and envelope-key registration. | Resolve every supported and unsupported capability combination and verify fail-closed selection before identity creation; upgrade in place with termination at every checkpoint and a tampered rewrap, verifying the principal unchanged or rolled back. |
| IW-03 | First run MUST create or import a usable identity, complete the relayer-funded on-chain handshake-key binding once per identity, and register the identity's envelope keys with proofs of possession before its first acquisition. | Start from no identity, restart, reinstall, and install multiple packages while asserting one stable identity, one binding transaction, and one envelope-key registration. |
| IW-04 | Custody MUST protect identity keys, envelope keys, any publisher or escrow master scalar, and any publisher derivation seed, and MUST persist per entitlement the envelope and interval index; it MUST NOT persist the decrypted credential or piece-group keys or conflate them with identity material. | Inspect supported storage surfaces and recovery exports before and after decryption and after a transfer out. |
| IW-05 | Recovery and multi-device use MUST preserve or explicitly rotate identity bindings according to declared capability; neither may silently create a different principal, and a device that lacks the envelope MUST recover it from chain history. The holder seed MUST stay on root devices; every other device MUST act under its own key admitted as a signer on the identity's contract account, receiving envelope secrets only in a reading role and never the seed. | Pair a reader on a second device profile and verify it reads alone with the seed absent from it; revoke it and verify denial and purge at its next state view; recover the root on a fresh profile from the seed and verify identity, binding, envelope recovery, access, and user-visible rotation semantics. |
| IW-06 | Publisher and administrative authority rotation MUST not change parameter sets, plaintext identity, or existing entitlement asset binding. | Rotate authority and verify existing credentials still decrypt plus successor publishing behavior. |
| IW-07 | External wallets MUST connect through EIP-1193 or WalletConnect and only sign; registrations and bindings MUST be EIP-712 typed data; the default identity MUST be a local key under `IKeyCustodyAdapter`; envelope keys MUST never be wallet keys. | Complete onboarding with the local identity, then with each external wallet path, and verify envelope keys are custody-held in every case. |

### Cryptographic Services

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| CR-01 | The MVP payload cipher MUST implement AES-256-CTR with a random 64-bit IV, 64-bit block counter, one continuous deployment stream keyed per piece group, and the address and extent rules in the cryptographic specification. | Run specification vectors, random-access ranges, piece and group boundaries, maximum-valid counters, and overflow rejection. |
| CR-02 | BLAKE3/Bao services MUST construct and verify plaintext, ciphertext, and header-sidecar commitments, streaming chunks, and random storage challenges against authenticated roots. | Corrupt roots, paths, chunks, lengths, and order across encryption, acquisition, and delegated storage. |
| CR-03 | Signature services MUST support Ed25519 for protocol layers and secp256k1 for the EVM chain adapter with explicit domain separation and complete-message binding. | Verify valid signatures and reject cross-domain, cross-layer, truncated-context, wrong-scheme, and replay substitutions. |
| CR-04 | Envelope services (`PairingElGamalAdapter` over `IPairingAdapter`) MUST encrypt one credential to a recipient's two registered keys with independent coins, reject shared or identity secrets, and round-trip exactly. | Swap keys, coins, and elements independently and verify decryption fails or yields a credential that fails validity. |
| CR-05 | Master-scalar, capsule-randomness, and piece-group-key generation MUST use a cryptographic random source for First Finder deployments; explicit publisher derivation MUST use the secret hierarchy, the unique non-reusable deployment identifier, and the piece-group index defined by the specification. | Prove First Finder non-determinism and publisher reproducibility only with the correct secret hierarchy, deployment identifier, and group index. |
| CR-06 | Manifest, hash-card, and sidecar validation MUST authenticate all suite fields and capsules and reject malformed bounds before any credential is exercised. | Fuzz authenticated descriptors and capsules and assert ordering with chain and custody spies. |
| CR-07 | Secret lifecycle services MUST minimize copies, exclude secrets from logs and diagnostics, keep decrypted credentials and piece-group keys memory-only, and zeroize them at every required success, interval-end, cancellation, loss, and error transition. | Instrument lifecycle boundaries and crash artifacts while exercising all terminal paths. |
| CR-08 | The credential KEM (`Bb1DepthOneKemAdapter`) MUST implement setup, issue, rerandomize, validity, encapsulate, well-formedness, and decapsulate exactly as specified, with every valid credential recovering the same value from every well-formed capsule and rerandomization requiring no master scalar. | Run algebraic vectors: cross-holder agreement on the encapsulated value, rerandomized-credential validity, cross-entitlement non-convertibility, malformed-capsule rejection, and trivial identity-element refusal. |
| CR-09 | The delivery proof (`SchnorrFsDeliveryProofAdapter`) MUST prove and verify the mint and transfer relations over the full statement in both verifier forms the pairing adapter declares, and the Rust verifier MUST match the deployed on-chain verifier bit for bit. | Cross-verify the Rust prover against the deployed contract on each curve form; mutate each statement field and each response; replay across settlements. |
| CR-10 | `IPairingAdapter` implementations MUST expose source-group operations, subgroup checks, and the pairing-product check on BN254 and BLS12-381 with encodings matching the target chain's precompiles. | Run vectors against precompile behaviour, including subgroup rejection and encoding edge cases. |
| CR-11 | Sidecar and derivation services MUST derive each parameter set's wrapping key from its encapsulated value and context, wrap and unwrap the piece-group key as the specification fixes, use BLAKE3 keyed derivation for every off-chain derivation and keccak256 under the specification's domain tags for the identity mapping and the delivery challenge, and freeze context strings, serialization, and output lengths with known-answer vectors. | Unwrap one piece-group key under two independently generated parameter sets; verify the identity mapping and the challenge against the deployed verifier and against an independent implementation's vectors. |

### Credential Delivery

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| CD-01 | Mint MUST deliver the recipient's first credential in the mint transaction: the issuer authors it under its master scalar, encrypts it to the recipient's registered envelope keys, and posts the envelope with a mint proof that the contract verifies before recording interval zero. | Mint with valid, malformed, and replayed proofs and with unregistered recipient keys; verify only the valid case records an interval and emits an envelope. |
| CD-02 | Sale MUST deliver the buyer's credential in the settlement: the seller decrypts its own envelope afresh, rerandomizes with a private offset, encrypts to the buyer's registered keys, and posts the envelope with a transfer proof against the previous envelope; the seller's persisted offset, if any, MUST NOT be required. | Sell after restart with no in-memory state, with a rerandomized reading credential, and with a malformed previous envelope; verify delivery, refusal, and that the buyer's credential is valid. |
| CD-03 | The on-chain verifier MUST verify mint and transfer proofs through the pairing adapter's precompile interface in the form the adapter declares, and MUST reject any statement field the contract's record contradicts. | Deploy on each curve form, verify the full acceptance-scenario proof set, and mutate stored record fields between proof generation and submission. |
| CD-04 | The persistent credential MUST survive restart and MUST be recoverable from chain history when the local envelope is lost; lost envelope secrets MUST end transfer but not reading while the decrypted credential is held. | Delete the local envelope and recover; delete envelope secrets and verify reading continues until interval end and transfer is refused. |
| CD-05 | Under an escrow suite, any current holder MUST be able to author the credential for a newly minted zero-price entitlement from its own credential with the transfer proof against its own current envelope; the contract MUST record it as interval zero of the new entitlement; the conforming client MUST serve pending grant requests for assets it holds automatically under the relayer's policy, including requests from identities that are offline; and the First Finder's absence MUST affect nothing. | Take the First Finder offline permanently; verify new identities obtain grants from other holders, including a requester that is offline when the grant is authored and loads it on its next run, existing holders read and transfer, and no path degrades. |
| CD-08 | The credential KEM's identity scope MUST be a declared capability fixed by the suite — entitlement scope for explicit-publisher deployments, asset scope for escrow deployments — and holder-assisted grant authorship MUST be refused under the entitlement scope. | Attempt holder-assisted authorship under each scope and verify acceptance only under the asset scope. |
| CD-06 | Claim MUST register the claimant's own parameter set, leave every escrow-era credential working, allow optional encrypted handover of the escrow master scalar with a compatibility proof, require later bodies to carry a sidecar per live set, and support voluntary replacement of escrow-era credentials under the claimant's set. | Claim with the First Finder absent and present; publish a successor; migrate one holder; verify all four properties and the retirement rule. |
| CD-07 | The cryptographic validation harness MUST produce, on the resolved pairing adapter, capsule, envelope, and proof sizes, decapsulation time per piece group, proof generation and verification time, and delivery-verification gas for a mint and a transfer, and the piece-group size MUST be chosen from those measurements before release. | Run the harness on each candidate curve and record the measurements and the resulting piece-group size in release evidence. |

### Publishing and Claims

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| PC-01 | Explicit publishing MUST prove publisher authority, create a unique deployment, derive its parameter set and capsule randomness from the explicit-publisher hierarchy, register authenticated metadata including the sidecar root, and seed the ciphertext and sidecar. | Publish a project-owned package, reject invalid authority, and consume it through a second identity. |
| PC-02 | First Finder escrow MUST record, per escrow deployment, source provenance, the upstream release attestation, verified on chain against a governance-updated source-key table where one is declared, or its recorded absence, and the escrow parameter set; MUST admit a later escrow deployment of the same asset from another First Finder, so that a deployment whose bytes fail attestation at clients is dead rather than the identity poisoned; MUST record no commitment to the maintainer's identity; and MUST NOT grant the finder publisher rights. | Inspect registry and entitlement state after ingest for the absence of any maintainer-derived field; register with a forged attestation and verify refusal, and with a declared absence and verify admission with the absence recorded; register a second escrow deployment and verify coexistence and client resolution past a dead one; attempt finder pricing, administration, issuance beyond escrow grants, and claim actions. |
| PC-03 | `IPublisherProofAdapter` and `IClaimVerifierAdapter` MUST bind proof results to claimant, package or claim set, contract, chain, nonce, and expiry. | Replay and redirect valid proofs across claimants, packages, contracts, chains, and time. |
| PC-04 | Claim-set verification MUST NOT depend on any party that can be unavailable at claim; the verifier MUST establish the claim set from upstream metadata at verification time and MUST NOT depend on any stored maintainer commitment or salt. | Complete and reject claims with the First Finder absent and with any portal-like component absent; verify that no claim path reads a maintainer commitment. |
| PC-05 | A successful claim MUST transfer complete protocol-level ownership and authority, register the claimant's parameter set, leave escrow-era credentials working, and allow claimant-issued successor deployments with the same plaintext root and a sidecar per live set. | Claim a First Finder record, administer it, issue entitlements under the new set, and publish then verify a successor deployment readable by both escrow-era and new credentials. |
| PC-06 | Claim-set and voucher handling MUST support one verified maintainer proving every covered escrowed record without exposing reusable raw proof material. | Claim multiple eligible versions, exclude an ineligible asset, replay the voucher, and inspect logs and storage. |
| PC-07 | The claim verifier's attestation key MUST be registered on-chain with rotation and revocation, and a revoked key's vouchers MUST be rejected. | Rotate and revoke the key and replay vouchers across the boundary. |

### Licensing

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| LI-01 | Packaged applications and libraries MUST ship under the chosen license, and every asset record MUST carry the rights-holder's content terms; the project's own packages MUST carry terms obligating an entitlement for use and a license per copy sold or bundled. | Inspect packaged license files and record fields; attempt publication of a project package without terms and verify refusal. |
| LI-02 | Legal review of priced transferable entitlements and of the paymaster, and confirmation of export or cryptographic-distribution constraints on shipping a client containing AES and pairing cryptography to every supported platform, MUST be recorded as release-evidence items before the transaction flow proof. | Release evidence contains both items with their disposition. |

### Relaying and Observability

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| RO-01 | The relayer or paymaster MUST sponsor free entitlement acquisition, batched grant requests and their fulfilling grants, and the single initial identity-binding transaction, with abuse controls and explicit failure states. | Complete clean free onboarding and acquisition, exhaust or deny subsidy, and verify actionable recovery without false authorization and requests queued locally until sponsorship returns. |
| RO-02 | Paid dogfood acquisition MUST require real buyer funding and MUST NOT be presented as covered by the free subsidy. | Attempt without funds, then fund the test identity and complete primary and secondary priced settlement. |
| RO-03 | Metrics MUST record CAS hit and cross-project reuse, resolution path and its reason, request and grant timing, the fraction of a closure independent after its first run and time to independence thereafter, install wall-clock, state-read volume and latency, decapsulation time per group, delivery proof generation and verification time and gas, swarm bytes, ingest bytes, relayer gas, job outcome, and adapter health without recording secrets. | Run every acceptance path, reconcile metrics with induced activity, and scan telemetry for sensitive fields. |
| RO-04 | Structured logs and traces MUST correlate one package request across package host, CAS, swarm, chain, credential custody, decryption, and background First Finder work. | Execute concurrent installs and reconstruct each path without confusing identities, assets, or jobs. |
| RO-05 | Health and diagnostics MUST expose degraded dependencies, compatibility decisions, durable job state, and recovery actions consistently through CLI, Tauri, extension, and machine-readable API. | Fail each dependency independently and compare all control surfaces and repair recommendations. |
| RO-06 | Attempt latency — the state read plus decapsulation — MUST be measured against a declared pre-run interactive latency budget for the local developer population. | Configure the budget, inject latency below and above it, and verify the result is a pass or failure rather than an unqualified measurement. |
| RO-07 | The daemon MUST respect the configured upload bandwidth and idle CPU limits, and the acceptance run MUST record idle CPU, memory, and seeding bandwidth on each supported platform. | Measure idle footprint on each platform under the default limits and under tightened limits and record both. |
| RO-08 | Cold-start time from reboot to the package host serving MUST be measured and reported in the acceptance run on each platform, with no threshold. | Reboot each platform and time readiness. |

## Cross-Cutting Application Requirements

| ID | Operational requirement | Required integration proof |
| --- | --- | --- |
| XA-01 | All external inputs, authenticated descriptors, adapter responses, IPC requests, network messages, and chain events MUST be treated as untrusted until validated at their boundary. | Fuzz and substitute inputs at every adapter contract and verify bounded failure without protected side effects. |
| XA-02 | Local APIs MUST be authenticated and least-privileged. The package host MUST confer resolution and serving and nothing else on any caller. Because every process of the installing user is one principal to the operating system, operations that transfer, export keys, change bindings, or widen exposure MUST complete only after an interactive confirmation through a surface the daemon owns that no IPC message can satisfy, and a project process MUST NOT gain custody, decrypted credentials, piece-group keys, or those operations merely because it runs as the user or can request a package. | Call daemon surfaces from other local users and from an npm lifecycle script running as the installing user; verify the script obtains resolution only, and that transfer, key export, settings mutation, and binding changes do not complete without the interactive confirmation. |
| XA-03 | Durable workflows MUST be idempotent, observable, cancellable where safe, and recoverable after process or machine failure. | Terminate every workflow at persisted transitions and verify exactly-once effects or safe compensation. |
| XA-04 | Failure messages MUST identify the failed capability and safe recovery action without exposing secret material or suggesting insecure bypasses. | Exercise incompatible adapters, denied authorization, corrupt content, stale state views, chain failure, and custody failure through every UI. |
| XA-05 | The packaged MVP MUST run without a developer toolchain on supported user machines. | Install published artifacts on clean machines lacking Rust, Node development tools beyond npm, Solidity tooling, and repository source. |
| XA-06 | The per-attempt state view MUST be obtained from at least three configured nodes by default and accepted only when at least two reachable nodes agree at a common reference block within the freshness bound. A node behind the reference is stale, not disagreeing, and one unreachable or stale node MUST NOT deny an attempt; two nodes returning different state at the same reference MUST fail closed; and a view older than `τ_soft`, as during a prolonged sequencer or provider stall, MUST fail closed rather than proceed. | Drive the decision table: one timeout, one stale provider, honest heads at different heights, divergent state at one reference, a reorganization, and a prolonged stall; verify each case's single defined result. |
| XA-07 | The repository MUST ship unit, integration, and end-to-end test facilities, including the demonstration harness, and every requirement's proof class MUST map to one of them. | Run the full suite in CI on a clean machine and reconcile every requirement identifier with a passing proof. |
| XA-08 | Artifact signing keys MUST be held under the same custody discipline as issuance material, with a documented rotation and revocation path, before the first signed release. | Inspect the release-process record; rotate a signing key in rehearsal and verify earlier artifacts stay verifiable and a revoked key's artifacts are refused by the updater. |
| XA-09 | The local package host and daemon MUST be documented as a supply-chain surface on developer machines in a threat-model statement covering same-user processes, local privilege escalation, artifact substitution, and redirect abuse, which SI-04, XA-01, XA-02, and PR-04 cite as their controls. | Release evidence contains the statement, and each named control's proof cites the threat it counters. |
| XA-10 | Every retry loop, pending settlement, discovery, transport, and chain reads, MUST use bounded exponential backoff with declared ceilings per adapter recorded in the configuration registry. | Hold each dependency unavailable and verify retry cadence stays within the declared ceiling and the bound is reported. |

## MVP Acceptance Scenarios

The MVP is releasable only when these scenarios pass through the packaged applications. Test-only calls that bypass onboarding, the local package host, capability resolution, or the deployed on-chain verifier do not satisfy them.

| ID | Release scenario | Required observable outcome |
| --- | --- | --- |
| AS-01 | Visual Studio Code clean install | The extension produces a ready Rust environment and installs a supported npm package after only permitted consent. Proves SI-01, SI-03–SI-13, and SI-19. |
| AS-02 | npm clean install | `npx chaintorrent`, as the one documented command, produces the same ready environment and completes the same package installation without Visual Studio Code. Proves SI-02–SI-13 and SI-19. |
| AS-03 | Idempotent reinstall | Repeating both entry points preserves identity, bindings, stores, jobs, and package-manager configuration. Proves SI-14. |
| AS-04 | Interrupted install and upgrade | Termination at every durable stage resumes or rolls back to one coherent signed installation. Proves SI-08, SI-15, and SI-17. |
| AS-05 | Repair and uninstall | Repair restores deliberately damaged components; uninstall restores prior npm configuration and honors state-preservation choices. Proves SI-16 and SI-18. |
| AS-06 | Plaintext cross-project hit | A second project installs from local plaintext with chain, swarm, and NPM unavailable. Proves PR-05 and PR-06. |
| AS-07 | Local encrypted hit | Local ciphertext and sidecar are authenticated, the attempt authorized, pieces decrypted, verified, committed, and served; denial, stale-view, and interval-end variants fail correctly. Proves PR-07 and EC-01–EC-08. |
| AS-08 | Remote encrypted hit | With the ingest source unavailable or disabled by the harness, a clean client discovers, leeches, Bao-verifies ciphertext and sidecar, obtains its credential within the bounded wait, authorizes each attempt, decrypts, and installs an existing deployment. Proves PR-03, EC-01–EC-08, and SW-01–SW-06. |
| AS-09 | NPM fallback is non-blocking on bootstrap | With plaintext and encrypted sources missing, npm supplies verified bytes and the initiating install completes while First Finder work is deliberately stalled. Proves PR-08 and FF-01–FF-03. |
| AS-10 | First Finder durable completion | The background job creates one parameter set and one deployment, registers both with the sidecar root, retains the master scalar as escrow custodian, grants the finder the asset's first entitlement with its credential delivered in the mint, and persists seeding of ciphertext and sidecar across restart. Proves FF-03–FF-07 and CD-05. |
| AS-11 | Registry outage for ingested content | After AS-10, a second clean identity installs a pinned dependency closure with NPM unavailable using only the canonical records, the ledger's version index, swarm, an escrow grant from any online holder, and the local application stack. Proves FF-08 and PR-09. |
| AS-12 | Concurrent First Finder race | Multiple finders ingest the same absent version; one wins and every loser cleans up protected state and follows the winner. Proves FF-05 and LC-07. |
| AS-13 | Delivery verification | Mint and transfer proofs verify on-chain in the resolved curve form; malformed, replayed, cross-entitlement, and record-contradicting proofs are rejected; unproven and identity envelope keys are refused; a lapsed lock refunds. Proves CD-01–CD-03, LC-05, LC-08, and LC-09. |
| AS-14 | Free entitlement lifecycle | A new identity is bound once, registers its envelope keys, obtains a free asset-bound entitlement with its credential delivered in the mint through the relayer, and performs repeated per-attempt authorization and local decryption without further transactions. Proves IW-01–IW-04, LC-01–LC-04, CD-01, and RO-01. |
| AS-15 | Paid dogfood and transfer | A funded buyer acquires an explicitly published project-owned package above zero; secondary transfer delivers the buyer's credential by verified proof, the buyer decrypts at the declared tier, and the seller's client destroys at `HARD`. Proves LC-03–LC-05, CD-02, and RO-02. |
| AS-16 | Escrow claim and successor | A verified maintainer claims multiple eligible escrow records with the First Finder absent, gains complete authority, registers its own parameter set, escrow-era credentials keep working, a successor deployment carries both sidecars and shares the plaintext root, the escrow deployment gains the claimant's sidecar by addition and opens under a credential of the claimant's set, and one migrated holder's escrow-era credential is superseded. Proves CD-06, LC-09, and PC-02–PC-06. |
| AS-17 | Adapter incompatibility | Every incompatible required capability combination is rejected during resolution before chain, swarm, authorization, or secret activity. Proves IC-05 and SI-10. |
| AS-18 | Daemon and job recovery | Closing editors and terminals and restarting the daemon or machine does not stop seeding or lose, duplicate, or corrupt durable work. Proves IC-03, FF-03, SW-03, and XA-03. |
| AS-19 | Interval-end destruction | A transfer out stops attempts at the declared tier and destroys decrypt-capable material at `HARD`, a reorganized transfer leaves the seller able to read, and retained plaintext remains reusable under the specified CAS rule. Proves EC-08 and LC-05. |
| AS-20 | Operational evidence | Metrics, traces, health, and diagnostics accurately describe every preceding scenario within the declared latency budget and reveal no protected material. Proves RO-03–RO-06 and XA-04. |
| AS-21 | Cryptographic cost baseline | The validation harness produces capsule, envelope, and proof sizes, decapsulation and proof timings, and delivery-verification gas on the resolved curve, and the piece-group size is chosen from them and recorded. Proves CD-07 and RO-03. |
| AS-22 | Swarm-native publication, empty swarm | With NPM available and no dependencies in the swarm, publishing the project's package leaves every dependency in the swarm afterward. Proves FF-09 and PC-01. |
| AS-23 | Swarm-native publication, partial swarm | With NPM available and some dependencies in the swarm, present dependencies are taken from the swarm, absent ones are bootstrapped, and every dependency is in the swarm afterward. Proves FF-09 and PR-03. |
| AS-24 | NPM outage, complete swarm | With NPM unavailable and every dependency in the swarm, a pinned closure installs from the swarm alone, and a fresh range resolution resolves against the ledger's version index with dist-tags marked best effort. Proves FF-08, PR-03, and PR-09. |
| AS-25 | NPM outage, incomplete swarm | With NPM unavailable and some dependencies absent, every available dependency succeeds, each unavailable one fails, and the install fails against the unavailable ones with an actionable result. Proves PR-03 and XA-04. |
| AS-26 | First Finder gone | After AS-10 the First Finder is permanently offline; a new identity obtains a grant authored by another holder, and holder-assisted authorship is refused for an explicit-publisher deployment. Proves CD-05 and CD-08. |
| AS-27 | Abuse resistance | A lapsed lock refunds; a flood of free mints from many fresh identities never spends beyond the configured subsidy budget and exhaustion is reported truthfully; a standard token transfer without delivery is refused; a revoked verifier key's voucher is rejected; and an attempt proceeds with one node failed or stale and halts on divergent state at one reference. Proves LC-10, LC-11, PC-07, and XA-06. |
| AS-28 | Settings round-trip and store migration | Every catalogue setting is changed from each surface with identical daemon behavior and a consent-trace record; invalid and forbidden values are refused without side effect; each store root is repointed over populated stores with interruption and recovery; seeding controls are honoured live; an export contains no secret and imports on a clean machine; reinstall preserves overrides. Proves ST-01 through ST-08 and IC-04. |
| AS-29 | First-run independence | A new identity installs a closure with NPM available; every ledger-known asset is served by NPM within the budget and the closure's requests are registered in one batched sponsored operation; ciphertext is prefetched, pinned, and seeded; an explicit-publisher asset's request reaches its issuance policy and a priced asset produces a notice and no transaction; the identity goes offline; holders fulfil the requests; on the next run the identity loads its credentials from chain history and every asset shows independent; then with NPM and every other participant unreachable and the plaintext CAS evicted, the same closure installs from local ciphertext under a fresh state view. Proves PR-03, PR-08, PR-10–PR-12, CD-05, LC-12, and RO-01. |
| AS-30 | Cache-only mode | An installation in cache-only mode installs a closure from NPM, installs it again into a second project from local plaintext with everything else unreachable, and produces no chain identity, no request, no prefetch, and no swarm or relayer traffic. Proves SI-20 and PR-06. |

## Completion Boundary

The MVP application environment is complete only when all deployable applications required by the chosen composition are packaged, both onboarding paths reach the same working postcondition, every blocking selection has a compatible implementation, and all acceptance scenarios pass against real application boundaries.

An external product's existence, a mocked adapter, an off-chain-only proof verifier, a manually prepared developer machine, or a successful isolated crate test is useful development evidence but is not completion evidence for this document.

