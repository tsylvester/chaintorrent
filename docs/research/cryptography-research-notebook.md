# Entitlement cryptography research notebook

Last updated: 2026-09-21, America/Chicago. Maintainers: GPT-6 (created 2026-09-15; 2026-09-21 review), Claude Fable 5.1 Max (2026-09-16, 2026-09-20 and 2026-09-21 increments), and subsequent research agents.

## Purpose and authority

This is the working memory for the investigation requested in the conversation that began with [The Foundations of Internet 3.0](https://medium.com/@TimSylvester/the-foundations-of-internet-3-0-237df6013582), [cryptography.md](cryptography.md), and [MVP Scope.md](<MVP Scope.md>). The user requested this notebook after the research repeatedly returned to previously examined issues.

This notebook records requirements, priorities, conclusions, candidate constructions, exclusions, evidence, and the next questions. It is not an adopted cryptographic specification, a security proof, an implementation plan, or permission to change the existing specifications. The user's later clarifications govern this research where they differ from the documents. Hypotheses offered by either the user or an agent are hypotheses, not cryptographic authority.

**Current result:** no complete construction has yet been demonstrated to satisfy the combined requirements. There are useful established components and several concrete research candidates. Failure to complete their composition is not a proof that the objective is impossible. As of 2026-09-21, fixed-depth key randomization (C11) supplies a concrete native-key construction for publisher mint, local access, and voluntary transfer. Its algebra is verified; its complete protocol, implementation costs, and First Finder custody are not. C09/C10 remain comparison candidates. Log 29: the Q06 pass finds a complete construction at the protocol-composition level (Q06 section); the current statement is in the critical-path file. Log 33: the Q09 delivery relations and proofs were independently re-derived and confirmed.

**Resume point:** since log 27 this file is the append-only ledger. Current belief lives in [cryptography-critical-path.md](cryptography-critical-path.md), which states the construction, the open obligations by owner, the accepted residuals, the do-not-re-propose index and the next unit of work; the requirements at their current readings live in [cryptography-requirements.md](cryptography-requirements.md). Read those two first. Consult this file by ID (S, X, E, C, log) when a claim needs provenance, and check S01–S21 and X01–X22 before proposing anything. Each of the two files carries a "current as of log N" stamp; if this log's latest row is higher, re-sync them before resuming. Supplied paper texts live in [docs/papers](papers/); the evidence register links each one.

## Current state described by the documents

The adopted MVP encrypts one canonical, seekable payload per deployment using a shared content key (SCK). A threshold provisioning system checks the requesting identity's current entitlement against settled chain state and returns the SCK in a response wrapped to that identity for an authorization window. Distinct response bytes protect delivery; the unwrapped payload key is the same for every authorized holder of that deployment.

The reference client keeps the response and decrypt-capable material in memory and destroys them, including derivatives and active cipher contexts, at expiry. Subsequent authorization requires new provisioning. The stated expiry guarantee excludes modified clients deliberately retaining material. The threshold provisioner remains a dependency on the read path.

The documents separately describe a later cooperative plaintext-fingerprinting overlay using a variant seed committed by the entitlement. That seed is not currently a construction for distinct native decryption keys. Its secret authorship is unresolved. The current proposal to allow compact decryption helpers must not be confused with this already-described fingerprint overlay.

The two original divergences remain distinct:

1. Individually wrapped responses contain a common SCK instead of native, distinct window material.
2. A threshold provisioning network resolves access instead of the desired entitlement-state and wallet-control mechanism without that network.

Relevant source sections: cryptography's **MVP Posture: Per-Window Provisioning**, **Provisioning Liveness, Centralization, and Participation**, **Recipient-Bound Decryption Credentials**, and the MVP scope's deferred **Recipient- and Window-Bound Decryption Credentials** and **Consensus-Native Provisioning**.

## Requirements and priorities

### Primary research requirements

| ID | Requirement | Current interpretation and provenance |
| --- | --- | --- |
| R01 | One common ciphertext payload per deployment | Every holder obtains the same payload; no full personalized payload or replacement torrent per holder. Explicitly reaffirmed by the user. |
| R02 | Seekable, incremental decryption | Preserve random access and out-of-order piece delivery. Inherited from the cryptographic specification. |
| R03 | Recover the original plaintext | Byte-identical plaintext is preferred. No fingerprinting or watermark is necessary to satisfy the current objective. |
| R04 | Distinct native decryption material per entitlement window | A distinct transport envelope around a common SCK does not satisfy the original objective. Relaxing tracing did not withdraw this requirement. |
| R05 | Authenticate entitlement and holder at every attempt | Check valid issuance lineage, exact NFT identity, current settled ownership, and control of the bound wallet identity before every conforming-client decryption attempt. |
| R06 | Enforce expiry in the conforming protocol | Conforming clients stop decrypting and destroy window material when it expires or entitlement is lost according to the selected state policy. Cryptographic expiry against a modified client is not mandatory. 2026-09-21 (log 22): the user states that a bearer must lose the ability to decrypt the volume when possession of the entitlement transfers, and that retaining previously decrypted plaintext is not the same as retaining the entitlement to decrypt again. Which reading is meant — conforming-protocol loss, protocol-invalid and distinguishable stale material, or cryptographic loss against a modified prior bearer — is interpretation 7, pending the user's answer. |
| R07 | No threshold decryption or key-provisioning network | Consensus may authenticate public state under its BFT assumptions. Secret-sharing a content key among those same validators is still threshold provisioning, not its elimination. |
| R08 | No publisher dependency during reads | Once a valid entitlement exists, its holder must not need the publisher's discretionary cooperation to decrypt. 2026-09-16 clarification: the original issuer may be on the decryption or transfer pathway of an existing entitlement provided it has no selective deniability — for example material pre-published at publication or mint, or delivered atomically with settlement and publicly verified. See P11. Log 15: as a manufacturer may refuse to make more objects but cannot interfere with the use or transfer of one already sold, the publisher's discretion is a right over new issuance (R10) and forbidden over existing entitlements. |
| R09 | Transfer and future recipients | Transfers must support owners unknown when the common ciphertext was created; the buyer needs its own material. 2026-09-16 (log 15): the transfer of an existing entitlement must not depend on the publisher's discretion; the seller's participation in a voluntary sale is an accepted dependency (P13). 2026-09-21 (log 22): if a schema requires the publisher to mint a replacement credential to the new bearer at transfer, that participation is acceptable iff the publisher has no deniability over the transfer or the issuance; the key property is non-deniability of transfer and use of an existing entitlement, not publisher non-involvement. A replacement mutates a member of the minted set without growing the size of the minted set. |
| R10 | Dynamic entitlement issuance | Publishers may mint later without a protocol-imposed supply ceiling. A fixed list of recipients selected at encryption is insufficient. 2026-09-16 (log 15): issuing new entitlements is the publisher's discretionary right; the protocol must not oblige issuance on demand, and publisher-driven scarcity is obligate. 2026-09-21 (log 22): the publisher must be able to mint an arbitrary number of entitlements without knowing how many or to whom, on demand, and must not be pre-bound to a specific number. A supply bound fixed at publication is therefore excluded (X19); the publisher remains free not to mint. |
| R11 | Asset-bound entitlement | A valid entitlement authorizes every live deployment of the asset. A new deployment cannot silently strand existing rights. |
| R12 | First Finder and escrow | Support bootstrap before the publisher arrives, later claim, and transfer of administrative authority without granting publisher rights to the bootstrapper. 2026-09-21 (log 22): escrow and claim must confer no special benefit on the First Finder; the claimant's outcome must equal that of an original publisher, the sole acceptable difference being that the claimant did not price the keys issued during escrow. |
| R13 | Nonselective availability | No particular publisher, helper operator, or provisioner should acquire a unilateral read-path veto; state the actual consensus and data-availability assumptions. |
| R14 | Evaluate the complete lifecycle | Identify who creates every secret and helper at encryption, mint, access, transfer, recovery, and escrow claim. Moving an unresolved issuer into another step is not a solution. |

R09-R13 are preserved from the source documents and previous research scope. Do not drop them merely because the latest conversational message concentrates on expiry, tracing, or helper size.

### Preferences, concessions, and non-goals

| ID | Topic | Disposition |
| --- | --- | --- |
| P01 | No personalized helper | Preferred if a viable construction exists. It is not a reason to reject an otherwise conforming compact-helper construction. |
| P02 | Compact helper or variant object | Explicitly acceptable as a fallback when it is a small fraction of total object size. The NFT may define, authenticate, or reference it; a separate torrent colocated in the swarm is acceptable. |
| P03 | Full-sized personalized helper | Excluded. It defeats the distribution value of the common ciphertext. |
| P04 | Cryptographic dependence on current NFT state | Preferred over a client-only state check, but client enforcement is acceptable for the interim construction. |
| P05 | Modified-client revocation | Desired stronger property, not a current acceptance condition. Retained complete classical decryption inputs are outside the accepted expiry guarantee. |
| P06 | Traitor tracing | Nice to have, not a primary requirement. Do not block the access construction on unstrippable attribution or arbitrary-decoder tracing. |
| P07 | Plaintext watermarking | Optional, only if acceptable for the content. It is neither preferred nor required. |
| P08 | Plaintext redistribution | The project does **not attempt to disallow** it. Do not describe this as encouraging redistribution or add plaintext-control machinery. |
| P09 | Trusted hardware | The source specification rejects proprietary TEEs and hardware DRM. No later instruction has adopted them. |
| P10 | Speed of research | Accuracy, completeness, and precision take priority. Independent mathematical investigation is authorized; do not ask the user to supply a cryptographic solution. |
| P11 | Decryption locality | 2026-09-16: preferentially, decryption is an algorithmic, independent act of the entitlement possessor with no network or request-resolution latency; latency is accepted only where it cannot be avoided. |
| P12 | Variant or helper object availability | 2026-09-16: if a compact object is required for decryption at all, it may be unique to one holder, shared by many, or universal, and is expected to propagate through the swarm so a holder who loses it can recover it. Small size and availability are the design intent. Implied: such an object is not decryption-capable without the holder's private material, otherwise any swarm participant could decrypt. |
| P13 | Forced transfer, lost keys, recovery | 2026-09-16: not protocol obligations. Key custody is the possessor's liability, as with a physical object; a holder who loses a key is expected to purchase a new entitlement, and issuer re-issuance is trivial where it exists. Enabling forced transfer is neutral-to-negative; re-issuance or recovery is neutral-to-positive. None is required or fatal to a solution. |
| P15 | Cryptographic loss of decryption ability at transfer | 2026-09-21 (log 24): the preferred property — a prior bearer's retained material stops working even in a modified client — is not available under the existing constraints (R01 common ciphertext, P09 no trusted hardware, R07 no read-path secret service). Distinguishable stale material (interpretation 7, reading a′) is the working target; conforming-protocol loss (reading a) is the minimum viable construction. Record any construction that approaches (b) under the unchanged constraints; do not relax the constraints to reach it. |
| P14 | Single-participant First Finder bootstrap | 2026-09-21: the initial deployment must work with the First Finder as sole available credential source. Requiring an independent custodian before publication blocks bootstrap and is excluded. Other custodians or holder assistance may improve availability later. This does not make a sole custodian fault tolerant or give it publisher rights. |

### Unresolved interpretations that must remain visible

1. **Window granularity.** The user proposed the ownership interval itself as a sufficient window: while the NFT remains registered to the holder. The current document also specifies bounded subwindows within an ownership interval, with settlement and volume limits. Both are research possibilities; no final replacement policy has been adopted. Every-attempt state checking remains required in either case. A single ownership interval does not require a fresh key at every check.
2. **Native capability isolation.** The document additionally prohibits exposing a common decryption-equivalent secret or deriving another window's capability. The user explicitly relaxed tracing and adversarial expiry, not the original common-SCK objection. Do not quietly treat a persistent asset master key, an easily recoverable common key, or a rerandomized wrap as satisfying R04. If a candidate needs weaker isolation, describe the exact relaxation separately. *2026-09-16 addition (our deduction, S13).* Every entitled holder of a fixed common ciphertext necessarily holds a compact decryption-equivalent object — its own window material plus any helper — so R04 can only constrain what is **common**. Three readings fit the wording and select different critical paths. **(A) Attributability:** the only compact objects that decrypt are per-holder and identify their holder, so leaked material is attributable in principle; only the multi-key-mask body class (C05/C07/C08) reaches this, it needs an issuer or a delegation chain, and it makes issued-key attributability (not arbitrary-decoder tracing) part of the primary requirement. **(B) Issued distinctness:** the material a holder is issued or generates is per-holder even though every decrypter can derive a compact common object such as a DEM key or a per-piece key file; any per-holder-witness construction qualifies, including witness encryption applied per piece, and the remaining divergence is the provisioner (Q02). **(C) Custody:** no party other than an entitled holder ever holds a compact common object at rest; this excludes a provisioner holding an SCK and an issuer holding a traceable-PRF master key, admits a witness-only path whose per-piece keys exist only as a holder's derived values, and routes to Q02. *User decision, 2026-09-16 (log 14):* **(C) is excluded** — any compact object the decryption procedure requires may be unique, shared, or universal and is expected to propagate through the swarm (P12). **(B) is accepted as sufficient.** **(A)'s prerequisites are available:** the NFT ownership history is an authenticated seller-to-buyer chain; the NFT may carry a link, reference, or object enabling programmatic behavior (P02, S08); the original issuer may be on the decryption pathway provided it has no selective deniability (R08 clarification, P11). *Operational criterion for (B) — accepted by the user on 2026-09-16 as sufficient if it yields a valid method, not as obligate (log 15); our deduction, S15:* (i) conforming decryption consumes the holder's identity-bound private material for every piece and produces no reusable common intermediate; (ii) every **compact** decryption-equivalent object a holder can derive either reveals that holder's identity secret or is traceable to the holder; (iii) payload-proportional derived objects (per-piece key files) and non-secret public helpers are acceptable. Clause (ii) is what separates (B) from a wrapped common key (X02): without it, distinct keys that compute one mask are labels on a common key. Clause (ii) is the non-convertibility property of traceable-PRF keys and the non-strippable, non-bearer witness property for witness encryption, so **(A) and (B) share their mathematical core**: the weak form of tracing — issued objects identify their holder — is part of R04's content, while arbitrary-decoder tracing (P06) remains optional. The user acknowledged on 2026-09-16 that clause (ii) is a pathway to traitor tracing (log 15); no clause is obligate, so failing one is a recorded property rather than a disqualification. **2026-09-21 review:** the claim that (A) and (B) necessarily have the same mathematical core is withdrawn. Log 15 makes this a sufficient stronger route, not a definition of every acceptable route. Fresh native keys with ledger-bound commitments may satisfy issued distinctness without self-enforcement or tracing. C11 evaluates that route explicitly. The claim that only the named multi-key-mask class can satisfy (A) was not established as a theorem.
3. **Deletion and persistent credentials.** A wallet identity key may persist. A retained asset decryption root that can generate every new window key is a different capability. Identify it explicitly instead of claiming that erasing only a leaf key destroys all decryption capability. Whether that lifecycle is acceptable is a candidate-specific application decision, not a settled mathematical impossibility.
4. **Helper budget.** Small fraction is qualitative. Measure total persistent bytes, per-holder bytes, per-window bytes, per-transfer bytes, and accumulation over time. A short update token does not imply that the updated ciphertext or helper is short.
5. **Meaning of an attempt.** Piece, file, stream session, and retry boundaries need an eventual protocol definition. Finality, stale state, reorganization handling, and offline behavior must be explicit; a cached historical proof is not proof of current ownership.
6. **Transfer participation and recovery.** A helper produced by the seller may fit an ordinary voluntary sale. Forced transfers, lost keys, account recovery, and administrative transitions may require a different path. Establish the concrete cryptographic dependency before asking which application cases must be supported. *User decision, 2026-09-16 (P13):* forced transfer, lost keys, and recovery are not protocol obligations. The seller's participation in a voluntary sale and the publisher's at mint are accepted dependencies when made non-deniable (R08 clarification). What remains open here is administrative transition — First Finder and escrow claim (R12) — and the atomic, publicly verifiable delivery of any per-holder material at settlement.
7. **Loss of decryption ability at transfer (user statement 2026-09-21, log 22).** The user states that a bearer must lose the ability to decrypt the volume when possession of the entitlement transfers, and that retaining previously decrypted plaintext is not the same as retaining the entitlement to decrypt again. Three readings fit the words. **(a) Conforming-protocol loss:** the entitlement and protocol authorization end at transfer and the conforming client destroys its material (R06); a modified prior bearer that kept complete inputs is the accepted S01 boundary (log 9, P05). C09, C10 and C11 all satisfy this. **(a′) Protocol-invalid and distinguishable stale material:** in addition, the prior bearer's material must not be the same object the new bearer holds, so the protocol can name the authorized credential of the open interval and treat the old one as stale. C10 and C11 satisfy this through path growth or fresh randomization bound by the interval record; C09's shared bearer key does not. **(b) Cryptographic loss against a modified prior bearer:** the retained material must stop working. For a fixed common ciphertext this contradicts S01 and log 9: the prior bearer keeps compatible capsules and a valid key. Reaching (b) would require re-encrypting the body at every transfer (against R01), a fresh secret-bearing component on every read (R07, X12), or hardware (X14); none is available under the unchanged requirements. *User decision, 2026-09-21 (log 24):* the three are an escalation chain, not alternatives. **(a) is the minimum viable construction**, depending on protocol and client conformance. **(a′) is potentially available and indeterminate**; it improves the protocol while still relying on client conformance, and is the working target. **(b) is the preference** but does not appear available under the existing constraints — the common ciphertext (R01) and the rejection of trusted hardware (P09) among them — and is recorded as P15. C11 meets (a′); C09 meets only (a). If a fresh secret bearing component on every read can be constructed without publisher deniability, that would appear to suffice for reaching (b) while preserving the constraints requiring a common ciphertext and refusing a TEE. It is possible that a fresh secret bearing component on every read could be accomplished through the involvement of some method on the NFT in the decryption chain. Involving the publisher in the decryption path, even when undeniable, introduces latency and risk that the publisher is not online, which produces deniability due to lack of availablility for service, so is indistinguishable from selective deniability, but if the method is on the NFT, and the bearer client must be online anyway for the decryption path, the latency and availability risk may be manageable. *Analysis of the user's note (2026-09-21, log 25; our deduction):* a contract method executes in public and cannot itself hold or generate a secret (S03, S08); any value it emits is copyable (X06). What a chain method *can* supply is a fresh public **attestation** — that at epoch `e` the owner of `k` is `pk` — while confidentiality rides on the holder's private key. Reaching (b) with that ingredient needs two things: capsules that accept only the current epoch's attestation, which for a fixed ciphertext means pre-encrypting one capsule set per epoch over the deployment's horizon (about `E` times the sidecar — roughly 120 monthly or 520 weekly epochs over ten years, our arithmetic), and a decryption relation that consumes the public attestation together with the holder's private key — exactly C02's private-witness relation with an epoch term, subject to its open formability and true-statement-security checks (E45 gives false-statement security only). A stale attestation must not open a later epoch's capsule; the attestation is public, so it supplies freshness, never confidentiality. No secret-bearing party acts at read time, so S20 is respected. This is the only route to (b) visible under the unchanged constraints and is recorded under C02 as the (b)-route; C11 supplies (a′) now and is a candidate for the holder-key half of such a relation.

## Settled reasoning: do not repeat without a changed premise

| ID | Conclusion | Limit of the conclusion / reopening condition |
| --- | --- | --- |
| S01 | A modified client retaining complete decryption inputs can replay them against unchanged ciphertext. | This is the boundary of the accepted software model, not a reason to halt the present research. Reopen only for a genuinely different trust or ciphertext model. |
| S02 | Changing NFT state does not erase an old secret or historical state proof. | Mutation authenticates a new owner/window and can invalidate protocol authorization. It does not by itself make saved algebraic decryption inputs stop working. |
| S03 | Public consensus authenticates state but does not automatically create confidential recipient material. | A private witness, pre-existing secret, or specified cryptographic mechanism can still use that public state. Public-chain confidentiality is not categorically impossible. |
| S04 | Knowing a wallet secret and proving ownership is not yet an algorithm for decrypting a previously fixed payload. | A construction must connect that witness to ciphertext compatibility. Hashing public ownership fields produces public material; adding a wallet secret produces a private value but does not supply the missing compatibility relation. |
| S05 | A public transaction signature or public ownership proof is a public witness. | A witness intended to exclude observers must include a private component, such as a secret opening or a separately held decryption signature. Do not publish that same component as the transaction authenticator. |
| S06 | Distinct issued values, attributable issued values, and tracing an arbitrary derived decoder are different properties. | Only the first remains primary. A proof extractor in a security reduction is not automatically a practical tracing procedure. |
| S07 | Basic witness encryption does not by itself prove confidentiality for a true statement with an unknown witness. | Its usual false-statement guarantee is insufficient here. Require extractability, witness hardness with a suitable security definition, or the specialized scheme's appropriate security game. |
| S08 | A public contract method or NFT reference can bind and locate a helper without privately authoring it. | Trace helper authorship and delivery. Contract execution and blockchain consensus do not constitute secret computation unless that capability is actually specified. |
| S09 | Removing a threshold network and removing every secret-bearing participant are different goals. | Secret work at publication or a voluntary transfer may be compatible; a discretionary read-path key service fails the stronger availability objective. Name the actual trust dependency. |
| S10 | Optional tracing does not make common-SCK wrapping into native distinct-key encryption. | Reopen only if the user explicitly relaxes R04 or a proof shows the supposed common value is not a reusable decryption-equivalent secret. |
| S11 | The reviewed RBE/RABE definitions encrypt under parameters after the recipient's registration; their decryption-helper updates do not supply an old-ciphertext retargeting algorithm. [E28-E31] | This is evidence about the reviewed definitions/instances, several at abstract level, not a theorem excluding every possible extension. |
| S12 | An unrestricted public update cannot distinguish an adversary's self-generated key from an authorized key. Identify the authenticated restriction and private resource that exclude that adversary; secret authors and private certified witnesses are two possibilities. | Design test, not a proved exhaustive trichotomy or a formal reduction of every solution to standard witness encryption. Each candidate needs its own algorithms and security game. |
| S13 | A hybrid exposes its symmetric payload key to its decryptor. Independent per-piece hybrids expose a common key file: 32-byte keys give 32 KiB for 1 GiB in 1 MiB pieces. This small but payload-proportional derived object is accepted under reading (B), log 14-15. | No general lower bound proving that a mask construction's smallest common decrypter must be payload-sized was established. Do not call a per-piece hybrid free of common derived material. |
| S14 | At a grant where the recipient's authenticated encryption public key is known, ordinary PKE can transport a credential already authored by a secret-bearing participant. | PKE does not solve authorship or fair settlement. RBE's distinguishing feature is public registration/curation without a master secret, not encryption before a recipient key exists. |
| S15 | Non-convertibility and self-enforcement are stronger potential properties of distinct credentials. Neither follows from different key bytes or a ledger commitment; neither is mandatory under log 15. | Supersedes the assertion that distinctness has security content only through tracing or identity-secret disclosure. C11 supplies native issued distinctness with protocol binding and explicitly limited attribution. |
| S16 | An identity-bound (self-enforcing) key — one whose leak exposes the holder's wallet secret — cannot be bearer-transferred without exposing that secret to the buyer. Per-interval identity binding therefore requires re-keying at transfer, either by a master-secret holder (excluded for existing entitlements as deniable, log 15) or by delegation from the seller's own key (C10). Bearer transfer (C09) yields per-entitlement keys shared along the ownership chain. | Our deduction. Reopen only for a transfer mechanism that changes the bound identity without the participation of either party. |
| S17 | Under log 14-15, master-issued post-encryption key generation — IBE, wildcard or hierarchical IBE, broadcast to every slot, traitor-tracing KEMs — is admissible: the publisher holds the master secret and acts only at mint, keys issued later decrypt the fixed ciphertext, and no party acts on the read or transfer path. S11/X18 excluded registration-based schemes for lacking exactly this master secret; that exclusion stands, and the master-issued family is its complement. | Changed premise recorded in log 14-15. Reopen S17 only if the user withdraws the publisher's permitted role at mint or reinstates reading (C). |
| S18 | In an all-wildcard WIBE ciphertext, any ancestor key can derive a usable key for any descendant identity by filling the wildcard with an arbitrary value. A leaked descendant key therefore attributes to its path prefix as a set, never to the leaf alone, and no choice of identity components yields non-repudiation against ancestors or against the master-secret holder. | Our deduction from the WIBE matching rule [E34] and HIBE derivation. Reopen only for a construction in which usable decryption additionally requires a holder secret that no ancestor can substitute — a mechanism outside WIBE key derivation. |
| S19 | In C11 the buyer can privately rerandomize a delivered native tuple, so the distinctness of the buyer's final key from anything the seller or any earlier holder possesses does not depend on the seller's honesty. A settlement commitment binds the delivered tuple; binding the final tuple needs further proof machinery. | Our deduction from the C11 rerandomization algebra. Exact matching of a leaked tuple to a recorded interval remains optional tracing; a leaker can rerandomize before leaking. An offset-linkage proof at the next sale is that optional machinery, not a correctness condition (X22). |
| S20 | Non-deniability at a protocol step requires that no single party's live action be needed at that step: unavailability is indistinguishable from refusal (user observation, log 25). Pre-published material, seller-side derivation at a voluntary sale, buyer-side rerandomization and public consensus attestations qualify; a publisher oracle on the read path does not, even under a contractual duty. | Our deduction with the user's observation. A bond or penalty motivates cooperation but cannot substitute for availability. |
| S21 | An escrow claim never needs the First Finder. Because encryption under any published parameters is public, the claimant can adopt its own master secret and parameters for all future issuance, keep deployment 0 live for escrow-era credentials, and publish every later body with one sidecar per live parameter set, so escrow-era holders follow new bodies without re-issuance. Handover of the escrow master secret is an optional shortcut, not a dependency; voluntary and involuntary re-keying have the same outcome for the claimant (user observation, log 25). | Our deduction on the C11 structure. The First Finder's retained escrow secret stays plaintext-equivalent (P08); dual sidecars cost one extra capsule set per later body while escrow-era credentials remain in use; pre-claim grant liveness is a separate matter (P14, F2). |
| S22 | The custody paths F0–F2 govern who authors the first credential of a newly minted escrow-era entitlement; they do not touch transfers, which are two-party under every option because the seller rerandomizes its own tuple, and they do not touch legal minting, which is a ledger act separate from credential authorship. F2 and entitlement-level attribution are exclusive by definition: if any holder can author a tuple for any entitlement, a leaked tuple attributes only to the asset. | Our deduction, confirmed by the user (log 28). Binding ledger authorization into the tuple itself would be a C02-type relation. Reopen only for a construction that lets designated non-holders mint without erasing per-entitlement identity, such as the depth-two pattern noted at F1. |

“Encrypt with the user's private key” is not a complete confidentiality primitive. Standard public-key encryption encrypts to a recipient's public key and decrypts with its private key; signatures prove control but are normally public. Any alternative use needs an actual scheme and security definition.

## Backward chain from the observable target

1. **Observable result:** an entitled holder decrypts any requested piece of the common payload to its original bytes, using distinct material assigned to the current entitlement window.
2. **Decryption interface:** conceptually, `Decrypt(C, pieceIndex, D_window, H_optional)` returns the piece. Here `D_window` is secret native material and `H_optional` is a permitted compact helper. This notation specifies an obligation, not an implemented cipher.
3. **Payload compatibility:** every valid window's material must work with that same `C`. The construction must explain this algebraically without simply recovering the old common SCK. This is one unresolved seam.
4. **Authorization binding:** the material and helper must bind asset, deployment, exact entitlement, ownership interval, holder identity, and the selected window context. Include settlement reference, expiry bounds, and freshness identifiers where that window policy uses them. Bind a key-agreement or ephemeral public key when the particular construction uses one.
5. **Private input:** only the authorized holder can supply the required private witness or secret operation. The ledger supplies authenticated entitlement state, not this confidentiality by itself.
6. **Resolution without threshold release:** either the holder's private witness directly drives a suitable encryption primitive, public updates adapt a compact helper, or an authorized secret participant creates a narrowly delegated helper at an allowed lifecycle event. Identify which.
7. **Future-state coverage:** the ciphertext and any persistent public parameters must support later owners and newly minted entitlements, including escrow, without having listed those keys when encrypting the payload. This is the other recurring unresolved seam.
8. **Operational lifecycle:** before each attempt, the conforming client checks current state and wallet control. It destroys expired window material and does not treat retained material as a right. Specify finality, races, helper availability, devices, and recovery.
9. **Established components:** ordinary signatures, public-key encryption, commitments, ledger proofs, and several specialized encryption/delegation primitives exist. A complete protocol must prove their composition and measure its costs. A diagram of components is not that proof.

The present investigation belongs primarily at steps 3, 6, and 7. Steps 4, 5, and 8 still need precise composition, but revisiting the accepted limitation of step 8 does not solve the missing primitives.

## Candidate register

Statuses describe the evidence accumulated in this investigation, not a claim about every paper in a field. **Open** means a concrete proof or construction obligation remains. **Conditional** means the path needs an identified lifecycle or requirement choice. **Parked** means it is not on the current critical path. **Excluded instance** rejects only the described construction.

### C01 — Direct private-witness decryption

**Status: open; strongest conceptual fit to avoiding a standing secret-release service.**

Proposed shape: the holder creates a fresh private window secret or keypair. Wallet-authorized state binds its public commitment/key to a valid NFT context. Decryption uses that private witness together with authenticated chain evidence. The published encryption policy recognizes valid future grants, rather than naming only a fixed holder at publication.

The window secret should not be deterministically regenerable from the permanent wallet key if honest erasure is intended to remove that capability. A private decryption signature must be distinct from the signature or proof broadcast with the registration transaction.

The Nuta–Schuldt–Nishide blockchain construction provides a useful precedent: a fresh private ephemeral value is committed in a signed blockchain record and used as a witness, with erasure after use. It assumes extractable witness encryption. Its recipient key is fixed at encryption and its first-record mechanism is not already a reusable, transferable NFT access policy. [E01]

**Open obligations:** a secure relation accepting unknown future NFT holders; authentic chain continuation and finality; private-witness security for true statements; practical encryption and piece decryption; dynamic minting; First Finder; and compatibility with the canonical payload. Encrypting only the SCK under witness encryption solves neither the native-key divergence nor SCK extractability. Direct payload encryption or another demonstrated payload construction is needed.

**Cost evidence:** one general-purpose 2026 proposal estimates hundreds of terabytes of ciphertext for 100-bit security. This is that proposal's estimate, not a lower bound on witness encryption or on specialized NFT relations. [E02]

### C02 — Specialized signature, commitment, or blockchain witness encryption

**Status: open; most useful narrow-primitive investigation under C01.**

These are positive existence results for restricted relations, not interchangeable implementations of arbitrary policy encryption.

| Subcandidate | Evidence accumulated | Exact missing step |
| --- | --- | --- |
| KZG-opening WE | A specialized opening relation has a core ciphertext of one group element and pairing-based operations. [E03] | An opening of the specified commitment must enforce valid NFT lineage, future ownership, and a private holder witness; those conditions do not follow from compactness. |
| BLS signature WE | Jäger exposes encryption to a fixed verification key and tag, with a signature as witness; a C++ implementation exists. Interface and paper were inspected, not implementation internals or runtime. [E04] | The key/tag are fixed at encryption. A policy admitting future owner keys needs another construction. Published signatures would make their witness public. |
| Homomorphic signature WE | The revised abstract reports several constructions and an implementation. The full current version and benchmarks have not been verified here. [E05] | Aggregating signatures does not itself establish an existential future-owner policy or common-payload decryption. |
| Linearly verifiable SNARK framework | The primary abstract reports composable gadgets, set-membership/DNF applications, and registered encryption. [E06] | No verified gadget composition yet covers the full entitlement-plus-private-witness relation. Do not equate this with arbitrary Groth16 verification. |
| BABE | The inspected paper uses interaction to supply missing input labels in its proving/decryption construction. Its honest-setup figures include 22.2 MiB storage and 126.5 ms decryption. [E07] | Those figures do not demonstrate general noninteractive WE. Identify who supplies the labels and whether that party can withhold reads; a dispute timeout does not recover missing plaintext. |
| T+1-eWEB | Inspected construction uses KZG state commitments, modified BLS signing, and knowledge of the next validators to condition on a specified next-block state slot. [E08] | Next-block, fixed-target coverage is not yet long-lived access for arbitrary future holders. Additional private-holder binding and consensus changes require analysis. The inspected PDF describes practicality limitations. |

Do not turn an unverified arithmetic-circuit WE claim into either a solution or a refutation. The IEEE paper linked in E09 was accessible only at abstract level during this investigation.

**Candidate relation sharpened by Q01 (2026-09-16; qualified 2026-09-21).** One possible route is witness encryption for `R((vk_cons, k), (pk, σ, sk)) := Verify(vk_cons, k‖pk, σ) ∧ pk = g^sk`, where `vk_cons` is the consensus certification key, `k` the asset or entitlement identifier, `pk` the holder's registered key, `σ` the chain's certification of that registration, and `sk` the private component. The encryptor knows only `(vk_cons, k)`; observers know `(pk, σ)`; confidentiality must hold against them. Two checks precede any gadget work:

1. *Ciphertext formability.* With a random-oracle message hash (BLS as in E04) the encryptor cannot form the ciphertext, because `H(k‖pk)` is unknown at encryption. The certification must therefore be algebraic in `pk` (a structure-preserving or Waters-type signature over a group-element message) or the WE must be proof-based (E06, E09). The consensus key must also be stable across validator rotation or certified by a chain of keys the ciphertext can follow; that is a separate obligation.
2. *Public-key substitution hazard.* In pairing-linear gadgets the decryptor's private-scalar term has the form `X^sk` with `X ∈ G_T`. Whenever the observer can obtain a `G_2` element `Z` with `X = e(g_1, Z)` from the projection key, the signature, or the public parameters, then `X^sk = e(pk, Z)` is computable from the public key alone and `sk` is not a private witness. For each candidate gadget, show that no such `Z` lies in the observer's span — including elements formed by pairing signature components that contain `pk` raised to a signing-key power. This derivation is ours and is unverified against the E06 gadgets.
3. *Strippability and bearer witnesses (added after the 2026-09-16 decisions).* If the certification binds `pk` algebraically, check whether the holder can cancel the `pk`-dependence with `sk` — for example cancellation in a same-group construction with an available `g^s` — and obtain a common witness. In a type-3 pairing, if σ lies in G1 and vk in G2, `σ / vk^sk` is undefined; an efficient cross-group map cannot be assumed. If the WE decrypts with a proof or a signature as its effective witness, that object is a bearer object usable without `sk`: in proof-based WE (E06) any valid proof for the fixed statement decrypts. Both may fail the optional stronger clause (ii), not issued distinctness itself. A private bearer proof is not automatically public; its copyability alone is not an outsider-confidentiality break.

The former **formability–substitution–bearer trilemma** is a heuristic for the stronger identity-bound branch, not a theorem or a gate on all acceptable constructions. The substitution test needs the specified public preimage Z; not every pairing-linear term necessarily exposes one. Formability, typed group operations and true-statement/private-witness security remain concrete checks for Q02. The new E45 preprint supplies only false-statement security and does not close that particular gap.

**The (b)-route (2026-09-21, log 25).** Interpretation 7's preferred reading maps onto this candidate: per-epoch pre-encrypted capsule sets whose relation consumes a public consensus attestation of the current owner for that epoch together with the holder's private key. Costs are `E` sidecars over the deployment horizon plus the relation's own ciphertext; the cryptographic gaps are unchanged — formability without the future holder's key, non-strippability, true-statement security — and the attestation supplies only freshness. Recorded so that (b) is pursued here rather than as a contract "method" that would have to hold a secret (S03, S08). The `E` capsule-set cost is not removable by puncturable or forward-secure keys (X21).

### C03 — Registration-based encryption and public compact updates

**Status: excluded as an independent access path (2026-09-16); retained as a component reference. See S11-S14 and X18. The 2026-09-15 status was "open; promising helper path".**

Users generate their own keys; a public curator aggregates registration information and supplies public helper updates without holding a master decryption secret. This directly addresses one source of issuer trust. An implemented registration-based encryption construction reports 914-byte ciphertexts and millisecond encryption/decryption. These are scheme-specific figures, not an NFT payload benchmark. [E10]

Existing registered-ABE correctness reviewed here applies to ciphertext encrypted under parameters that include the registration. It does not automatically give a newly registered user access to an old ciphertext. A changing public key, changing ciphertext header, and changing user helper are three different operations. [E11]

**Open obligations:** public adaptation for future registrations while retaining the common payload; who can create that adaptation; native distinct material rather than a shared-key KEM; bounded helper costs; transfer and repeated windows; dynamic issuance; First Finder; and asset-wide deployment rights.

**Leads dispositioned 2026-09-16** (the 2026-09-15 entry listed these as unfinished): Cuckoo Commitments is a compiler from small- to large-identity RBE and inherits post-registration encryption [E30]; "updatable RBE" is not a re-targeting notion — in RBE an update is auxiliary information the decryptor fetches after later registrations, and its count has a proven lower bound [E28, E29]; the revocable registered-ABE result removes access by deregistration and does not grant access to earlier ciphertexts [E32]; the linear-CRS registered ABE of GLWW and of the E06 framework keep the post-registration model [E06, E12]; the distributed-broadcast lead is a CCA-security result in which the sender fixes the recipient subset from existing public keys [E31]; the lattice RBE registry that supports key insert, remove, and update describes registry operations, not access to ciphertexts created before the key existed [E33]. All dispositions except E28 are abstract-level. Retroactive access without a secret author is, by S12, witness encryption for the certification relation and is tracked under C02/Q02. Delivery of authored material to a known recipient needs only public-key encryption (S14). The family's remaining value here is evidence that a public curator is compatible with self-generated keys, and the E06 gadget framework itself.

### C04 — Compact proxy re-encryption or delegated helper

**Status: conditional/open; plausible placement of secret work at publication or transfer.**

Proposed shape: a secret-bearing party creates a compact delegation or transformation bound to the new holder's public key and NFT context. A proxy need not see plaintext; the NFT authenticates the helper or its hash. The holder uses the helper and its own native secret while checking current state.

The critical distinction is whether the helper transforms a small cryptographic header or requires a personalized copy of the payload. A compact re-encryption key can still be used to transform an entire file. A hybrid PRE system that just rewraps the common SCK remains the baseline divergence.

**Concrete counterexample already found:** in the examined AFGH construction, a recipient with the re-encryption value `g^(a1*b2)` and its secret `b2` can recover `g^a1`. Combined with the original pairing ciphertext, that value is a common weak decryption capability. This is within the paper's distinction between weak decryption secrets and the delegator's full master secret; it is not a break of its stated master-key security. It excludes that use as a solution to the stronger native-material requirement, not every PRE scheme. [E13]

**Open obligations:** collusion-resistant and repeatable delegation; secret/helper author; atomic buyer readiness; later minting; unavailable or lost seller; recovery; First Finder and escrow claim; helper availability; and whether any retained ancestor key decrypts or regenerates all future keys. Binding a helper update into a sale is not enough unless its correctness and buyer usability are established before settlement. Public consensus can verify supplied evidence; it cannot supply an absent seller's secret transformation by itself.

*2026-09-16 decisions applied (wording fixed 2026-09-20, log 15):* seller participation in a voluntary sale and publisher participation at mint are accepted dependencies (P13; R08 clarification). "Seller" in this candidate is the previous entitlement holder who authors the buyer's helper at a voluntary sale — never the publisher; the "unavailable or lost seller" obligation (that previous holder cannot participate: forced transfer, lost key, absence) and "recovery" leave the list under P13. The publisher's discretion over new issuance is unaffected (R10) and its discretion over an existing entitlement's transfer is forbidden (R09). The atomic, publicly verifiable helper-at-settlement requirement above is now the non-deniability mechanism rather than an extra. X10 was re-checked against the (B) criterion: it also fails clause (ii) (compact, identity-free recovered value); the original exclusion ground stands. Under that criterion a delegated helper should be usable only with the buyer's identity-bound secret and should not let the buyer derive an identity-free compact decrypter; the clauses are sufficient, not obligate (log 15).

ERC-7857 is an interface/example lead, not a verified solution. The inspected version described ZK work as unfinished and included TEE-dependent behavior. Do not infer a trust-free construction from the presence of an NFT transfer method. [E14]

### C05 — Key-homomorphic, updatable, or constrained-function helpers

**Status: reviewed instances deprioritized; C11 now supplies a simpler native-key route.**

Prior source checks, consolidated 2026-09-21: E15's Section 5.4 changes a header while preserving a symmetric-key-encrypted body, and its recipient recovers that symmetric key. E16's compactness definition concerns header/token size, not unchanged payload bytes; its chained example encrypts the preceding body again. E17's packed LWE construction updates the body, has a bounded update budget, and its polynomial-matrix tokens scale with the packing parameter. These facts do not prove that every key-homomorphic or updatable scheme fails; they explain why these instances did not advance the preferred compact unchanged-body construction. Do not repeat their initial survey.

The question is whether a compact constrained transformation can connect independent holder secrets to the same payload without revealing a reusable common asset key. Relevant literature includes key-homomorphic constrained PRFs and updatable encryption. [E15-E17]

Simple public key deltas do not solve the problem: a holder with its key and an invertible delta can recover the shared original key. Constraining a delta might reduce what can be recovered, but the asset-level decryption capability and collusion behavior still need analysis.

**Open obligations:** exact transformation equations; security under holder/helper collusion; whether the body changes; whether a header is enough; all sizes; who computes update tokens; and whether the result is truly native distinct decryption or a renamed wrapping layer.

### C06 — Delegatable identity/attribute encryption and local window derivation

**Status: conditional/open lead (2026-09-15); 2026-09-20: its wildcard and hierarchical leads feed C10, and the delegation objection below dissolves under the changed premise (S17, S01).**

Wildcard identity encryption, hierarchical delegation, ABE key randomization, and decentralized authorization systems may support locally derived or restricted keys. They deserve examination now that tracing and modified-client expiry are not mandatory. [E18-E19]

However, a key fixed to one owner generally cannot delegate to a different owner merely by filling additional wildcard positions. Keeping an ancestor credential may retain asset-wide decryption or issuance power. A publisher-issued attribute key also needs an issuance/escrow story.

*2026-09-20:* the objection above assumed the old owner must lose capability when the new owner gains it. S01 accepts retention, and log 14-15 accept the seller as the delegating party at a voluntary sale, so the seller deriving the buyer's child key *is* the transfer. An ancestor's retained power over sub-identities of the same entitlement equals leaking that entitlement's access (S01), not asset-wide power. Publisher issuance at mint is now permitted (R08, R10). See C10.

**Open obligations:** common-ciphertext compatibility; changing owner without transferring the same capability; unbounded transfer depth; native window isolation; persistent-secret accounting; dynamic minting; and whether the cited system encrypts only authorization metadata rather than this payload.

### C07 — Traceable PRFs, traitor tracing, and functional encryption

**Status: parked as a secondary enhancement (2026-09-15); 2026-09-20: partially unparked into C09 — traitor-tracing KEMs are relevant as master-issued, post-encryption, collusion-resistant key generation with per-piece headers, not for decoder tracing (S17).**

Traceable evaluation keys can differ while computing a common pseudorandom mask, giving a conceptual direct-payload route. Existing candidates still need issuer, collusion, cost, and dynamic-recipient analysis. A reviewed older lattice traceable-PRF scheme exposes its master key under two-key collusion; a subsequent construction changes the security and parameter tradeoff. This is a scheme-specific result, not a rejection of all traceable PRFs. [E20-E21]

Pairing-based optimal traitor tracing and registered functional encryption are adjacent results. Do not mistake a traceable KEM for native payload decryption if every recipient obtains the same content key. Do not resume arbitrary-decoder tracing work unless it helps a primary requirement or the user restores tracing to the critical path. [E22-E23]

### C08 — Delegatable multi-key mask along the ownership chain

**Status: optional stronger branch; native issued distinctness now has the concrete C11 route.**

The assertions below about exact full-domain equality and the smallest common decrypter were research targets, not established traceable-PRF guarantees. The reviewed traceable-PRF notion allows negligible disagreement on random inputs; that is not automatically correctness on every sequential piece index. No general compactness lower bound or mandatory tracing requirement is established.

Shape: the body is masked piece by piece by a function that many distinct full-domain keys compute identically, so the smallest common decryption-equivalent object is the mask itself, payload-sized. The publisher authors the first key at mint; at a voluntary transfer the seller derives a fresh, distinct key for the buyer, so neither the publisher nor a network is on the transfer or read path. Under clause (ii) of the proposed (B) criterion the keys must be non-convertible — a holder or coalition cannot derive an identity-free compact key — which is the traceable-PRF property (S15). Without it the distinct keys are labels on a common key and C08 collapses to X02.

Required primitive: a PRF or mask family with (1) many distinct keys computing one function, (2) parent-to-child delegation producing a fresh distinct key, (3) no compact common object derivable by any coalition, and (4) per-piece evaluation cheap enough for seekable reads. Traceable PRFs give (1) and bounded-collusion (3) with a master-key issuer and without (2) [E20, E21]; hierarchical delegation gives (2) for encryption keys, not for a common mask [E18]. Whether a delegatable traceable PRF or an equivalent exists is unsearched (Q07).

**Open obligations (2026-09-16 decisions applied):** framing — the parent must not be able to reproduce the child's exact key, so key generation at a sale must be a two-party step in which the buyer contributes private randomness; atomic, publicly verifiable delivery of the buyer's key material with settlement, so the seller has no selective deniability; First Finder and escrow claim still need an ancestor-key holder (R12, R14); growth of delegation depth over many transfers; coalition analysis across the ownership chain; all sizes. Forced transfer, lost keys, and recovery are no longer obligations (P13). The NFT ownership history serves as the authenticated record of the delegation chain, and the NFT may reference each holder's public helper (P02, P12).

### C09 — Master-issued per-entitlement keys, bearer-transferred with the NFT

**Status: derived 2026-09-20 under the changed premise (log 14-15, S17); instance verification is Q08. Fallback for C10.**

Shape: at publication the publisher runs the setup of a scheme in which any key issued later from the master secret decrypts any ciphertext under the public parameters, and encrypts the body as one header per piece (or per group of pieces) encapsulating a per-piece key, with the piece under that key. At mint — the publisher's discretionary act (R10) — it issues `key_i` for entitlement `i` and delivers it to the first owner's key-agreement key atomically with the mint. At a voluntary sale the seller delivers `key_i` to the buyer's key-agreement key atomically with settlement; the buyer checks the key against the public parameters and against the entitlement's published binding before signing. Reads are local (P11); the conforming client checks state per attempt (R05) and destroys the key on transfer (R06).

Instances (all unverified for this composition): BGW05 broadcast encryption with every slot as the recipient set — two-element headers and keys, public parameters linear in the slot count, slot count fixed at setup [E35] (excluded 2026-09-21 by X19: the user requires no pre-bound entitlement count); wildcard IBE encrypted to the all-wildcard pattern — unbounded identities, header growth per pattern level scheme-specific [E34]; traitor-tracing KEMs, which add decoder tracing and, in some schemes, public traceability [E22, E37].

Properties: keys are distinct per entitlement but shared along that entitlement's ownership chain. This does not supply a distinct key for each ownership interval. Reads and voluntary bearer transfers need no publisher or provisioner; current-state validation still requires a fresh state view. The per-piece hybrid exposes common piece keys, so the earlier claim of no compact common object outside the publisher is withdrawn. The optional stronger clauses (i)-(iii) are not proved merely by carrying an identity index. C11 supplies a native-key-changing alternative.

**Open obligations:** native per-window material, verifiable delivery, setup-bound supply for BGW, implementation costs, and First Finder/claim custody. Publishing a successor and reissuing to known holders is not itself an escrow solution: the original deployment remains live, pre-claim issuance must work, and no holder may be stranded. See the comparison and custody analysis below. A same-parameter successor is compatible with existing valid keys; changing parameters requires an explicit migration construction. Attribution to a chain is optional and does not substitute for R04.

### C10 — Hierarchical delegation along the ownership chain (per-interval keys)

**Status: derived 2026-09-20 from C06's leads under the changed premise (S17); matching semantics verified from the WIBE text [E34]; instance costs and asymmetric derivation unverified (Q08).**

Shape: the body is encrypted, piece by piece, to the all-wildcard pattern `(*, …, *)` of depth `L` in a wildcard identity-based encryption scheme, whose key derivation is exactly that of a hierarchical IBE [E34]. The publisher derives the depth-1 key for identity `(i)` at mint. At each voluntary sale the seller derives the child key `(i, …, buyer)` from its own key and delivers it atomically with settlement. In WIBE an identity matches the pattern when it is no longer than the pattern and agrees with it at every non-wildcard position, and the paper states that any ancestor of a matching identity also matches because it can derive the descendant's key [E34]; so every owner along the chain, at any depth up to `L`, decrypts the same ciphertext with a distinct key that embeds its path.

Attribution: a leaked key names its path, hence the owners up to that depth — later owners are excluded, earlier ones are not. **Non-repudiation against ancestors is not available inside WIBE (S18):** because the ciphertext is all-wildcard, any ancestor can derive a usable key for *any* descendant identity by filling the wildcard with a value of its choice, so a leaked descendant key could always have been made by an ancestor. Using the buyer's wallet secret as its identity component does not repair this — the buyer would then need every ancestor's secret to decrypt, and ancestors can still fill the level arbitrarily. Self-enforcement therefore needs a mechanism outside HIBE key derivation; asymmetric traitor tracing and self-enforcement [E36] remain the literature to check for one. Publisher framing is unchanged: the master-secret holder can derive any key.

Properties: per-interval distinct native material used per piece (clauses i-iii); no provisioner, network, or publisher on the read or transfer path; publisher discretion only at mint; unbounded entitlements, since identities are strings; the seller's retained parent key decrypts forever (S01) and can derive further sub-keys of the same entitlement only, which equals leaking that entitlement's access. Falls back to C09 bearer transfer when the depth bound is exhausted.

**Q08 review correction (2026-09-21):** the full E34 PDF and supplied text are now readable. BB-WIBE uses BDDH; BBG-WIBE uses the stronger decisional L-BDHI assumption, not BDDH. The selective-pattern theorems do not need the random-oracle upgrade used for adaptive patterns. A single fixed public pattern is an appropriate selective-pattern application, but it does not extend the identity domain. Setup fixes maximum depth L. At the all-wildcard pattern of depth L, every legal identity matches, and the security game allows no legal nonmatching-key queries. The former assertion that it protects against holders of keys deeper than L is withdrawn: those keys are outside this setup. This is outsider confidentiality, not revocation of formerly valid keys.

**Remaining obligations:** an implementation and group choice supporting the stated assumptions; verifiable encrypted-key delivery and header authentication; First Finder custody; and the exact window/storage protocol. Prefix attribution and non-repudiation are optional properties, not reasons to block the primary construction. R11 works for correctly formed ciphertexts under the same parameters; incompatible-parameter redeployment needs a separate compatibility mechanism. C11 removes the need to increase depth for every new holder.

### C09, C10 and C11 — verified cost and property comparison

Updated 2026-09-21 from E34's Figure 1 and Sections 6.1–6.2, E35 Section 3.1, and the C11 derivations. Let `bG` and `bT` be the serialized sizes of source- and target-group elements in a chosen secure symmetric-pairing implementation. The former 48-byte/BLS12-381 estimates are withdrawn: these papers do not specify that type-3 implementation, and a target-group element is not a compressed source-group point. No byte benchmark or latency measurement has been performed.

Here `d` is the current hierarchy depth, `L` the configured maximum depth, and `N` the BGW slot count. WIBE costs include the target-group encrypted random message used by the proposed per-piece hybrid; BGW is already a key-encapsulation interface. Identities, descriptors, signatures, proof and transport overhead are additional. The table compares algebraic operations, not runtime.

| Property | C09 / BGW05 all slots | C09 / BB-WIBE depth 1 | C10 / BB-WIBE depth L | C10 / BBG-WIBE depth L | C11a / randomized BB-WIBE depth 1 |
| --- | --- | --- | --- | --- | --- |
| Common capsule per piece | `2bG` | `3bG+bT` | `(2L+1)bG+bT` | `(L+2)bG+bT` | `3bG+bT` |
| Native holder key | `bG` | `2bG` | `(d+1)bG` | `(L-d+2)bG`, including remaining delegation components | `2bG` |
| Public parameters | `(2N+1)bG` | `5bG` | `(2L+3)bG` | `(L+4)bG` | `5bG` |
| Pairings per capsule | 2, plus subset-product work | 2 | `d+1` | 2 | 2 |
| Other decryption work | For a fixed all-slot set the per-user product can be precomputed | One exponentiation for the wildcard identity | About d wildcard exponentiations | About d wildcard exponentiations | One exponentiation for the wildcard identity |
| Native key changes at sale | No | No | Yes, by increasing depth | Yes, by increasing depth | Yes, by fresh randomization |
| Sales before depth exhaustion | No depth limit | No depth limit | L−1 after a depth-1 mint | L−1 after a depth-1 mint | No configured depth limit |
| Issuance ceiling | N fixed at setup — excluded by X19 (2026-09-21) | No separate slot-count parameter | No separate slot-count parameter | No separate slot-count parameter | No separate slot-count parameter |
| Cryptographic identity | Fixed slot | Fixed entitlement ID | Growing ownership path | Growing ownership path | Fixed entitlement ID |
| Holder/window binding | Ledger record and delivery | Ledger record and delivery | Path plus ledger record and delivery | Path plus ledger record and delivery | Commitment to fresh tuple plus ledger record and delivery |
| Main security assumption | Decisional N-BDHE | BDDH | BDDH | Decisional L-BDHI | Inherits BB-WIBE for the same fixed policy |
| Publisher needed after mint | No for bearer sale or read | No for bearer sale or read | No for delegated sale or read | No for delegated sale or read | No for randomized sale or read |
| Retained old key decrypts | Yes | Yes | Yes | Yes | Yes |
| Tracing/non-repudiation established | No for this composition | No | No; see S18 | No; see S18 | No; fresh tuples can be rerandomized again |
| First Finder complete | No | No | No | No | No; see custody paths below |

The identity domains are finite cryptographic domains, not literally infinite supplies. WIBE avoids a separately configured recipient count; a collision-resistant encoding of registry identifiers must still be specified. BB-WIBE keys contain d+1 elements. BBG keys include L−d remaining delegation components and two decryption elements; Figure 1's L+2 is the maximum, not the depth-L leaf size.

If a capsule takes q bytes and covers B payload bytes, its common-sidecar overhead is approximately q/B; total capsule storage is `ceil(payloadBytes/B)*q`. A 1 MiB group makes that fraction `q/1048576`; a 16 KiB piece makes it `q/16384`. This is a size formula, not a claim that pairing latency, serialization, proof costs or small-object economics have been measured. Holder-envelope storage is O(number of recorded windows), with constant credential size per C11a window.

**Conclusion of the comparison:** C11a obtains fresh interval keys at C09's depth-one ciphertext and decryption cost, avoiding C10's finite ownership-path depth. C10 remains relevant if an embedded path is valuable, but no longer needs to be selected merely to get distinct native keys. BBG trades a stronger parameterized assumption and delegation-key storage for fewer pairings at larger depth. BGW retains a setup-bound slot count. None of these facts settles the custody or storage policy.

### C11 — Fixed-depth rerandomization of native entitlement keys

**Status: algebra verified 2026-09-21; proposed protocol composition, not an adopted suite.** This is a third choice beyond C09's bearer transfer and C10's growing hierarchy. It uses the same BB-WIBE construction [E34], at depth one, but changes the native key at each ownership interval without changing the identity encrypted to. The NFT binds the issued key to its holder and interval. The distinction matters: identity-path growth is not necessary for distinct native key material.

**C11a: same-identity BB-WIBE.** In the paper's symmetric prime-order pairing groups, public parameters are `g1, g2, h1 = g1^alpha, u0, u1`; master issuance material is `h2 = g2^alpha`. Map the exact entitlement identifier to a scalar `I` with an authenticated, domain-separated mapping. Define `F_I = u0 * u1^I`. At mint the issuer samples a secret `r` and issues only the group-element tuple `D = (A, B) = (h2 * F_I^r, g1^r)`. It does not issue `r`.

For the all-wildcard pattern `(*)`, encrypt a random group message `M_j` with fresh `t_j` as `C_j = (U_j, V_j, W_j, Z_j) = (g1^t_j, u0^t_j, u1^t_j, M_j * e(h1,g2)^t_j)`. Every valid entitlement key recovers exactly the same message: `M_j = Z_j * e(B, V_j * W_j^I) / e(U_j, A)`. The numerator cancels the entitlement-specific blinding factor in the denominator. These are the depth-one specialization of the paper's equations, not a new confidentiality assumption.

At transfer the seller samples a fresh private offset `s` and computes `D_new = (A * F_I^s, B * g1^s)`. This is a key with total exponent `r+s`, exactly the fresh-key distribution for the same `I`. It requires neither `r` nor the master secret. Size remains two source group elements after any polynomially bounded number of transfers; there is no transfer-depth parameter. Fresh samples give distinct keys except with negligible collision probability. A buyer may privately rerandomize again after delivery, then bind the final commitment to its interval.

**Critical secret handling:** the seller sends the group tuple, never the total randomizer. A holder who learns `r` can compute `h2 = A / F_I^r`. Knowing its own additional offset `s` does not reveal the unknown original total. Private buyer randomization prevents a seller from simply reconstructing that exact final tuple, but supplies no tracing or non-repudiation theorem. A prior holder retains mathematical access if it keeps its old tuple, as already accepted in S01.

**Public-parameter validity check.** For a secret candidate tuple, the holder can check `e(A,g1) = e(h1,g2) * e(F_I,B)`, together with canonical encoding, curve and subgroup validation. In the prime-order model, writing `B=g1^r` and using nondegeneracy shows that this equation forces `A=h2*F_I^r`; the verifier need not know `r`. The tuple must remain secret. Publishing a proof of this check requires zero knowledge; publishing the tuple itself gives observers access.

**Independent re-derivation (2026-09-21, second maintainer).** Decryption correctness of C11a and C11c, the fresh-key distribution of `(A·F^s, B·g1^s)`, and the Q09 substitution `T = P^(r_A+s_A) · R^(-r_B-s_B)` were re-derived by hand and agree with the text above. Two further properties follow from the same algebra and are recorded as our deductions: (1) master-secret hiding — recovering `h2` from any number of valid tuples `(h2·F^r_i, g1^r_i)` requires `F^r_i` from `g1^r_i`, a CDH instance; no holder ever learns its own total exponent, so neither a tuple holder nor a coalition of them can strip a key, and a seller's known offset `s` gives no advantage; (2) in C11a a tuple for identity `I` cannot be converted into a tuple for `I' ≠ I` without `u1^r`, again CDH from `B`, and the validity equation identifies the unique `I` a usable tuple was issued for. So C11a supplies entitlement-level attribution of a usable leaked tuple with non-convertibility across entitlements, while C11c supplies neither; both leave interval attribution optional (S19). Under interpretation 7, C11 satisfies readings (a) and (a′), not (b).

**C11b: same-attribute CP-ABE.** Waters' standard-model construction [E40] is another exact rerandomization instance. Use one asset attribute and one public parameter set per asset. Its key `(K,L,K_a) = (g^alpha * (g^a)^t, g^t, h_a^t)` becomes `(K*(g^a)^u, L*g^u, K_a*h_a^u)` using public bases and fresh offset `u`. Key size is three source-group elements with no transfer-depth growth. The original `t` must not be disclosed. Public-parameter checks are `e(K,g)=e(g,g)^alpha*e(g^a,L)` and `e(K_a,g)=e(h_a,L)`. The paper proves selective security under its stated q-parallel BDHE assumption; its symmetric-group proof is not automatically a proof for a type-3 implementation. Our NFT composition is separate from that theorem. BSW CP-ABE has an explicit Delegate algorithm [E41], but its original generic-group/random-oracle security model is a less attractive implementation basis.

Unlike C11a's fixed entitlement identity, C11b's cryptographic attribute identifies the asset. Every holder can create another working same-attribute tuple; only authorized ledger issuance creates a new legal entitlement. This can simplify credential continuity after an administrative claim, but an available secret-bearing participant is still required to initialize a new holder.

**C11c: a fixed asset identity with independently randomized credentials.** Use one fixed public identity `I_asset` for every credential of an asset, with independent parameters for different assets. Encrypt to that fixed identity. The secret tuple remains `(A,B)=(h2*F^r,g1^r)`, with `F=F_I_asset`; rerandomization remains `(A*F^s,B*g1^s)`. The capsule is `(U,V,Z)=(g1^t,F^t,M*Y^t)` and decryption is `M=Z*e(B,V)/e(U,A)`. This is the fixed-identity specialization of E34: a `2bG+bT` capsule, `2bG` native key and two basic decryption pairings, with no supply or transfer-depth parameter.

Each NFT/window gets an independently randomized native tuple and authenticated encrypted delivery. NFT and holder identities are bound by the ledger/protocol, not by different cryptographic identity strings inside those tuples. Cryptographic key generation creates no legal minting authority. A holder can make more working tuples, just as a modified holder could already share access; only the authorized publisher or escrow contract can create a valid NFT. Strict cryptographic separation between NFTs is not supplied. C11a retains the variant with an embedded entitlement identity.

C11c gives the holder-assisted issuance benefit of C11b with two key elements rather than three. Any existing compatible credential can generate a fresh tuple for a **ledger-authorized** recipient. A publisher, First Finder custodian or claimant may retain such an issuance credential; it can generate fresh tuples without knowing h2 or their total randomizers. Public successor encryption also needs no h2. Recovering the original master secret is therefore unnecessary for protocol-level minting/publishing continuity in this variant. An available secret compatible credential is still necessary; this is not public secret-free issuance. Never publish the discrete logarithm of F relative to g1: together with B, that logarithm strips the key's blinding and reveals h2. Likewise, g2 must not equal g1 or have a publicly known discrete logarithm relative to it: that would expose h2 from the public h1. Independently sampled source-group generators in the source construction must not be replaced with publicly known scalar multiples merely for implementation convenience.

**Common payload and seekability.** Our proposed hybrid uses independent random `M_j` values per piece, derives a symmetric piece key with a domain-separated KDF, and encrypts that piece once. Every holder receives the same encrypted pieces and the same authenticated capsule sidecar. Bind asset, deployment, suite, piece index and geometry in the key derivation/authentication context. No per-holder payload copy or payload-sized helper is produced. A holder-specific encrypted credential is constant-size before transport, proof and context overhead, and can be NFT-linked in the swarm. The all-wildcard header contains three source-group elements and one target-group element; do not call all four 48-byte elements.

This exposes common derived per-piece keys. It does not issue a deployment-wide SCK, and the native credential remains a distinct algebraic decryption key rather than a fresh transport wrapper around that SCK. It fits reading (B) as recorded in log 14-15; it does not meet a stronger prohibition on all common derived material. A collection of piece keys is a reusable small decrypter for that deployment. The optional stronger criterion is not claimed. Grouping pieces reduces capsule/pairing cost but increases how much content one derived key unlocks; it is a published suite choice, not a hidden cache.

**Mint, transfer and use — proposed composition.**

1. At publication the publisher retains asset issuance material, publishes authenticated parameters, and encrypts the common payload and sidecar. First Finder uses the separate custody analysis below.
2. At mint, the publisher authors a valid randomized tuple for the exact NFT. At voluntary transfer, the current holder rerandomizes its tuple. The recipient supplies an encryption public key authenticated by its wallet; the author encrypts the tuple to that key.
3. The settlement record binds the ciphertext/helper digest and a hiding commitment to the tuple, asset, exact NFT, ownership-interval counter, recipient identity/key, suite and chosen window context. Prove that the encrypted tuple opens that commitment and satisfies the validity equation. The witness consists of group elements, encryption coins and commitment opening, **not** the unknown key randomizer. Contract checks of issuance authority/ownership are separate predicates.
4. A transition succeeds only with a valid, available encrypted credential for the buyer. The proof relation is specified; the concrete proof/encryption composition, setup and cost remain Q09. General NIZKs and pairing-equation proof systems exist [E42], but that does not make an arbitrary wallet encryption scheme directly compatible with a particular proof system.
5. For every conforming attempt, validate the current settled NFT owner, issuance lineage, wallet control, interval/window and authenticated payload descriptor, then use the tuple locally. Expiry invalidates protocol use and triggers destruction of in-memory keys, derivatives and contexts. Reading does not contact the publisher, seller or a threshold provisioner; checking current consensus state still needs a fresh state view.
6. The current credential/encrypted helper is persistent for an ownership interval if restart and subsequent voluntary transfer are to work. This is a disclosed change from the MVP's memory-only provisioned responses. Historical helpers may remain in the swarm. Conforming software must not unwrap or use them after entitlement loss. A bounded-subwindow design must separately account for the persistent credential that produces later window keys; erasing only a leaf is not total erasure.

The ownership-interval counter and commitment give exact protocol attribution of an issued tuple. They do not prove that arbitrary leaked material came from that holder: a holder can rerandomize or export piece keys, and master/previous holders can create other valid keys. This limitation is consistent with optional tracing. The NFT is obligatory in the conforming authorization/delivery path; public NFT state is not itself a confidential key generator or a cryptographic expiry device.

**R11 across deployments.** Public encryption under the same authenticated asset parameters works with every existing key, so successor payloads can use fresh piece randomness without reissuing all credentials. Authority-key rotation need not rotate those parameters. A claimant or publisher can also publish a body with one sidecar per live parameter set, since encryption under any published parameters is public; this is how escrow-era credentials follow later bodies without re-issuance and without the First Finder (S21). A successor under incompatible parameters needs an explicit compatibility/migration mechanism; announcing the same plaintext root does not manufacture one. The registry must reject a purported compatible deployment without the required suite relationship. This remains a protocol rule to specify, not a power given to publishers.

**Public capsule validity.** An authenticated capsule identifies committed bytes, not well-formed components. C11c additionally checks `e(U,F)=e(g1,V)`; C11a checks `e(U,u0)=e(g1,V)` and `e(U,u1)=e(g1,W)`, with group/subgroup validation. Prime order and nondegeneracy force the shared encryption exponent. Every valid native tuple then recovers the same M, preventing malformed capsule components from silently selecting among honest holders. Correct plaintext still needs the authenticated content-integrity rules. These checks add work beyond the table's basic decryption pairings and may be cached for immutable authenticated capsules. A public proof of consistent exponents is an alternative to evaluate, not an omitted cost included in the two-pairing figure.

**Atomic delivery is narrower than perfect fair exchange.** A valid proof and an available envelope can establish that a settled buyer has usable material. They do not hide that material until finality: a buyer may see and decrypt a pending or subsequently reverted transaction. An off-chain buyer check before paying has the same issue. A payment/NFT escrow and proof-bound finalization can prevent ordinary withholding after settlement; strict fairness against premature secret extraction needs its own threat model and construction. No threshold-free secret-release oracle is assumed here.

### Q09 increment — verifiable delivery using linear encryption and a representation proof

**2026-09-21: a concrete algebraic composition, derived here; no implementation or independent security audit.** C11a's two secret group elements can be encrypted to the buyer with linear encryption [E43]. Unlike ordinary ElGamal in a symmetric pairing group, this construction uses the Decision Linear assumption, which is intended to remain hard when DDH is easy. It adds that assumption to BB-WIBE's BDDH assumption. The generalized Schnorr/Fiat-Shamir method [E44] below avoids needing a general arithmetic-circuit proof or a Groth-Sahai setup for this particular relation.

Let the buyer's authenticated encryption public key be `(u,v,h)`, with private scalars `(x,y)` satisfying `u^x=v^y=h`. Choose nonzero x,y and nonidentity group generators; the fixed suite may reuse a public h. The buyer proves possession of x,y and signs this encryption-key binding with its wallet identity. These are separate encryption keys, not a claim that an EVM signing key directly implements linear encryption.

For each native key element `Q` in `(A,B)`, independently choose encryption coins `(r_Q,s_Q)` and publish `E_Q=(E_Q1,E_Q2,E_Q3)=(u^r_Q,v^s_Q,Q*h^(r_Q+s_Q))`. The buyer recovers `Q=E_Q3/(E_Q1^x*E_Q2^y)`. The two elements use independent coin pairs. Thus the complete encrypted native credential is six source-group elements.

Define public values `Y=e(h1,g2)`, `P=e(h,g1)`, `R=e(F_I,h)`, and `T=e(E_A3,g1)/(Y*e(F_I,E_B3))`. Substituting the encrypted values into C11a's validity equation shows that the envelope contains a valid native key exactly when its encryption coins satisfy:

- `E_A1=u^r_A` and `E_A2=v^s_A`;
- `E_B1=u^r_B` and `E_B2=v^s_B`;
- `T=P^(r_A+s_A)*R^(-r_B-s_B)`.

All five are representation equations in prime-order groups, sharing the same four secret scalars. No knowledge of the native key's original randomizer or issuance secret is required. This is a substantial simplification over proving a pairing-based decryption algorithm inside a generic circuit.

**Proof protocol.** Use fresh proof blinders `(b1,b2,b3,b4)` and first messages `V=(u^b1,v^b2,u^b3,v^b4,P^(b1+b2)*R^(-b3-b4))`. Hash a canonical, domain-separated transcript containing the complete public statement, V and exact settlement context to a scalar challenge c. The responses are `z=(b1+c*r_A,b2+c*s_A,b3+c*r_B,b4+c*s_B)` modulo the group order. The verifier checks the four source-group equations, for example `u^z1=V1*E_A1^c`, and the target-group equation `P^(z1+z2)*R^(-z3-z4)=V5*T^c`.

The statement/context includes the chain and contract, asset and immutable parameter digest, exact NFT, current and next ownership/window context, seller and buyer identities, recipient encryption key, envelope digest, transaction purpose and expiry. The wallet signatures and contract predicates authorize that particular state change; the representation proof establishes encrypted-key validity. Hashing context into the proof does not replace the chain's authority and current-state checks.

**Why the algebra gives the required check.** Two accepting transcripts with identical first messages and different challenges recover each encryption scalar by dividing the corresponding response difference by the challenge difference. The first four equations force those scalars to be the envelope's coins. Substitution into the fifth then forces the decrypted `(A,B)` to satisfy the native validity equation. Honest-verifier simulation chooses c,z and reconstructs V, giving the usual Sigma-protocol zero-knowledge argument. Fiat-Shamir's noninteractive knowledge/zero-knowledge claims use the random-oracle model and require a complete composition proof; this paragraph is the algebraic argument, not a production-security certification.

**Costs.** Sending `(c,z1,z2,z3,z4)` lets the verifier reconstruct V before recomputing the hash, so the proof contributes five scalars. The envelope plus proof is therefore `6bG+5bZ`, excluding the recipient public key/proof of possession, wallet signatures, context and transport framing. Here bZ is a scalar's encoding size. This is constant per grant/transfer, independent of payload size, entitlement supply and sale count. Proof verification uses source/target-group exponentiations and the pairings needed for T, P and R; fixed per-asset/per-entitlement pairings can be precomputed. No concrete latency, chain gas cost or proof-library performance is claimed.

The encrypted tuple has a unique plaintext under a well-formed recipient key. Its on-chain digest therefore binds delivery to that tuple without publishing it. A separate publicly checkable hash of the plaintext tuple would require additional proof machinery if exact leaked-key matching is wanted; it is optional tracing functionality. Fresh native keys still rely on correct fresh randomization. Different envelope bytes alone do not prove that native keys differ, so that must not become the key-generation rule. The buyer can privately rerandomize its delivered key once more.

**What this closes and what it does not.** It supplies a concrete, compact publicly checkable valid-key envelope for C11a. The linear encryption scheme is CPA-secure; no CCA theorem is claimed for the combined object. Clients must authenticate the committed envelope and capsule before decryption, and the final protocol must analyze substitution, error leakage, replay, concurrent transactions and proof malleability under its precise oracle model. A valid proof does not guarantee swarm availability. Carry the small envelope in an available settlement record or require an explicit data-availability condition; a hash of missing bytes is insufficient. The proof likewise does not prevent pre-finality buyer extraction or make a vanished First Finder deliver a secret. Those remain distinct obligations.

**Second increment (2026-09-21): settlement ordering, freshness and the residual race.** A settlement that (1) locks the buyer's payment, (2) has the seller post the envelope and proof, and (3) has the contract verify the proof and atomically transfer the NFT and release payment removes ordinary withholding in both directions: the seller cannot be paid without posting a valid envelope, and the buyer cannot obtain the envelope before payment is locked. Because a valid proof makes step 3 deterministic, the remaining pre-finality exposure is a dropped or reorganized inclusion of step 2 after the buyer has read it: the buyer then holds a valid tuple without a settled transfer. That is bounded to one entitlement's access and is S01-class — the seller could equally have leaked its own tuple — and the seller may re-post to complete the sale; an expiry or forfeit rule for the locked payment belongs to the protocol specification. Freshness of the buyer's final key does not depend on the seller (S19). The envelope and proof are small enough to live in the settlement record itself, which supplies data availability without a separate helper object; group encoding sizes are implementation-dependent and unmeasured. Nothing here is a fair-exchange theorem. A time-lock or VDF envelope does not close the residual race and is excluded (X20).

### Q09 third increment — verifier environment, type-3 layout, and a source-group delivery proof

**2026-09-21 (log 28): derived here by one maintainer; algebra checked by hand, not independently re-derived, not implemented, not audited.** This increment supersedes the first increment's envelope and proof for the chosen variant C11a; the first increment's relation remains correct in symmetric groups with a target-group-capable verifier.

**Verifier environment, resolved from the specifications.** cryptography.md rules that proofs verified inside a contract are chain-layer artifacts whose scheme follows the target chain's precompiles, and the MVP expects an EVM launch with the chain unselected and the protocol's own chain behind an adapter later. The portable verifier interface is therefore the one every pairing-capable EVM exposes: source-group addition and scalar multiplication, and a check that a product of pairings equals one (E47). BN254 exposes G1 arithmetic and the pairing check everywhere; BLS12-381 additionally exposes G2 arithmetic where the newer precompiles are deployed. No precompile exposes target-group arithmetic or returns a pairing value. Every on-chain check below is written against that interface; the curve stays a parameter. Both curves are type-3, so the symmetric-pairing setting of E34 and E43 is left behind here.

**Type-3 layout for C11a.** Groups `G1, G2, GT` of prime order `p` with `e: G1 × G2 → GT` and no efficient map between `G1` and `G2`. Public parameters `g1, u0, u1 ∈ G1` and `g2, hpub = g2^alpha ∈ G2`; master issuance material is the scalar `alpha` (equivalently `g1^alpha`), retained by the issuer. `F_I = u0 · u1^I ∈ G1`. Native key `(A, B) = (g1^alpha · F_I^r, g2^r)` with `A ∈ G1`, `B ∈ G2`. Rerandomization `(A · F_I^s, B · g2^s)`. Validity `e(A, g2) = e(g1, hpub) · e(F_I, B)`. Master-secret hiding and cross-identity non-convertibility become co-CDH instances (given `g2^r`, compute `F_I^r` or `u1^r` in `G1`). This placement puts the per-holder key's larger element in `G2` and keeps two of the three capsule elements in `G1`, which minimizes sidecar bytes; the mirrored placement costs one more `G2` element per capsule. The type-3 security statement for the depth-one scheme is the asymmetric BB1 selective-identity result (E46, lead), which replaces E34's symmetric theorem for this layout; that citation is an obligation, not yet inspected.

**KEM-form capsule.** Capsule `(U, V, W) = (g2^t, u0^t, u1^t)` with `U ∈ G2` and `V, W ∈ G1`; the encapsulated value is `K = e(g1, hpub)^t`, computed by the encryptor as `Y^t` with `Y = e(g1, hpub)` precomputed once. Decryption: `K = e(A, U) / e(V · W^I, B)`. The piece key is the domain-separated KDF of `K` and the context; the first increment's random target-group message and its `Z` element are dropped, which is the standard identity-based KEM form and the interface the comparison table already credits to BGW. Capsule well-formedness: `e(V, g2) = e(u0, U)` and `e(W, g2) = e(u1, U)`. Capsule bytes in this form: three source-group elements, one of them in `G2`, so about 128 bytes on BN254 and 192 on BLS12-381 with standard compressed encodings; two pairings and one `G1` scalar multiplication per capsule to decrypt. The KEM's security argument is the usual one under the same decisional assumption as the scheme; recorded as our deduction pending the E46 check.

**Envelope under SXDH.** Because DDH is hard in both source groups of a type-3 curve, Decision Linear encryption (E43) is unnecessary; ElGamal in each group suffices. The buyer's encryption key is `pk1 = g1^x ∈ G1` and `pk2 = g2^y ∈ G2` with **independent** secrets `x, y`, each with a Schnorr proof of possession, the pair signed by the wallet. Sharing one secret across the two groups is a hazard: with `g1^x` and `g2^x` both public, `e(g1^(x·rho), g2) = e(g1^rho, g2^x)` is a DDH test in `G1`, and the `G1` ciphertext loses semantic security. Envelope: `E = (C1, C2, D1, D2) = (g1^rho, A · pk1^rho, g2^sigma, B · pk2^sigma)`, two `G1` and two `G2` elements, about 192 bytes on BN254 and 288 on BLS12-381. The buyer recovers `A = C2 / C1^x` and `B = D2 / D1^y` and checks validity locally with three pairings. The first increment's six-element envelope with four coins becomes four elements with two coins.

**The direct validity proof is not precompile-verifiable, and its obvious repair leaks.** Substituting the envelope into the validity equation gives, with public `P = e(pk1, g2)`, `R = e(F_I, pk2)` and `T = e(C2, g2) / (e(g1, hpub) · e(F_I, D2))`, the relation `C1 = g1^rho`, `D1 = g2^sigma`, `T = P^rho · R^(-sigma)`: the first increment's shape with two scalars. Its Sigma protocol has a target-group first message `P^b1 · R^(-b2)` and a target-group verification equation, both outside the precompile interface. Sending the source-group preimages `pk1^b1` and `F_I^(-b2)` in its place is unsound as a repair: from the response `z1 = b1 + c·rho` the verifier computes `pk1^z1 / pk1^b1 = pk1^(c·rho)`, hence `pk1^rho`, which is the ElGamal mask of `C2`, and `A` is exposed (X23). A Groth–Sahai proof (E42) of the same pairing-product statement is precompile-verifiable and constant-size, with a binding CRS derivable from public coins, but is several times larger than the proof below and verified by dozens of pairings; it is the fallback if the chained proof is ever unavailable, not the choice here. A general SNARK over the pairing relation is the cost the first increment was designed to avoid and is not considered.

**Chained source-group proof.** The seller has a source-group witness for its tuple's validity that the direct proof ignores: the tuple is its decryption of the previous interval's envelope. Let the previous envelope on the entitlement's record be `E_o = (C1_o, C2_o, D1_o, D2_o)`, encrypted to the seller's keys `pk1_s = g1^x_s`, `pk2_s = g2^y_s`, and the new envelope `E_n = (C1_n, C2_n, D1_n, D2_n)` to the buyer's `pk1_b, pk2_b`. The seller's held tuple is `(C2_o / C1_o^x_s, D2_o / D1_o^y_s)`, possibly shifted by its own private offset; the sale applies a further offset; write `s` for the total. The seller proves knowledge of `(x_s, y_s, s, rho, sigma)` satisfying six representation equations, three per source group: `pk1_s = g1^x_s`; `C1_n = g1^rho`; `C2_n · C2_o^(-1) = C1_o^(-x_s) · F_I^s · pk1_b^rho`; `pk2_s = g2^y_s`; `D1_n = g2^sigma`; `D2_n · D2_o^(-1) = D1_o^(-y_s) · g2^s · pk2_b^sigma`. The scalar `s` is shared between a `G1` and a `G2` equation, which a generalized Schnorr argument handles with one response. Special soundness: two accepting transcripts with equal first messages extract every scalar; substitution gives `A_n = C2_n / pk1_b^rho = (C2_o / C1_o^x_s) · F_I^s` and `B_n = D2_n / pk2_b^sigma = (D2_o / D1_o^y_s) · g2^s`, the seller's decrypted tuple shifted by `s`, which is valid whenever the previous interval's tuple was. Honest-verifier zero knowledge: the simulator picks the challenge and responses and reconstructs each first message in its own source group. The base case is the mint proof: the issuer proves knowledge of `(alpha, r, rho, sigma)` satisfying `hpub = g2^alpha`; `C1 = g1^rho`; `C2 = g1^alpha · F_I^r · pk1^rho`; `D1 = g2^sigma`; `D2 = g2^r · pk2^sigma`, with `r` shared across groups; extraction gives a tuple of exactly the issued form under the published `hpub`. The induction runs from mint through every sale, anchored by the ledger binding each interval's encryption keys and envelope; the contract supplies `E_o` and `F_I` from the entitlement's own record, so no other entitlement's envelope can be substituted. Private buyer rerandomization after delivery (S19) is preserved: only the total offset enters the next proof, never its split. Publishing every envelope in the settlement record makes the ownership history literally the delegation chain log 14 anticipated. Relation to X22: that row stands as written — linkage adds nothing to correctness where a target-group-capable verifier exists — and the chained proof is adopted here on the changed premise that the verifier has no target-group arithmetic, which X22's disposition did not consider.

**Sizes and verification work.** With Fiat–Shamir the seller sends the challenge and five responses, six scalars, about 192 bytes on either curve. Where the verifier has `G2` scalar multiplication (BLS12-381 under E47's newer precompiles) it recomputes all six first messages and hashes them: about a dozen source-group scalar multiplications and no pairing. Where it has only `G1` arithmetic (BN254) the seller also sends the three `G2` first messages, about 192 further bytes, and the verifier checks each `G2` equation as a pairing product of `G1`-exponentiated elements against fixed `G2` points, `e(g1^z, ·)` terms only, using non-degeneracy of `e(g1, ·)`: twelve pairings, batchable into one product check with hash-derived weights. Mint proofs are the same size class. Prover work is a handful of exponentiations. No gas or latency figure is claimed; the counts are operations against E47's interface.

**Consequences for the composition.** (1) The buyer's envelope secrets `(x, y)` must persist for the whole ownership interval, because they are the witness for the next sale; a holder that loses them keeps reading if it kept its tuple but cannot transfer, a P13-class outcome, and key rotation is a sale to oneself producing a new interval. (2) The settlement record stores the full envelope, four elements, as before; the previous envelope is read from the same record at the next sale. (3) The proof of knowledge of the encryption coins, bound by Fiat–Shamir to the full context including the interval counter and envelope digest, makes the envelope non-malleable and non-replayable in the sense the composition needs; no party ever exposes a decryption oracle, since the buyer decrypts only its own envelope and uses the result only locally and as a zero-knowledge witness, so the CPA-plus-proof-of-knowledge form is the target of the oracle-model statement rather than a CCA theorem. (4) The buyer's local validity check is redundant given the proof and is kept as defence in depth at three pairings. (5) Settlement ordering and the reorg residual are unchanged (second increment, X20). (6) Still open under Q09: an independent re-derivation of the six-equation and five-equation relations as log 23 did for the first increment; the formal oracle-model statement and its Fiat–Shamir argument; the data-availability rule; the locked-payment forfeit rule; and the concrete challenge-hash domain and context schema.

### Q09 fourth increment — formal statement, settlement protocol, data availability and context schema

**2026-09-21 (log 29): the statement to be proved, the protocol around it, and the remaining proof obligations. Same maintainer as the third increment; the algebra was re-walked once more and the BN254 pairing count corrected to about ten after combining pairs that share a `G2` point.**

**Relations.** Mint relation `R_M`: statement `(params, F_I, pk1, pk2, E = (C1, C2, D1, D2))`, witness `(alpha, r, rho, sigma)`, equations `hpub = g2^alpha`, `C1 = g1^rho`, `C2 = g1^alpha · F_I^r · pk1^rho`, `D1 = g2^sigma`, `D2 = g2^r · pk2^sigma`. Transfer relation `R_T`: statement `(params, F_I, pk1_s, pk2_s, pk1_b, pk2_b, E_o, E_n)`, witness `(x_s, y_s, s, rho, sigma)`, the six equations of the third increment. Both are systems of linear representation equations over `G1` and `G2` with scalars shared across groups.

**The proof system.** The generalized Schnorr Sigma protocol for each relation, made non-interactive by Fiat–Shamir with the challenge `c = H(context)` where the hashed context is the full public statement, the settlement context below, and the first messages, and `H` is modelled as a random oracle. Claimed, to be proved in that model: completeness; knowledge soundness by rewinding (forking), so an accepting proof yields a witness; zero knowledge by oracle programming; and simulation soundness, which is needed because published proofs are visible to the confidentiality adversary and follows for Fiat–Shamir Sigma protocols whose statement is hashed (E48, lead). Recomputing first messages from responses is the compact form where the verifier has the group arithmetic; on BN254 the three `G2` first messages travel with the proof.

**Composition claims.** Under SXDH (DDH in `G1` and in `G2`), the random-oracle model, and the type-3 decisional assumption for the depth-one KEM (E46 to confirm): (i) *Delivery soundness.* If the contract accepts settlement `n` for entitlement `I` with registered buyer keys `(pk1_b, pk2_b)`, then the decryption of `E_n` under the unique `(x, y)` with `pk1_b = g1^x`, `pk2_b = g2^y` is a valid C11a tuple for `I` under the published parameters. Induction on `n`: the mint proof extracts a tuple of the issued form under `hpub`; each transfer proof extracts `(x_s, y_s, s, rho, sigma)`, and since `x_s` and `y_s` are the unique discrete logs of the seller keys the contract stored at settlement `n−1`, the extracted plaintext of `E_n` is the seller's actual decryption of `E_o` shifted by `s`. The contract supplies `E_o`, the seller keys and `F_I` from the entitlement's own record, so no substitution across entitlements or intervals is possible. Proof of possession of buyer keys is hygiene, not load-bearing here. (ii) *Ledger confidentiality.* The public transcript — parameters, capsules, every envelope, every registered key, every proof — is indistinguishable from one in which every envelope encrypts random group elements and every proof is simulated. Hybrid over envelopes using DDH in the respective group, with proofs simulated by oracle programming; simulation soundness keeps honest verification meaningful in the hybrids. The chain relation between consecutive envelopes is hidden because each is masked under an independent key with independent coins. (iii) *Non-holder content confidentiality.* Given (ii), an observer with no valid tuple faces the KEM with no key at all: `K = e(g1, hpub)^t` is indistinguishable from random under the decisional assumption. This is weaker than the selective-identity game, since no key-extraction query is needed; every valid tuple decrypts every capsule by design, so confidentiality against other holders is not a goal. (iv) *Attribution.* A usable tuple for `I` cannot be produced from tuples for `I' ≠ I` or from the transcript without solving co-CDH (C11 deductions, type-3 form). (v) *Non-malleability and replay.* The statement hashed into the challenge includes `E_n`, `E_o`, both parties, all four keys, the interval counters, the purpose and the expiry; a proof is valid for exactly one settlement, and the contract rejects a counter it has already advanced. Not claimed: CCA security of the envelope (no decryption oracle exists in the protocol); fair exchange; interval-level attribution of leaked material (S19).

**Settlement protocol, transfer.** T1: the buyer registers `(pk1_b, pk2_b)` with two Schnorr proofs of possession under a wallet signature; a key pair may be reused across entitlements. T2: the buyer locks payment against the exact NFT, the seller, the price and an expiry. T3: before expiry the seller posts `(E_n, π_T)`, supplying `E_o` in calldata; the contract checks `E_o` against the stored digest, loads the seller keys and `F_I`, verifies `π_T`, and on success atomically advances the interval counter, records `(buyer, pk1_b, pk2_b, H(E_n))`, emits `E_n` and `π_T`, transfers the NFT and releases payment. T4: if no valid post arrives by expiry, the buyer withdraws the lock. No seller forfeit is required by any requirement; a listing bond against sellers who let locks expire is a market-design option outside this research. Mint is the same protocol with the issuer in the seller's role, `π_M` in place of `π_T`, no `E_o`, and price set by registry policy, zero pre-claim. Step 3 remains deterministic, so ordinary withholding is excluded in both directions and the only residual is the reorg race (second increment, X20).

**Data availability.** Contract storage holds, per interval, the holder, its two keys and the envelope digest; the envelope and proof themselves are calldata and an event. The holder's persistent credential is `(tuple, x, y, E_o, interval index)`: the tuple for reading, `(x, y)` and `E_o` as the next sale's witness and statement. A holder that loses `E_o` recovers it from chain history through any honest full-history node, which is the R13 statement of this dependency: no single provider has a veto. A holder that loses `(x, y)` cannot transfer (P13). The separate hiding commitment of the first increment is unnecessary: the stored digest of `E_n` binds the delivered ciphertext, and its plaintext is unique under the registered keys.

**Challenge context schema.** `H(domain-tag ‖ suite-id ‖ chain-id ‖ contract ‖ asset-id ‖ parameter-set digest ‖ nft-id ‖ counter-old ‖ counter-new ‖ purpose ‖ seller ‖ buyer ‖ pk1_s ‖ pk2_s ‖ pk1_b ‖ pk2_b ‖ E_o ‖ E_n ‖ expiry ‖ R1 ‖ … ‖ R6)`, with canonical encodings; for mint the seller fields and `E_o` are replaced by a mint marker and the issuer's identity, and `hpub` is inside the parameter-set digest. Domain separation from every other signature and proof in the protocol follows cryptography.md's existing rule.

**Remaining Q09 obligations.** An independent re-derivation of `R_M` and `R_T` by a second maintainer; the written random-oracle proofs of (i), (ii) and (v); inspection of E46 for the exact type-3 assumption behind (iii) and of E48 for simulation soundness; and, at implementation, the concrete hash-to-scalar and encoding choices.

### Q09 fifth increment — proof sketches and the KEM assumption stated exactly

**2026-09-21 (log 30): reductions at the level of a careful sketch, each naming its hybrid steps and the exact place a standard theorem is invoked. Same maintainer. These are not a paper's proofs; they are what a second maintainer or reviewer should check line by line.**

**Preliminaries invoked.** (P1) Generalized Schnorr protocols for systems of linear representation equations in prime-order groups are complete, 2-special-sound and special honest-verifier zero-knowledge, with responses unique given statement, first message and challenge. (P2) Fiat–Shamir in the random-oracle model turns such a protocol into a non-interactive argument with knowledge soundness by forking, zero knowledge by oracle programming, and simulation soundness when the statement is hashed (E44, E48). (P3) Every group element the contract accepts — registered keys, envelope elements, first messages — is validated as a point of the prime-order subgroup; where a precompile does not perform the `G2` subgroup check the contract must (E47 to confirm per curve). (P4) The digest used for stored envelopes is collision-resistant.

**Proof of (i), delivery soundness.** Let the adversary control the issuer, every seller and any buyers it likes, and interact with the honest contract over polynomially many settlements. Suppose with non-negligible probability some accepted settlement `n` for entitlement `I` records buyer keys `(pk1_b, pk2_b)` and an envelope `E_n` whose decryption under the unique `(x_b, y_b)` with `pk1_b = g1^x_b`, `pk2_b = g2^y_b` is not a valid tuple for `I`. Take the least such `n`. If `n = 0`, run the (P2) extractor on the accepted mint proof to obtain `(alpha, r, rho, sigma)` satisfying `R_M`; then `C1 = g1^rho` gives `C2 / C1^x_b = C2 / pk1_b^rho = g1^alpha · F_I^r` and likewise `D2 / D1^y_b = g2^r`, a tuple of the issued form under the published `hpub = g2^alpha`, contradicting invalidity. If `n > 0`, settlement `n−1` delivered a valid tuple to the seller keys `(pk1_s, pk2_s)` the contract stored, by minimality. The seller supplied `E_o` matching the stored digest, so `E_o` is the envelope of settlement `n−1` by (P4). Run the extractor on the accepted transfer proof to obtain `(x_s', y_s', s, rho, sigma)` satisfying `R_T`; `pk1_s = g1^x_s'` forces `x_s'` to be the discrete log of `pk1_s`, and likewise `y_s'`, so `C2_o / C1_o^x_s'` and `D2_o / D1_o^y_s'` are exactly the seller's valid tuple `(A_o, B_o)`. Substituting, `C2_n / C1_n^x_b = C2_n / pk1_b^rho = A_o · F_I^s` and `D2_n / D1_n^y_b = B_o · g2^s`, a valid tuple with exponent shifted by `s`, contradicting invalidity. Extraction fails with negligible probability per proof and there are polynomially many proofs, so the union bound gives the claim. Nothing in the argument uses the buyer's proof of possession, and the seller's own keys are never assumed honestly generated.

**Proof of (ii), ledger confidentiality.** Game `G0` is the real transcript of parameters, capsules, registered keys, envelopes and proofs over polynomially many honest mints and sales; the adversary is an observer, and the same hybrids serve an insider who holds some tuples and asks about others. `G1`: every proof is replaced by a simulated proof, programming the oracle at each challenge point; distinguishable from `G0` only by an oracle collision, negligible by (P2). From `G1` on, no witness is used anywhere. `G2.k` for `k = 1 … m` over the `m` envelopes: in envelope `k` replace the `G1` mask `pk1^rho` by a uniformly random `G1` element. A distinguisher between `G2.(k−1)` and `G2.k` solves DDH in `G1` on `(g1, pk1, g1^rho, pk1^rho)`: the reduction embeds the instance as that envelope's key and coin, generates every other envelope and key itself, and simulates every proof. `G3.k` likewise replaces the `G2` mask `pk2^sigma` by DDH in `G2`. In the final game every envelope is a pair of uniformly random elements in each group and every proof is simulated, so the transcript is independent of every tuple and of every offset. The chain relation between consecutive envelopes never appears because each mask is independent. For an insider who knows tuple `k` and asks about tuple `k+1 = tuple k` shifted by `s`, the same hybrid on envelope `k+1` hides `F_I^s` and `g2^s`, so the shift is hidden. Simulation soundness (P2) is what allows a joint game in which the adversary also submits proofs that the honest contract must still reject when false while honest proofs are simulated; it is not needed for the pure observer.

**Proof of (iii), non-holder content confidentiality, and the assumption it needs.** After (ii) the observer's view of the KEM is the parameters and capsules alone. For one capsule that view is `(g1, u0, u1, V, W) = (g1, g1^a, g1^b, g1^(a·t), g1^(b·t))` in `G1` and `(g2, hpub, U) = (g2, g2^alpha, g2^t)` in `G2`, and the question is whether `K = e(g1, g2)^(alpha·t)` is distinguishable from random. Call this **Assumption A1**: for random `a, b, alpha, t`, given those eight elements, `e(g1, g2)^(alpha·t)` is indistinguishable from a random target-group element. Many capsules reduce to one by a hybrid in which the simulator, holding `g1^a`, `g1^b` and `g2^alpha` from the instance, generates every other capsule with its own `t_k`. Two facts about A1. First, it is not the three-exponent DBDH statement as written: the target has two exponents, and the reduction that would embed a DBDH instance needs `g1^t` to build `V` and `W`, but any instance that supplies `g1^t` alongside `g2^alpha` is trivially solvable through `e(g1^t, g2^alpha)`; the mirrored layout has the same shape. Second, A1 holds in the generic bilinear group model: the exponents reachable by pairing one `G1` element with one `G2` element are the products of `{1, a, b, a·t, b·t}` with `{1, alpha, t}`, and `alpha·t` is not among them or in their linear span, so a generic adversary has only negligible advantage. The claim therefore stands under A1, which is what the specification must name unless E46's asymmetric theorem is proved under an assumption that implies it; that is now the exact question for the E46 inspection. Adding an independent second `G2` generator does not restore a third exponent, because the master element absorbs it and the observer's view keeps the same shape.

**Proof of (v), non-malleability and replay.** The challenge is `H` of the full statement, the context and the first messages. Any change to the envelope, the parties, the keys, the counters, the purpose or the expiry changes the hash input, so a verifying proof for the altered statement requires either a second preimage of the challenge or a fresh witness, and the former is negligible in the oracle model. An identical `(E_n, π)` replayed for the same statement is rejected by the contract's counter, which the statement fixes. Two buyers competing for one NFT lock against the same counter; the first valid post advances it and the second post's statement no longer matches, so at most one settles and the other lock refunds at expiry.

**What (i)–(v) leave for a second reader.** The forking-lemma bookkeeping for many extractions; the oracle-collision bound in `G1`; the exact statement of simulation soundness used, against E48; and whether E46 gives A1 or A1 must be stated on its own. The Sigma-protocol facts (P1) are textbook and are not re-proved here.

### Q09 sixth increment — sources inspected: A1 reduces to DBDH-3b; Fiat–Shamir hypotheses confirmed

**2026-09-21 (log 31): from the LaTeX texts the user supplied in docs/papers. One correction to the fifth increment, one confirmation, one new evidence entry.**

**Correction: Assumption A1 is implied by decisional BDH-3b.** The fifth increment claimed that no reduction from a standard DBDH form applies to the type-3 layout because building `V = u0^t` needs `g1^t`. That was wrong: the embedding that works does not keep the scheme's `g1` equal to the instance's generator. Chatterjee and Menezes (E49, Section 3.1 and Remark 2) define, for generators `ĝ1 ∈ G1`, `ĝ2 ∈ G2`: BDH-3, given `ĝ1^α, ĝ1^β, ĝ1^γ` and `ĝ2^β, ĝ2^γ`, compute `e(ĝ1, ĝ2)^(αβγ)`; BDH-3b, given `ĝ1^α, ĝ1^β` and `ĝ2^β, ĝ2^γ`, the same target; BDH-3c, given `ĝ1^α` and `ĝ2^β, ĝ2^γ`; with BDH-3 ≤ BDH-3b ≤ BDH-3c as problems, so DBDH-3c is the weakest assumption and DBDH-3 the strongest, and BDH-3 equivalent to BDH-2 when the Type-2 relation constant is known (their Lemma 3, Remark 1). Reduction from decisional BDH-3b to A1, renaming its `β ↔ γ` so the instance is `(ĝ1^α, ĝ1^γ; ĝ2^β, ĝ2^γ; Z)`: the simulator sets the scheme's generator `g1 := ĝ1^α`, picks `c0, c1` and sets `u0 := ĝ1^c0`, `u1 := ĝ1^c1`, sets `g2 := ĝ2` and `hpub := ĝ2^β` (so the scheme's master exponent is `β`), and forms the challenge capsule as `U := ĝ2^γ`, `V := (ĝ1^γ)^c0`, `W := (ĝ1^γ)^c1` (so the capsule exponent is `γ`), with `Z` as the candidate `K`. Every published element is uniformly distributed as in the scheme, `V` and `W` are computed without `g1^t` because `u0, u1` are powers of `ĝ1` rather than of `g1`, and `e(g1, hpub)^t = e(ĝ1^α, ĝ2^β)^γ = e(ĝ1, ĝ2)^(αβγ)` is exactly the instance's target. Hence the zero-key observer game of claim (iii) is hard under decisional BDH-3b, a named Type-3 assumption under which E49 also proves BF-IBE-3; the generic-group argument of the fifth increment stands as corroboration, not as the assumption. The mirrored layout admits the same embedding with the roles of the groups exchanged. The specification can therefore name DBDH-3b rather than a bespoke A1; A1 remains the exact intermediate statement.

**E46 inspected.** Boneh–Boyen 2004/172 is symmetric throughout: `e: G × G → G1` in its notation; Setup picks `g`, `α`, sets `g1 = g^α`, random `g2, h_1..h_ℓ ∈ G`, `params = (g, g1, g2, h_1..h_ℓ)`, `master-key = g2^α`, `F_j(x) = g1^x · h_j`; keys `(g2^α · ∏ F_k(I_k)^r_k, g^r_1, …, g^r_j)`; ciphertext `(e(g1, g2)^s · M, g^s, F_1(I_1)^s, …)`. Decision BDH is the 5-tuple `(g, g^a, g^b, g^c, T)` with `T = e(g, g)^abc` or uniform (Definition 3.1). Theorem 4.1: the ℓ-HIBE is IND-sID-CPA with the same advantage under Decision BDH, for any depth and key-query count. No asymmetric-group statement appears, so the Type-3 assumption comes from E49 as above; the depth-one, all-wildcard, zero-key specialization used here needs strictly less than Theorem 4.1 provides. The KEM form is the obvious hashing of `e(g1, g2)^s` and is not discussed in the paper; its security is the reduction above.

**E48 inspected.** The Fiat–Shamir challenge is `H(α, x)` with the statement `x` included (Section 3). Definition 2, quasi unique responses: no PPT adversary finds `(x, α, β, γ, γ')` with both `(α, β, γ)` and `(α, β, γ')` accepting and `γ ≠ γ'`; unique responses is the probability-zero case. Definition 5, unbounded simulation soundness: an adversary with the programmable oracle and a simulator that proves arbitrary, possibly false, statements cannot output a fresh accepting proof of a false statement. Theorem 2: a non-trivial three-round public-coin HVZK proof system with quasi unique responses becomes, under Fiat–Shamir in the random-oracle model, a simulation-sound NIZK with respect to its canonical simulator. Theorem 3: a non-trivial Sigma protocol with unique responses becomes weakly simulation-extractable with extraction error `Q/h`, the extractor running the adversary twice. Application to `R_M` and `R_T`: in a prime-order group the response to a fixed first message and challenge is the unique solution of each linear verification equation, so responses are perfectly unique; the first messages are uniform group elements, so the min-entropy condition ("non-trivial", their Definition 3) holds. Both theorems therefore apply. Weak simulation extractability strengthens claim (i): the extractor works even for an adversary that has seen simulated proofs, which is the joint soundness-and-privacy game the fourth increment mentioned.

**Standing of the fifth increment after this.** Claims (i), (ii) and (v) are unchanged. Claim (iii) is now under decisional BDH-3b via A1. Preliminary (P2) is discharged by E48's Theorems 2 and 3; (P3) is discharged for BN254 and reduced to key registration on BLS12-381 by E47.

### Q09 seventh increment — extraction and hybrid bookkeeping

**2026-09-21 (log 32): the two second-reader items the fifth increment left open, written out. Same maintainer; the independent re-derivation of the relations is in progress by a second agent and is not assumed here.**

**Multi-proof extraction for claim (i).** The fifth increment's argument takes the least settlement index at which delivery fails. A reduction cannot identify that index directly, because it cannot decrypt envelopes addressed to adversarial buyers. The standard repair is index guessing. Let `N` bound the number of settlements the adversary causes. The reduction picks `i* ← {0, …, N−1}` uniformly, runs the adversary against the honest contract, and if settlement `i*` is accepted applies the E48 Theorem 3 extractor to its proof, rewinding the adversary once to the oracle query that fixed that proof's challenge. If `i*` is the first failing index, the extracted witness contradicts validity exactly as in the fifth increment: at `i* = 0` it exhibits a tuple of the issued form, and at `i* > 0` it exhibits the seller's decryption of settlement `i*−1`, valid by the choice of `i*`, shifted by the extracted offset. If the guess is wrong the reduction learns nothing, so the success probability is at least `(1/N)` times the Theorem 3 extraction probability, which is a polynomial in the adversary's success minus the extraction error `Q/h`. In the pure adversarial-prover game no simulated proofs exist and plain knowledge soundness would do; Theorem 3 is invoked so that the same bound holds in the joint game where honest parties' proofs are simulated. A single rewinding suffices for one proof; there is no compounding across settlements because only one index is extracted.

**Concrete loss terms for claims (ii), (iii) and (v).** Let `m` be the number of envelopes, `J` the number of capsules the observer sees, `Q` the number of oracle queries, `h` the size of the challenge space, and `p` the group order. Step `G0 → G1` (all proofs simulated): the canonical simulator picks challenge and responses and programs the oracle at the resulting first messages; each simulated transcript is distributed exactly as a real one by perfect special honest-verifier zero knowledge of the linear-relation protocol, and the programming fails only if the adversary already queried that point, which for uniform first messages happens with probability at most `Q/p` per proof, so the statistical distance is at most `m·Q/p`. Steps `G2.k` and `G3.k`: one DDH instance per envelope per group, so the total advantage across them is at most `m·(Adv_DDH,G1 + Adv_DDH,G2)`. Claim (iii)'s capsule hybrid: `J` steps, each one decisional BDH-3b instance, so at most `J·Adv_DBDH-3b`. Claim (v): a proof accepted for a statement other than the one hashed requires a fresh challenge collision, at most `Q/h` per attempt. The observer's total advantage against the joint confidentiality of every tuple, offset and capsule value is therefore at most `m·(Adv_DDH,G1 + Adv_DDH,G2) + J·Adv_DBDH-3b + m·Q/p + Q/h`, all terms negligible for polynomial `m, J, Q`. The reduction's running time is the adversary's plus `O(m + J)` group operations. No tightness beyond these linear factors is claimed, and none is needed at the protocol's scale.

**What remains for Q09 after this.** Reconciling the second agent's independent re-derivation of `R_M`, `R_T`, the extraction, the pairing-form verification, the leak hazard and the A1 argument; nothing else at the research level.

### Q09 eighth increment — independent re-derivation reconciled

**2026-09-21 (log 33): a second agent re-derived, from the primitives and without trusting the ledger text, every item of the third, fifth and sixth increments. Its report is reconciled here; the corrections below are adopted.**

**Confirmed independently, element by element.** Decryption correctness and the validity equation, including that in the prime-order model the equation forces `A = g1^alpha · F_I^r` given `B = g2^r`, so the set of valid tuples for an identity is exactly the rerandomization class of any one of them. Capsule well-formedness forces a common exponent, so every valid tuple returns the same `K`. The mint relation `R_M` and all three parts of the transfer relation `R_T`, including that only the total offset enters. The six-message Sigma protocol with 2-special soundness and perfect honest-verifier zero knowledge, and perfect uniqueness of responses, which is what E48 needs. The twelve-pairing form of the three `G2` equations. The direct-proof leak and its general principle: a Schnorr response shared between bases `g1` and `pk1` with both first messages public reveals `pk1^rho`. The shared-secret DDH test. The DDH embeddings of the confidentiality hybrid. The A1 monomial argument, and the reduction from decisional BDH-3b by rebasing `g1`, which the agent derived before reading the sixth increment and which matches it under the renaming `β ↔ γ`. The agent adds that DBDH-3c is insufficient by this route, since no `G1` element carries the capsule exponent, and that no SXDH-only reduction should be expected, because the public well-formedness check ties `V` to `g2^t`; the composition therefore rests on SXDH, decisional BDH-3b and the random-oracle model.

**Corrections adopted.**
1. *Pairing and multiplication counts (fourth increment).* "About ten pairings" holds only for the hash-weighted single product; no `G2` point repeats inside any one of the three equations, so an unweighted check is twelve pairings in three precompile calls, and checking the product of three separate equalities in one call is not equivalent without weights. The mint proof is ten pairings unweighted, eight weighted. For a verifier with `G2` arithmetic the scalar multiplications are sixteen, in six multi-scalar multiplications, not "about a dozen". The critical path is corrected accordingly.
2. *Persistent credential (fourth increment).* The stated credential omitted the private post-delivery offset. Resolution adopted: the sale base is the fresh decryption of `E_o`, never the held tuple, so no offset is persisted; the credential is `(x, y, E_o, interval index)`, and the reading tuple is decrypted from `E_o` at start-up and optionally rerandomized in memory. Private rerandomization keeps its S19 role and nothing else depends on it.
3. *Simulated proofs (fifth increment, claim (ii)).* The proofs of possession on registered keys must be among the simulated proofs, because the reduction does not know the discrete log of the embedded key; "masked under an independent key" should read "under independent coins", since keys may repeat across a holder's envelopes and a buyer's key is its seller key at the next sale; in the insider variant the hybrids apply only to envelopes addressed to honest keys; and in the final game `C1` and `D1` stay structured while only the masks are random. The per-proof programming failure probability is at most `(Q_H + Q_P)/p`, counting proof queries as well as hash queries.
4. *Registration hygiene.* The contract must reject identity-element keys (`x = 0` or `y = 0`), which the first increment required and the later ones dropped. Added by this maintainer on the same principle: reject a mint whose `F_I` is the identity element, since then `A = g1^alpha` and the holder would receive the master issuance material outright; hitting it requires `I = −a/b` for the unknown logs of `u0, u1`, so it is not reachable by choice, but the check is one comparison.
5. *Custody consistency (noted, no change).* `R_M` requires the prover to hold the scalar `alpha`, which matches parameter sets as the unit of issuance authority and excludes holder-assisted minting, consistent with C11a and F0.

**Standing after reconciliation.** Every research-level Q09 obligation is closed: relations, proof system, composition claims with bookkeeping, assumption identification, and independent re-derivation. What remains is implementation and measurement, and the specification changes that are separate work.

### First Finder and claim — remaining custody decision for C09-C11

The source documents require pre-claim $0 issuance, bootstrapper erasure after durable escrow, no publisher or consumer right conferred by bootstrap, continuing access through deployment 0, and claimant authority without rights drift. C09/C10's former suggestion to retain the bootstrap master then reissue under a new deployment does not establish those properties. The claimant's optional successor does not replace the obligation to keep the original deployment accessible.

The backward dependency is now concrete: local piece decryption needs a valid native credential; mint/transfer needs an existing compatible credential or issuance secret; a first pre-claim grant has no publisher and no earlier consumer. The First Finder can initialize the secret, but after its required erasure some stated mechanism must preserve confidential authorship for later grants and administrative claim. Public references and consensus validation alone do not supply that secret. This is an unresolved composition/trust choice, not a general impossibility proof.

| Path | How authorship survives | Benefit | Condition or conflict |
| --- | --- | --- | --- |
| F0 — First Finder as initial escrow custodian | The First Finder retains compatible issuance material, serves valid pre-claim grants, and supplies a credential to the authenticated claimant at handover. C11c needs no recovery of the original master secret. | Required initial bootstrap case under P14. Existing holders read and transfer locally; publisher rights remain separate from custody. | Until another compatible credential source exists, pre-claim grants depend on the First Finder being reachable and cooperative; claim completion does not (S21, log 25): the claimant re-keys under its own parameters and publishes dual sidecars for later bodies. This single-participant limit is explicit and accepted for bootstrap; later replication is not a creation prerequisite. |
| F1 — independent replicated custodians | Bootstrap delivers a full per-asset issuance secret to several independently operated custodians, with verifiable encrypted backup/delivery; any one can serve an authorized mint or claimant. | No threshold operation; issued holders read and transfer locally. No particular custodian has a veto while another honest custodian is reachable. | New custody assumption: any one custodian can leak full asset capability; confidentiality is not protected against one corrupt custodian. Availability requires at least one honest reachable retained copy, with replacement and incentives still specified. Cryptographic custody conveys no ledger publisher rights. Needs a business/trust decision. Log 28: if replication is ever wanted with C11a, a depth-two WIBE pattern — asset at level one, entitlement identity at a wildcard level two — would let designated custodians hold a per-asset level-one minting key without the master secret while ordinary holders keep attributable level-two keys; capsule about five source-group elements in KEM form. Noted, not derived. |
| F2 — existing-holder assistance | With C11b or C11c, any compatible asset credential can generate fresh tuples for an authorized claimant or new grantee. | No master-secret reconstruction or dedicated read service; ledger retains issuance authority. C11c lets any compatible holder supply the claimant with sufficient minting capability. | Still needs a confidential first grant and at least one available cooperating credential holder for later grants/claim. Ordinary public-data availability does not ensure this. C11a's fixed-entitlement-identity key cannot simply mint a different identity. |
| F3 — public bootstrap credential | Publish escrow access material because the current bootstrap content and $0 acquisition are already public. | Public swarm availability supplies credentials without a secret custodian. | Changes the requirement that a helper be useless without private holder material; NFT enforcement becomes entirely client-side for initial access. Public plaintext/free price is not permission to assume this concession. |
| F4 — private-witness bootstrap | An authenticated future grant and private holder witness unlock an escrow credential using a suitable restricted/extractable WE construction. | Preserves a route toward no persistent secret author. | C02's specific future-holder relation and efficient secure instantiation remain unsolved. A generic WE title or public consensus signature does not close them. |

**User observation, 2026-09-21:** the First Finder already knows the secret and is a known participant; why not retain it until claim? This is a valid candidate, F0, rather than a mathematical error. It is not recorded as approval to discard the availability requirement. A malicious First Finder can retain or leak the initial secret even in the erasure design. Honest erasure instead limits later compromise exposure and removes an ongoing dependency when custody has moved elsewhere.

**Bootstrap decision settled (2026-09-21, P14).** The user requires the initial First Finder object to work with its sole available source. Independent custody before publication is therefore not an available requirement. The preceding question about whether to permit that initial state is resolved. Evaluate growth from one credential source to more without replacing the starting case.

**F0 lifecycle.** The First Finder initializes random per-asset issuance material and an escrow record, retains that material as operational custodian, and issues distinct credentials only against valid escrow-controlled grants. The contract, not the First Finder's key, determines legal issuance, price and authority. Existing consumers need no First Finder to read or voluntarily transfer. Once a claimant proves authority, custody delivery encrypts the existing issuance material to its bound encryption key and proves compatibility with the registered public parameters. For C11a, master validity is the simple equation `e(h2,g1)=e(h1,g2)`; the same encrypted-group-element proof technique can verify the handover. Successful handover changes custody and issuance administration while retaining the same parameters and original deployment, so existing native credentials keep working. After acknowledged handover the conforming First Finder erases its copy. Deliberate retention is not prevented, just as it was not prevented at initialization.

The significant requirement choice is consequently **liveness during escrow and claim**, not the mere existence of a First Finder secret. Claimant identity verification may succeed on-chain while secret handover stalls; those are different events and must not be reported as complete equivalent ownership. A bond or recorded identity can motivate cooperation but does not reconstruct a lost key. F1 adds independent available copies; F2 supplies another route where a compatible holder credential suffices. F0 alone meets the lifecycle only under an explicit assumption that its custodian remains available and follows the escrow policy until handover. Whether that temporary single-party dependency is acceptable is a business/availability decision, not a missing cryptographic technique.

*Superseded in part, 2026-09-21 (S21, log 25):* claim completion does not stall on handover. The claimant adopts its own parameters for all future issuance, keeps deployment 0 live, and publishes later bodies with one sidecar per live parameter set, which needs only public parameters. The remaining single-party dependency is pre-claim grant liveness, accepted at bootstrap by P14 and mitigated under C11c by F2. Handover remains available as a shortcut when the First Finder is present.

Deferring all issuance until claim fails current pre-claim requirements; F0 revises erasure and availability rather than satisfying them silently; reinstating threshold custody fails R07. F0/F1 can be evaluated without accepting their assumptions, but a fully conforming protocol cannot be declared until its custody assumption or another complete path is selected. Loss recovery is optional; pre-claim issuance and administrative continuity are not.

**R12 equivalence under F0 with C11 (user statement 2026-09-21, log 22).** After handover of the issuance material under the same parameters and deployment, the claimant can mint, price and administer exactly as an original publisher; escrow-era credentials keep working; and the only difference is that the claimant did not price the escrow-era keys, which the user names as the sole acceptable difference. The residual is the First Finder's possible retained copy of the issuance material — a capability it necessarily had from the moment it encrypted the object. With it the First Finder can create decrypting tuples but no legal entitlements, which is plaintext-equivalent and P08-class, not a benefit of ownership. Erasure after acknowledged handover is a conforming-behaviour rule, as at initialization. Under interpretation 7(b) this residual would instead be a violation, as would every prior bearer's retained tuple; under (a) and (a′) it is not. Under S21 the same equivalence holds without any handover: the claimant's own parameters govern all future issuance, and the escrow secret never touches the claimant's deployments.

### Q10 — custody state machine under F0 and S21, and the R11 registry rule

**2026-09-21 (log 29): derived from F0, S21, P14 and the Q09 increments; a protocol description, not a specification change.**

**Parameter sets.** A parameter set `P = (g1, u0, u1, g2, hpub)` with its master scalar is the unit of issuance authority. The registry marks parameter sets **live** per asset. `P0` is created at bootstrap by the First Finder; `P1` is created at claim by the claimant; a publisher may later create `P2, …` by rotation. An entitlement is bound for life to the parameter set it was minted under, because its identity element `F_I` and its mint envelope are under that set's bases; transfers never change it.

**States and transitions.**
- **S0 Bootstrap.** The First Finder samples `alpha0`, publishes `P0`, encrypts deployment 0 with a sidecar under `P0`, and creates the escrow record. It is the sole credential source (P14).
- **S1 Escrow-live.** The escrow contract authorizes zero-price grants; the First Finder authors each grant's mint envelope and `π_M` under `alpha0`. Holders read locally and transfer by the chained proof with no custodian involved. If the First Finder is unreachable, no new grants complete; nothing else changes.
- **S2 Claimed.** The claimant proves authority on-chain under the source documents' identity-proof rules, samples `alpha1`, and registers `P1` as live. All future issuance is under `P1`. `P0` entitlements continue to read deployment 0 and to transfer under `P0`'s public bases. The First Finder is not involved (S21).
- **S3 Handover, optional.** If the First Finder is reachable it may deliver `alpha0` to the claimant encrypted to the claimant's registered keys with a Schnorr proof that the encrypted scalar satisfies `hpub0 = g2^alpha0`; the claimant then also holds `P0` authority. After acknowledgement the conforming First Finder erases (P08-class residual if it does not). Handover changes nothing for holders.
- **S4 Later bodies.** Every later deployment of the asset carries one capsule sidecar per live parameter set, so `P0` holders follow new bodies without re-issuance. Retirement: a set may be marked retired only when the registry shows no live entitlement under it; a retired set needs no sidecar on subsequent bodies.
- **Voluntary migration.** A `P0` holder may at any time request a replacement credential under `P1`: the claimant mints a `P1` tuple for the same NFT and the registry supersedes the `P0` credential in one settlement, which is the "replacement mutates a member of the set without growing it" case the user allowed (R09, log 22). Refusal leaves the holder whole, so non-deniability is not required of this step. Migration shrinks the `P0` population toward retirement.
- **Rotation as recovery.** A publisher that loses its master scalar cannot mint under that set; it registers a new set and continues, at the cost of one more sidecar per body while the old set has live entitlements. Custody of the master scalar is the publisher's liability (P13 applied to the publisher).

**Invariants.** No transition ever requires action by an existing holder or affects its ability to read or transfer. Deployment 0 stays live. Every live set has a sidecar on every body. Legal issuance authority is always the registry's, never a key's.

**R11 registry rule.** A deployment record for an asset is accepted only if it lists a capsule sidecar for every parameter set the registry marks live for that asset at the time of the record; a record that omits one is rejected as incompatible, and "same plaintext root" claims do not substitute. This closes the R11 migration item: there is no incompatible-parameter case, because compatibility is defined as coverage of live sets.

**Growth beyond one source.** Not required. If replication is wanted with C11a, F1 with the depth-two variant (F1 row, log 28) is the recorded route; sharing `alpha` itself among custodians is F1 as originally described.

### Q05 — window, storage and attempt semantics: conclusions

**2026-09-21 (log 29).** These settle interpretations 1 and 5 at the research level; adopting them into cryptography.md and MVP Scope.md is separate work (discipline item 8).

**Window.** The cryptographic window is the ownership interval. Each interval has exactly one native tuple and one envelope key pair; no subwindow keys exist. The MVP's bounded subwindows existed to force re-provisioning through the provisioner; with no provisioner and per-attempt state checks, a subwindow adds nothing for a conforming client, and against a modified client it bounds nothing, since retained material cannot be expired (S01). Any volume or time limit within an interval is conforming-client policy outside the cryptographic scope. Interpretation 3's retained-root question is answered by naming the persistent credential exactly: `(tuple, x, y, E_o, interval index)`, constant size, destroyed at interval end under the rule below; there is no root that regenerates later window keys because there are no later window keys.

**Attempt.** An attempt is one derivation of a piece key from one capsule, hence one per capsule or piece group. Before an attempt the conforming client requires: a state view at the chain adapter's SOFT tier, no older than `τ_soft`, showing the wallet as owner of the exact NFT at the credential's interval index; a wallet-control assertion for the session, renewed every `τ_wallet`; and the authenticated payload descriptor for the deployment. `τ_soft` and `τ_wallet` are suite parameters chosen at implementation against the adapter's tiers, not research quantities.

**Transitions and reorganizations.** On observing a transfer out at SOFT, the client stops new attempts; it destroys the credential and all derived material only when that transfer reaches the HARD tier, so a reorganized transfer never strands a still-owner. On a transfer in, the client uses the new credential once the settlement is at SOFT and the decrypted tuple passes the local validity check. A cached state proof older than `τ_soft` is not proof of current ownership.

**Offline.** No attempts without a fresh state view. The user's stated model is that the bearer client is online for the swarm in any case (log 22 text under interpretation 7), so this is the existing posture, not a new restriction; latency is that of one state read per `τ_soft`, accepted under P11.

**Helper budget (interpretation 4), by formula.** Per capsule: three source-group elements, one of them in `G2`. Per interval on chain: a four-element envelope plus a six-scalar proof, plus three `G2` elements on BN254. Per holder persistent: the credential above, constant. Accumulation: linear in intervals in chain history, constant per holder, none per read. For a 16 KiB piece group the sidecar is below one percent of payload on either curve, and for a 1 MiB group a few hundredths of a percent. Byte measurement on a chosen curve remains the only open part.

### Q06 — whole-target evaluation

**2026-09-21 (log 29): first pass over the assembled construction — C11a in type-3 groups, KEM capsules, SXDH envelopes, chained delivery proofs, F0 custody with S21 claim, the Q05 client rules. "Met" means at the protocol-composition level under the cited residuals; nothing is proved or measured.**

| ID | Verdict | How, and residual |
| --- | --- | --- |
| R01 | Met | One encrypted payload and one common sidecar per live parameter set; both common to every holder. |
| R02 | Met | Independent capsules per piece or group; any order, any offset. |
| R03 | Met | Byte-identical plaintext; no watermark. |
| R04 | Met, reading (B) | Fresh native tuple per interval with a fresh total exponent; entitlement-level attribution in addition. Residual: common per-piece `K` values (S13). |
| R05 | Met | Q05 attempt rule: SOFT-tier ownership at the credential's interval, wallet assertion, authenticated descriptor. |
| R06 | Met at (a′) | The settlement record names the interval's credential by envelope digest; destruction at HARD finality of transfer out. Residual: (b) unavailable (P15). |
| R07 | Met | No provisioner, no threshold operation; the contract only verifies public proofs and orders state. |
| R08 | Met | Reads are local; the publisher acts only at mint, which is discretionary anyway; no read-path party. |
| R09 | Met | Seller-only transfer by chained proof; buyer unknown at encryption; publisher absent. Residual: reorg race, S01-class (X20). |
| R10 | Met | Unbounded identities; discretionary mint under the master scalar; no pre-bound count (X19). |
| R11 | Met | Sidecar-per-live-set rule (Q10); no incompatible-parameter case remains. |
| R12 | Met | F0 bootstrap, claim without the First Finder (S21), claimant equivalence except escrow-era pricing. Residuals: pre-claim grant liveness (P14); retained escrow copy, P08-class. |
| R13 | Met, assumptions stated | Read path needs the swarm for payload and sidecar, chain history for envelope recovery, and a state view; any honest node suffices for each; no single operator veto. |
| R14 | Met | Secrets and who holds them: `alpha` (issuer, at publication and mint); `r`, `rho`, `sigma` (issuer or seller, at mint or sale, `r` never disclosed to anyone); `s` (seller, at sale); `x, y` (holder, for the interval); `t` (encryptor, per capsule, discarded). No unresolved author remains; recovery is a non-obligation (P13). |

Preferences: P01, P02, P03, P08, P09, P10, P11, P12, P13, P14 met; P04 met at settlement time by proof, at read time by client check, as the accepted interim; P05 and P15 not met, as recorded; P06 partly delivered, entitlement-level, with interval-level optional; P07 unused. Interpretations: 1, 3, 5 settled (Q05); 2, 6, 7 previously settled; 4 settled by formula, measurement pending.

**Verdict.** Under the requirements as they stand after logs 14–28, a complete construction is specified at the protocol-composition level for every requirement, with the residuals recorded above and no requirement unmet except the two preferences the user has already classed as unavailable. What separates this from a buildable, adoptable design is not a missing mechanism: it is the written random-oracle proofs of the Q09 claims, inspection of E46 and E48, an independent re-derivation of the Q09 relations, implementation on a chosen curve, and measurement. Adoption into the specifications is separate work.

## Exclusion and deferral ledger

| ID | Method or assumption | Disposition and reason | Reopen only if |
| --- | --- | --- | --- |
| X01 | Wrapped SCK via threshold release | Existing MVP baseline; fails both original target divergences. | The user adopts a baseline comparison or explicitly relaxes the target. |
| X02 | Wrapped SCK via one server, HPKE, or a conventional hybrid PRE/ABE/KEM | Removing threshold operation alone leaves a shared extracted payload key, and may add a single read-path veto. | A native-payload composition or explicit requirement change is supplied. |
| X03 | Full-sized personalized ciphertext or XOR helper | Excluded by the user because personalized data is comparable to payload size. | A proved compact construction replaces the full-size object; do not merely call its PRG seed a compression without showing how the correction is generated. |
| X04 | Hash of public NFT/owner/state fields as secret key | Public observers compute the same value. | A real private input and ciphertext-compatibility construction are specified. |
| X05 | NFT mutation as automatic destruction of old mathematical capability | False for retained compatible inputs; current-state protocol invalidation is still useful. | The mechanism changes the retained-input premise or trust model. |
| X06 | Public chain witness alone as exclusive decryption witness | Observers can copy it. | A private, holder-bound witness is included and supported by the security game. |
| X07 | Plain WE false-statement security as sufficient proof | Does not establish the needed true-statement/private-witness confidentiality. | A suitable strengthened or specialized security proof is provided. |
| X08 | Fixed-recipient broadcast/SWE as a complete dynamic-NFT solution | Does not by itself admit keys and entitlements unknown at encryption. | A verified public update/delegation mechanism handles old ciphertext and future holders. |
| X09 | Public registration automatically unlocks all old ciphertexts | Not guaranteed by the reviewed RBE/RABE correctness definitions. | A specific scheme proves the required retroactive or updatable correctness. |
| X10 | AFGH instance described in C04 | Exposes the examined common weak decryption capability to recipients. Re-checked 2026-09-16: it also fails clause (ii) of the (B) criterion (compact, identity-free recovered value); the original exclusion ground stands. | Use a materially different construction and analyze its exact collusion/decryption properties. |
| X11 | Linear public key delta | Recipient plus delta recovers the shared key in the simple construction. | A constrained or different transformation has a proof excluding that recovery. |
| X12 | Threshold MPC/FHE/KMS operated by existing validators | Reuses consensus participants but retains threshold secret custody or release. | The user permits it, or the selected construction demonstrably avoids that operation. |
| X13 | Time release or consensus randomness alone | Public release is not exclusive holder-bound access; expiry and owner change are separate. | A private-holder binding and complete lifecycle are shown. |
| X14 | Hardware-enforced erasure or TEE gate | Outside the stated open-software/no-hardware-DRM posture. | The user changes that application requirement. |
| X15 | Cooperative watermark as a decryption credential | Alters/render-marks plaintext and can be omitted; it does not solve either original divergence. | Used as an explicitly optional addition after the access construction works. |
| X16 | Proof extractor treated as an operational traitor tracer | The existence argument does not provide a practical tracing algorithm. | An actual tracing procedure and relevant security experiment are supplied. Not a primary blocker. |
| X17 | Abstract, interface, or benchmark treated as complete composition | The unexamined relation, setup, interaction, or payload step may be precisely the missing part. | Full construction and applicability have been checked. |
| X18 | Reviewed RBE/RABE/distributed-broadcast instances as retroactive access paths | Their inspected correctness/API does not provide access to ciphertext created before the relevant registration. PKE solves delivery of an already-authored credential at a known-recipient grant, not public retroactive authorship. | An actual scheme with the required cross-epoch algorithms and security game; evaluate its stated resource, without presuming every extension is formally equivalent to WE. |
| X19 | Broadcast encryption with a slot count fixed at setup (the BGW05 all-slot instance of C09) | The user requires that the publisher not be pre-bound to a specific number of entitlements and be able to mint an arbitrary, unknown number on demand (R10, log 22); a setup-time slot count violates this even when the publisher chooses it. | An unbounded-slot variant with comparable header and key costs, or the user accepts a publisher-chosen bound. |
| X20 | Time-lock puzzle or VDF envelope to close the Q09 pre-finality reorg race (proposed 2026-09-21, log 26) | A puzzle delays opening; it does not condition opening on settlement. A buyer whose step-2 inclusion was reverted still solves the puzzle after the delay and holds a valid tuple, so the leak is unchanged. Conditioning on finality needs a value secret before step 3 and public after: a contract cannot hold it (S03, S08) and a live seller releasing it reintroduces the withholding the escrow ordering removed. Conforming buyers already wait for settled ownership (R05), so the delay only slows a modified client, adds read-path latency against P11, and replaces the Q09 five-equation Sigma proof with a cross-group circuit proof over a hidden-order group and a sequentiality assumption with no evidence entry. The residual stays S01-class and bounded to one entitlement. | An opening condition tied to settled state that no secret-bearing party has to perform at step 3. |
| X21 | Puncturable or forward-secure KEM as an O(1) replacement for the (b)-route's per-epoch capsule sets (proposed 2026-09-21, log 26) | Puncturing is an operation the holder performs on its own secret key; public chain state cannot alter what a modified client has stored, so a prior bearer that keeps its unpunctured key decrypts every later epoch. This is X05 and S02 with a primitive name. Forward security protects old ciphertexts from a later key compromise, the opposite direction from excluding an old key from new epochs; that direction needs per-epoch capsules or a fresh per-epoch secret delivered to the current holder, which is a read-path secret-bearing party (R07, X12). Dominated: C11 already reaches (a′) with one capsule set for conforming clients, and nothing changes for modified ones. Leaves the (b)-route's formability, non-strippability and true-statement gaps untouched. | A scheme that removes an old key's capability for later epochs by an operation neither the old holder nor a read-time secret-bearing party performs, under R01 and P09. |
| X22 | Mandatory proof of knowledge of the rerandomization offset at the next sale, linking the new envelope to the previous settled envelope (proposed 2026-09-21, log 26) | Q09's representation proof already forces the validity equation on every envelope and needs no offset or randomizer. All valid tuples for one entitlement identity are interchangeable for decryption and non-convertible across identities (CDH), so linkage adds no correctness; ownership continuity is the ledger's NFT record, not an algebraic chain of offsets. The proof is feasible with the same generalized-Schnorr technique, but its only use is S19's optional final-tuple binding, and a leaker rerandomizes before leaking. Not a Q08 item. Log 28: the same chained structure is adopted in the Q09 third increment for a different reason — it is the only Sigma-protocol form whose every equation lies in a source group, which the precompile verifier environment requires; this row's correctness finding is unchanged. | The user elevates interval-level tracing from optional to required; even then it is tracing machinery, not a correctness condition. |
| X23 | Source-group substitutes for the target-group first message of the direct validity proof, i.e. sending `pk1^b1` and `F_I^(-b2)` in place of `e(pk1, g2)^b1 · e(F_I, pk2)^(-b2)` to make it precompile-verifiable (checked 2026-09-21, log 28) | Unsound as a repair: with the response `z1 = b1 + c·rho` public, `pk1^z1 / pk1^b1 = pk1^(c·rho)` yields `pk1^rho`, the ElGamal mask of `C2`, and the delivered `A` is exposed to every observer. The target-group first message is what hides the mask. | Never; use the chained source-group proof or a Groth–Sahai proof of the pairing-product statement (Q09 third increment). |

No row above is a universal impossibility claim about a whole encryption family. Record the exact failed instance or unsupported inference before expanding an exclusion.

## Next research queue

Each investigation must answer its assigned question or record the exact missing evidence. Merely renaming a family does not advance the queue.

| Order | Question | Required output / completion condition | State |
| --- | --- | --- | --- |
| Q01 | Can public registered-encryption updates grant future registrants access to a fixed old payload? | Exact cross-epoch correctness, secret author, header/body changes and sizes. | Reviewed instances closed negative on 2026-09-16; evidence scope corrected 2026-09-21 (S11-S14). Parked, not a family-wide impossibility theorem. |
| Q02 | Can restricted WE express a future authorized holder with a private witness efficiently? | A precise relation and supported primitive; distinguish fixed verification keys, state-slot equality and arbitrary future authorized keys. | Secondary issuer-free branch. C02 records typed formability and witness-security checks; optional non-bearer tracing does not gate issued distinctness. New E45 supplies false-statement security only. 2026-09-21 (log 25): also carries the (b)-route of interpretation 7 — per-epoch capsule sets plus a public epoch attestation and the holder's private key. Log 26: puncturable/forward-secure keys do not remove the per-epoch cost (X21). |
| Q03 | Can compact delegation produce native independent window material without a persistent common decrypter? | Follow every secret through publication, mint, window creation, transfer, devices, recovery, First Finder, and claim. Supply a concrete construction or a precise counterexample. | Open; AFGH instance excluded, broader PRE not excluded. |
| Q04 | Can constrained/key-homomorphic updates improve the common-payload composition? | Exact equations, collusion behavior, body changes and sizes. | E15-E17 instance results consolidated in C05; deprioritized behind C11. Per-piece common keys are accepted under reading (B), so no mandatory traceable-mask gate remains. |
| Q05 | Does a candidate require a lifecycle concession? | A concrete business/UX choice after explaining its cryptographic dependency. | Log 14-15 settled issued distinctness, optional stronger tracing, publisher discretion at mint, voluntary seller participation and optional recovery. C11 now leaves ownership-interval versus bounded-subwindow storage, and Q10's non-threshold escrow custody assumption. No need to choose growing hierarchy solely for distinct interval keys. 2026-09-21 (log 22, 24): the R10 bound question is closed (X19); interpretation 7 is settled as an escalation chain with (a′) as the working target. Remaining Q05 items: ownership-interval versus bounded-subwindow storage, and Q10's custody growth policy. Log 27 proposes adding the C11a versus C11c choice and interpretation 5's attempt semantics. Log 28: the user chose C11a with F0 custody; the a/c choice is closed. Log 29: window, storage and attempt semantics concluded (Q05 section); closed at the research level, adoption into the specifications pending. |
| Q06 | Does the assembled candidate satisfy the whole target? | Evaluate R01-R14, preferences, all unresolved interpretations, setup assumptions, confidentiality, authenticity, lifecycle, availability, and measured costs. | Log 29: first pass completed (Q06 section). Every requirement met at the composition level under recorded residuals; P05 and P15 unmet as classed by the user. Remaining: proofs, E46 and E48 inspection, independent re-derivation of the Q09 relations, implementation, measurement. |
| Q07 | Does a delegatable multi-key mask (C08) exist: distinct full-domain keys computing one function, fresh-key delegation, coalition resistance? | Named primitive and source, or a precise statement of what is missing; framing analysis; issuer at each lifecycle event; sizes. | Answered in shape 2026-09-20 by C10: hierarchical key derivation supplies (1) distinct keys decrypting one ciphertext, (2) fresh-key delegation, and (3) collusion resistance, with per-piece headers instead of a mask; payload-proportional per-piece keys satisfy clause (iii), so the mask form is unnecessary. The mask variant stays unsearched and is not needed. |
| Q08 | What are the exact costs and properties of C09/C10 and the new C11 alternative? | Source-supported group/operation counts, depth and supply bounds, validity checks and security scope. | Algebraic comparison completed 2026-09-21 above C11. Corrected group-byte assumptions, BBG assumption, and unsupported deeper-key adversary claim. Concrete group/implementation performance remains unmeasured. Delivery and escrow move to Q09/Q10. Log 28: type-3 placement fixed for C11a (`A ∈ G1`, `B ∈ G2`; capsule `U ∈ G2`, `V, W ∈ G1`), KEM-form capsule adopted, curve left as BN254 or BLS12-381 per the launch chain; E46's asymmetric statement is the remaining citation obligation. Log 29: helper-budget formulas recorded under Q05; byte measurement on the chosen curve is the only open part. |
| Q09 | Can a publicly verifiable encrypted credential be delivered non-deniably at mint/transfer? | Compose recipient encryption, native-key validity, commitment and exact ledger context in a sound zero-knowledge proof; account for data availability, setup, costs and pre-finality leakage. | 2026-09-21: explicit linear-encryption/generalized-Schnorr relation for C11a, six group elements plus five scalars before context/recipient-key overhead. Correctness and special-soundness algebra derived. Full Fiat-Shamir/protocol security, implementation, availability and transaction-race analysis remain. Second increment 2026-09-21: escrow-first settlement ordering, buyer-enforceable freshness (S19), and the residual reorg race identified as S01-class; oracle-model composition still open. Log 26: time-lock envelopes (X20) and mandatory offset-linkage proofs (X22) checked and excluded. Log 27 proposes adding, as sub-items, the verifier environment for the pairing-equation proof at settlement and the concrete commitment scheme. Third increment (log 28): verifier environment resolved to the precompile interface (E47); the direct proof is not precompile-verifiable and its source-group repair leaks (X23); a chained source-group proof with a mint base case is derived, six scalars plus, on BN254 only, three `G2` elements; the separate commitment scheme is unnecessary because the stored envelope is the binding. Remaining: independent re-derivation, the formal oracle-model statement, data-availability and forfeit rules, and the challenge-hash context schema. Fourth increment (log 29): relations `R_M`, `R_T` stated; proof system and its five composition claims stated; settlement protocol T1–T4, data availability and the challenge context schema fixed. Remaining: independent re-derivation, the written random-oracle proofs, E46 and E48 inspection. Fifth to seventh increments (logs 30–32): proof sketches, Assumption A1 reduced to decisional BDH-3b (E49), E46 and E48 reviewed, extraction and hybrid bookkeeping with concrete loss terms. Eighth increment (log 33): independent re-derivation reconciled with five corrections. Closed at the research level; measurement remains under Q08. |
| Q10 | How does First Finder custody grow and survive administrative claim without threshold operation? | Preserve initial single-source bootstrap, pre-claim issuance, no bootstrap publisher rights, original-deployment continuity and claimant authority. | Initial F0 case settled by P14/log 21. C11c plus F2 lets any available compatible holder support later grants/claim. Remaining work is the growth/handover state machine and explicit liveness under loss of available credential sources, not permission to bootstrap alone. R12 equivalence under F0 analyzed 2026-09-21 (custody section): met except for escrow-era pricing, with the First Finder's retained copy as a P08-class residual under readings (a)/(a′) of interpretation 7. S21 (log 25) removes the claim-time First Finder dependency; remaining: pre-claim grant liveness (P14, F2) and the dual-sidecar cost while escrow-era credentials remain in use. Log 27 proposes adding the R11 registry rule for compatible deployments. Log 28: F0 chosen with C11a, so F2 is unavailable by construction (S22); the depth-two variant at F1 is the recorded route if replication is later wanted. Log 29: state machine S0–S4, voluntary migration, rotation as recovery and the R11 sidecar-per-live-set rule recorded (Q10 section); closed at the research level, adoption pending. |

If a genuine remaining gap becomes irreconcilable, report the entire backward chain, the precise preconditions needed to close it, and at least three distinct alternatives. Candidate alternatives may include a stronger restricted primitive, a compact delegation path, or a specifically identified application concession. They must not be presented as conforming if they fail an unchanged requirement.

## Evidence register

Evidence levels: **reviewed** means the relevant argument/interface was examined in the accumulated research, not that the paper was independently proved correct or its implementation audited; **abstract** means only the primary abstract supports the recorded claim; **lead** means the full construction has not been checked for this application. Benchmarks are authors' reported results.

| ID | Primary source | Evidence and use |
| --- | --- | --- |
| E01 | [Nuta, Schuldt, Nishide: blockchain-based forward-secure public-key encryption](https://eprint.iacr.org/2022/073); [journal text](https://www.jstage.jst.go.jp/article/transfun/advpub/0/advpub_2022CIP0016/_pdf/-char/en) | Reviewed construction: private ephemeral opening, signed chain registration, erasure, EWE assumption, fixed recipient. Precedent, not completed NFT adaptation. |
| E02 | [General witness encryption proposal, 2026/175](https://eprint.iacr.org/2026/175) | Primary abstract reviewed; authors' hundreds-of-terabytes estimate at 100-bit security. Do not generalize to a lower bound. |
| E03 | [Witness encryption for KZG commitments, 2024/264](https://eprint.iacr.org/2024/264) | Reviewed restricted relation and compact core. Algebraic-group-model assumptions matter; arbitrary NFT predicates are not supplied. |
| E04 | [Jäger, CCS 2024 paper](https://robocall.science/publication/ccs24/ccs24.pdf); [BLS Witness Encryption implementation](https://github.com/wspr-ncsu/BLS-Witness-Encryption) | Paper/interface reviewed for fixed key/tag signature WE. Implementation availability checked; internals and runtime not audited. |
| E05 | [Homomorphic signature-based WE, 2025/443](https://eprint.iacr.org/2025/443) | Abstract revised July 28, 2026 reviewed. Earlier indexed March 2025 PDF was not the current full version; current construction and evaluation remain unverified here. |
| E06 | [A Framework for Witness Encryption from Linearly Verifiable SNARKs and Applications, 2025/1364](https://eprint.iacr.org/2025/1364) | CRYPTO 2025; revised 2026-02-18. Abstract reviewed: gadgets are WE for relations induced by linearly verifiable arguments; applications are registered ABE with linear CRS via DNF/set-membership encryption, and registered threshold encryption. Its registered encryption keeps the post-registration model (S11). Full gadget composition for the C02 relation not verified. |
| E07 | [BABE, January 2026 research paper](https://docs.babylonlabs.io/papers/BABE_Research_Paper_Jan_2026.pdf) | Relevant pp. 6-9 reviewed: garbling, interaction for labels, honest setup, and reported performance. Not general noninteractive WE. |
| E08 | [Blockchain WE, 2025/1064](https://eprint.iacr.org/2025/1064); [inspected PDF](https://eprint.iacr.org/2025/1064.pdf) | Next-block construction reviewed. Current abstract and indexed PDF differ in title/scope; the latter also discusses RAM obfuscation. Check version before extending any claim. |
| E09 | [Arithmetic-circuit EWE claim, IEEE DOI](https://doi.org/10.1109/JIOT.2025.3540308) | Abstract-level lead only; full primary text inaccessible during research. Neither validated nor refuted. |
| E10 | [Efficient registration-based encryption, 2022/1505](https://eprint.iacr.org/2022/1505) | Reviewed self-generated keys/public curator model; reported 914-byte ciphertext and performance. Future registrations on old ciphertext unresolved for this application. |
| E11 | [Registered ABE](https://www.cs.utexas.edu/~dwu4/papers/RegisteredABE.pdf) | Definition 4.2 and associated note reviewed for post-registration correctness. Do not extend it to old ciphertext without an update construction. |
| E12 | [Registered ABE with linear CRS](https://www.cs.utexas.edu/~dwu4/papers/RegisteredABE-Linear.pdf); [RBE in the plain model](https://eprint.iacr.org/2025/502.pdf); [distributed broadcast-encryption lead](https://arxiv.org/html/2506.10338v1) | Leads captured 2026-09-15; dispositioned 2026-09-16 under C03 with E28-E33. The plain-model RBE paper was not inspected; it is covered by S11 at the definitional level only. |
| E13 | [Ateniese, Fu, Green, Hohenberger: Improved Proxy Re-Encryption Schemes](https://isi.jhu.edu/~mgreen/proxy.pdf) | Reviewed pairing construction and weak-secret distinction; supports the specific C04 counterexample, not a general PRE impossibility. |
| E14 | [ERC-7857](https://eips.ethereum.org/EIPS/eip-7857) | Inspected interface/example limitations. Mutable proposal: re-check current revision before making current implementation claims. |
| E15 | [Key-homomorphic constrained PRFs, 2015/180](https://eprint.iacr.org/2015/180.pdf) | Section 5.4 prior review: the PRE header changes while c2 remains symmetric encryption under kappa; the recipient recovers kappa. This instance does not eliminate a shared derived body key. |
| E16 | [Improving Speed and Security in Updatable Encryption Schemes, 2020/222](https://eprint.iacr.org/2020/222.pdf) | Definition 3.2 and Section 4.1 prior review: compact header/token size is distinct from body invariance; the chained construction encrypts a previous ciphertext body again. |
| E17 | [CCA-1 Secure Updatable Encryption with Adaptive Security, 2022/1339](https://eprint.iacr.org/2022/1339.pdf) | Prior construction/Section 5.4 review: LWE body updates, bounded update budget and packed polynomial-matrix token growth with packing parameter. Not a demonstrated compact helper for an unchanged canonical body. |
| E18 | [Generalised key delegation for hierarchical identity-based encryption](https://research.ibm.com/publications/generalised-key-delegation-for-hierarchical-identity-based-encryption); [Scalable Wildcarded Identity-Based Encryption with Full Security](https://www.mdpi.com/2079-9292/9/9/1453) | Leads for wildcard restriction and delegation. 2026-09-20: both feed C10. The first is WKD-IBE (2007/221: keys with wildcard entries derive keys for concrete replacements; an identity-based broadcast scheme with constant ciphertext size is derived from it); the second returned HTTP 403 on fetch. The 2026-09-15 note that a fixed-owner key cannot be redelegated is superseded by S17 and the C06 addition. |
| E19 | [Decentralized Authorization with Private Delegation](https://escholarship.org/uc/item/8r20m39b) | Lead: determine whether the encrypted object is authorization metadata or an applicable common payload. |
| E20 | [Traceable PRFs, 2020/316](https://eprint.iacr.org/2020/316.pdf) | Reviewed conceptual direct-mask use and evaluation-key distinction. Parked until useful for primary requirements. |
| E21 | [Collusion-resistant traceable PRFs](https://www.cs.utexas.edu/~dwu4/papers/TPRF-CR.pdf) | Reviewed scheme-specific two-key weakness and later parameter/security tradeoff. Do not generalize across all constructions. |
| E22 | [Optimal traitor tracing, 2024/867](https://eprint.iacr.org/2024/867.pdf) | Pairing-based traceable encryption/KEM result reviewed at research level; master issuance and common-key implications remain separate. |
| E23 | [Registered functional encryption / tracing without trusted authority, 2024/179](https://eprint.iacr.org/2024/179.pdf) | Adjacent result; registration does not automatically answer old-payload access. |
| E24 | [Functional witness encryption and extractability, 2013/650](https://eprint.iacr.org/2013/650.pdf) | Reviewed security-definition distinction. Writing a function that uses a secret is not a proof that an authorized witness cannot extract that secret. |
| E25 | [Broadcast and trace from positional WE, PKC 2019](https://www.iacr.org/archive/pkc2019/114420110/114420110.pdf) | Reviewed Definition 4: fresh-challenge decoder experiments do not automatically cover a decoder specialized to one published ciphertext. Tracing currently optional. |
| E26 | [Witness PRFs for vector commitments, 2026/1079](https://eprint.iacr.org/2026/1079) | Restricted-opening lead. General witness-PRF relations and an NFT payload composition must not be inferred from the title. |
| E27 | [Witness PRFs, 2014/301](https://eprint.iacr.org/2014/301.pdf) | Reviewed distinction: witnesses for the same input evaluate the same function value; varying input does not automatically preserve compatibility with one fixed payload. |
| E28 | [Garg, Hajiabadi, Mahmoody, Rahimi: Registration-Based Encryption, 2018/919](https://eprint.iacr.org/2018/919) | Defining abstract reviewed: encryption uses "any public parameters released subsequent to the recipient's registration"; updates are auxiliary decryption information, at most O(log n) per user. Supports S11. |
| E29 | [Mahmoody, Qi, Rahimi: Lower Bounds for the Number of Decryption Updates in RBE, 2022/1285](https://eprint.iacr.org/2022/1285) | Abstract reviewed: Ω(log n / log log n) decryption updates for poly(log n) parameters with fixed update times. Confirms that "update" in RBE is a decryptor-side quantity, not ciphertext re-targeting. |
| E30 | [Fiore, Kolonelos, de Perthuis: Cuckoo Commitments, 2023/1389](https://eprint.iacr.org/2023/1389) | Abstract reviewed (ASIACRYPT 2023): compiles small-identity RBE into large-identity RBE via cuckoo hashing; 1.7 MB ciphertexts for 1000 users. No retroactive or ciphertext-update capability claimed. |
| E31 | [Lee: Adaptive Chosen-Ciphertext Security of Distributed Broadcast Encryption, arXiv 2506.10338](https://arxiv.org/abs/2506.10338) | Abstract reviewed: the sender fixes the recipient subset from existing public keys; the contribution is adaptive CCA security under a q-type assumption. Not a future-recipient mechanism. |
| E32 | [Revocable Registered Attribute-Based Encryption With User Deregistration, IEEE IoT-J 2025](https://doi.org/10.1109/jiot.2025.3573050) | Search-result abstract only: revocation and permanent deregistration. Removes access; does not grant access to earlier ciphertexts. |
| E33 | [Scalable Registration-Based Encryption from Lattices, 2026/717](https://eprint.iacr.org/2026/717) | Search-snippet level: its bucketed laconic-encryption registry supports insert, remove, and key update as registry operations under the standard RBE model. Not inspected further. |
| E34 | [Abdalla et al.: Identity-Based Encryption Gone Wild, 2006/304 PDF](https://eprint.iacr.org/2006/304.pdf); [supplied text](papers/vertopal.com_2006-304.latex) | Full relevant syntax, Figure 1 and Sections 6.1-6.2 reviewed 2026-09-21. Symmetric-pairing schemes with configured maximum depth. BB-WIBE: BDDH, d+1 key elements, L+w+2 ciphertext elements for a length-L pattern with w wildcards; BBG: L-BDHI, two pairings and w+3 ciphertext elements. C11's same-identity rerandomization and validity equation are our algebraic deductions. |
| E38 | [From Selective to Full Security: Semi-Generic Transformations in the Standard Model, 2012/341](https://eprint.iacr.org/2012/341.pdf) | Text extracted: defines selective-pattern security for WIBE — the adversary commits to the challenge pattern in advance and may query keys for any identity not matching it — and notes that all known WIBE constructions satisfy it. Supports the C10 deduction that one fixed public pattern needs only selective-pattern security. |
| E39 | [Birkett, Dent, Neven, Schuldt: chosen-ciphertext secure WIBE, 2006/377](https://eprint.iacr.org/2006/377.pdf) | Text extracted at the introduction level: the original CCA transform for WIBE needs a (2L+2)-level CPA scheme for an L-level CCA scheme; this work improves it. Lead for header CCA security in C10; not inspected further. |
| E35 | [Boneh, Gentry, Waters: Collusion Resistant Broadcast Encryption With Short Ciphertexts and Private Keys](https://crypto.stanford.edu/~dabo/papers/broadcast.pdf) | Section 3.1 reviewed: setup for N users has 2N+1 public source-group elements, one private element per user, two-element KEM header, two decryption pairings plus a subset product; decisional N-BDHE basis. Setup emits all keys; retaining issuance material and assigning unused slots later is our proposed lifecycle, with N still fixed. |
| E36 | Asymmetric traitor tracing and self-enforcement: [Kiayias, Yung: Breaking and Repairing Asymmetric Public-Key Traitor Tracing, DRM 2002](https://crypto.stanford.edu/DRM2002/asymvpp2.pdf); [Watanabe, Hanaoka, Imai: Efficient Asymmetric Self-Enforcement Scheme with Public Traceability, PKC 2001](https://doi.org/10.1007/3-540-44586-2_17) | Abstract-level: in asymmetric schemes the authority cannot frame a subscriber and tracing yields non-repudiable proof (Pfitzmann's notion, cited there); WHI01 binds a subscriber's publicly certified discrete-log secret into its key without revealing it, so leaking the key leaks that secret (self-enforcement, after Dwork–Lotspiech–Naor). Supports clause (ii) by identity binding; compatibility with HIBE derivation unverified. |
| E37 | [Chabanne, Phan, Pointcheval: Public Traceability in Traitor Tracing Schemes, Eurocrypt 2005](https://doi.org/10.1007/11426639_32) | Abstract-level: pairing-based traitor tracing with ciphertext rate approaching 1 and tracing delegable to untrusted parties. C09 instance lead; not inspected. |
| E40 | [Waters: Ciphertext-Policy Attribute-Based Encryption, 2008/290](https://eprint.iacr.org/2008/290.pdf) | Section 3 setup, key generation, decryption and selective-security theorem reviewed. Same-attribute rerandomization and public validity equations in C11 are our deductions. Scheme proof uses symmetric pairings and q-parallel BDHE; no type-3 implementation proof or benchmark performed here. |
| E41 | [Bethencourt, Sahai, Waters: Ciphertext-Policy Attribute-Based Encryption](https://www.cs.utexas.edu/~bwaters/publications/papers/cp-abe.pdf) | Section 4.2 supplies Delegate for an attribute subset; using the same set gives fresh randomized keys. Original generic-bilinear-group/random-oracle security is not a modern implementation endorsement. |
| E42 | [Groth, Sahai: Efficient Non-interactive Proof Systems for Bilinear Groups](https://web.cs.ucla.edu/~sahai/work/web/2008%20Publications/Eurocrypt_Groth2008.pdf) | Primary introduction and equation framework reviewed as a proof-composition lead. Establishes tools for bilinear equations, not turnkey compatibility with arbitrary recipient encryption or fair exchange. |
| E43 | [Boneh, Boyen, Shacham: Short Group Signatures](https://crypto.stanford.edu/~dabo/papers/groupsigs.pdf) | Section 3.2.1 defines linear encryption of group elements and its CPA security under Decision Linear, including symmetric pairing groups where DDH is easy. Only that encryption component is used in Q09; no group-signature or tracing requirement is imported. |
| E44 | [RFC 8235: Schnorr Non-interactive Zero-Knowledge Proof](https://www.rfc-editor.org/rfc/rfc8235.html) | Primary reference for Schnorr/Fiat-Shamir, context binding and validation. Q09's five-equation representation relation is our explicit generalization, not a protocol specified or audited by this RFC. |
| E45 | [Hair, Sahai: Witness Encryption via Prime-Order Generic Groups, arXiv 2609.18275v1](https://arxiv.org/html/2609.18275v1) | New 2026-09-16 preprint; introduction and security statement inspected 2026-09-21. Polynomial algorithms in a fresh generic-group encoding model, with false-statement security only. Authors explicitly exclude a guarantee for true statements with an unknown witness. Does not by itself solve C02's private-holder requirement; no concrete implementation or full proof audited here. |
| E46 | [Boneh, Boyen: Efficient Selective-ID Secure Identity-Based Encryption Without Random Oracles, 2004/172](https://eprint.iacr.org/2004/172); [supplied text](papers/vertopal.com_2004-172.latex) | Reviewed 2026-09-21 from the supplied text (log 31): Section 4.1 scheme, Definition 3.1 Decision BDH as a symmetric 5-tuple, Theorem 4.1 tight selective-identity security. Symmetric groups throughout; no asymmetric statement. C11a is its depth-one, all-wildcard specialization; the Type-3 assumption for the zero-key observer game is supplied by E49 (Q09 sixth increment). |
| E49 | Chatterjee, Menezes: On cryptographic protocols employing asymmetric pairings — the role of ψ revisited, Discrete Applied Mathematics 159 (2011) 1311–1322; [supplied text](papers/vertopal.com_1-s2.0-S0166218X11001648-main/9bd1c399148b4d479439e96d3fa83645.latex) | Reviewed 2026-09-21, Section 3.1 and Remark 2 (log 31): Definitions of BDH-2, BDH-3, and the variants BDH-3b and BDH-3c with `BDH-3 ≤ BDH-3b ≤ BDH-3c`; Lemma 3 equivalence of BDH-2 and BDH-3 when the Type-2 relation constant is known; BF-IBE-3 proved under BDH-3b. Used to reduce the Q09 fifth increment's Assumption A1 to decisional BDH-3b. The paper treats BF-IBE, BLS and group signatures, not BB1; the BB1 embedding is ours. |
| E48 | [Faust, Kohlweiss, Marson, Venturi: On the Non-malleability of the Fiat–Shamir Transform, 2012/704](https://eprint.iacr.org/2012/704) (INDOCRYPT 2012); [supplied text](papers/vertopal.com_2012-704.latex) | Reviewed 2026-09-21 from the supplied text (log 31): challenge `H(α, x)` with the statement included; Definition 2 quasi unique responses; Definition 3 min-entropy of commitments; Definition 5 unbounded simulation soundness; Theorem 2 (simulation soundness under quasi unique responses and non-triviality) and Theorem 3 (weak simulation extractability under unique responses, extraction error `Q/h`, two runs). Both apply to the generalized Schnorr protocols for `R_M` and `R_T`, whose responses are perfectly unique and first messages uniform. Discharges preliminary (P2) of the Q09 fifth increment. |
| E47 | EVM precompile interface: [EIP-196](https://eips.ethereum.org/EIPS/eip-196) and [EIP-197](https://eips.ethereum.org/EIPS/eip-197) (BN254 `G1` addition, scalar multiplication and pairing-product check); [EIP-2537](https://eips.ethereum.org/EIPS/eip-2537) (BLS12-381 `G1` and `G2` addition, scalar multiplication, multi-scalar multiplication, pairing-product check and map-to-curve; included in the 2025 Pectra upgrade, deployment on any given L2 to be checked) | Reference standards cited for the verifier interface used throughout the Q09 third increment: source-group arithmetic and a product-of-pairings-equals-one check, with no target-group arithmetic and no returned pairing value. Inspected 2026-09-21 (log 30): EIP-197 returns only one or zero, requires G1 inputs on the curve and G2 inputs "checked to be equal to the group order", so the pairing precompile itself enforces the G2 subgroup check on BN254, whose G1 has cofactor one; EIP-2537 is Final, returns only a boolean, and rejects any G1MSM, G2MSM or PAIRING_CHECK input that is on-curve but "not in the correct subgroup", while its ADD operations are not stated to subgroup-check, so a contract should validate stored keys through MSM or a pairing check at registration. The upgrade that shipped EIP-2537 is not named on the page; deployment must be confirmed per launch L2. Gas figures deliberately not quoted. |

## Research update discipline

1. Read the requirements file and the critical-path file before resuming, then this ledger by ID as needed. Begin from the critical path's next unit of work, not from a generic survey.
2. Record new user decisions in the requirements tables and in the dated log. Mark the older interpretation superseded rather than letting contradictory requirements coexist silently.
3. For every candidate, record the exact source/version, relation, security definition, setup, secret holders, algorithms, update behavior, costs, and lifecycle cases actually examined.
4. Separate source claims from our deductions and proposed compositions. Mark inaccessible text, abstract-only evidence, missing proofs, and unmeasured performance plainly.
5. Record a failed path with the exact failed requirement and a reopening condition. Do not replace a construction-specific failure with a statement that the whole field is impossible.
6. Do not re-open S01-S10 or X01-X17 without a changed premise, new construction, or new evidence. State which condition changed before spending another research pass on it.
7. End each research increment by updating the relevant candidate and queue status. A new paper title is a lead, not progress on the composition until its specific obligation is checked.
8. Keep this as the research memory. Changes to cryptography.md, MVP Scope.md, code, or workplans are separate work and must not be smuggled into notebook maintenance.
9. After each increment, append here first: the log row and any S, X, E or candidate entries. Then rewrite the affected sections of the critical-path file in place and bump its stamp; rewrite the requirements file only for a user decision. Never append dated corrections to either of those files; dated history belongs here.

## Decision and progress log

Earlier conversation decisions are ordered below without invented timestamps. They predate this notebook; their wording here summarizes the user's instructions rather than quoting the source papers.

| Sequence | Decision or progress | Effect |
| --- | --- | --- |
| 1 | Current wrapped-SCK and threshold-provisioning divergences identified and accepted. | Research targets both native distinct material and threshold-free access. |
| 2 | User requested backward reasoning from the observable target to established methods. | Maintain the complete dependency chain; do not ask the user for mathematical solutions. |
| 3 | NFT methods, mutation, and references offered as hypotheses, explicitly not authoritative solutions. | Explore authenticated context and helper paths; do not infer secret delivery from public execution. |
| 4 | Ownership interval proposed as a possible window; byte-identical plaintext preferred. | Do not require a new key at every check or watermarking merely for attribution. Window granularity remains explicit. |
| 5 | Ownership/state checking required at every decryption attempt. | Mandatory conforming-client behavior even if algebraic current-state dependence is unavailable. |
| 6 | Plaintext policy corrected to not attempting to disallow redistribution. | No plaintext policing or encouragement is part of the research objective. |
| 7 | Full-sized personalized Path C rejected. | Preserve common-payload distribution economics. |
| 8 | Traitor tracing downgraded to nice to have. | Stop treating attribution of arbitrary derived material as the critical path. |
| 9 | Protocol enforcement explicitly accepted; modified-client cryptographic expiry not mandatory. | Stop cycling on retained-secret replay as a blocker. |
| 10 | Small-fraction variant/helper objects accepted as fallback, optionally NFT-linked torrents. | Investigate compact helpers; do not equate them with the rejected full-sized Path C. |
| 11 | State-of-art snapshot delivered; restricted WE, registered encryption, and PRE remained candidates. | No candidate was declared complete. Further lead discovery did not close the composition gaps. |
| 12 | 2026-09-15: user requested a persistent notepad after repeated research cycles. | Created this file, consolidated requirements and exclusions, and captured unfinished leads and queue. |
| 13 | 2026-09-16: Q01 investigated and closed negative (S11-S14, X18, C03); C02 relation sharpened; C08 lead recorded; Q07 added; the R04 reading clarification (interpretation 2) was put to the user. | The RBE family leaves the critical path. Q02 is next unless the user selects reading (A) of R04, in which case Q07 precedes it. |
| 14 | 2026-09-16: user answered the R04 question — (C) excluded; (B) sufficient; (A) prerequisites available (NFT ownership history as the delegation chain; NFT may reference a helper; issuer allowed on the pathway without selective deniability; algorithmic independent decryption preferred, latency accepted if unavoidable); forced transfer, lost keys, and recovery are non-obligations. | P11-P13 added; R08 clarified; interpretations 2 and 6 updated; S15 recorded; C04 and C08 obligations revised; X10 re-checked; Q07 moves first, Q02 second. The operational criterion for (B) and the (A)/(B) convergence against log 8 are put to the user for confirmation. |
| 15 | 2026-09-16 (recorded 2026-09-20): user ruled that criterion (i)-(iii) is acceptable if it yields a valid method but premature to call obligate, and noted that clause (ii) is a pathway to traitor tracing; clarified that "unavailable or lost seller" in C04 means the previous holder, never the publisher; and set publisher scarcity as obligate — the publisher may refuse to issue new entitlements or keys but cannot selectively deny an existing holder's decryption or transfer (manufacturer analogy). | R08, R09, R10 clarified; interpretation 2 and S15 reworded from "proposed" to "accepted, not obligate"; C04 wording fixed; the premise that no issuer may hold a master secret is withdrawn for mint (S17). |
| 16 | 2026-09-20: C09 and C10 derived under the changed premise; C06 and C07 partially unparked; S16-S17 recorded; Q07 answered in shape by C10; Q08 added and placed first; E34-E39 added; WIBE matching semantics and efficiency statements verified from extracted text, PDFs not readable; Q08 partially answered (C10 findings); S18 records that non-repudiation against ancestors is unavailable inside WIBE. | Critical path is Q08 (verify C10, then C09), then the Q05 window-granularity decision, then Q02 as the issuer-free alternative. |
| 17 | 2026-09-20: user chose to compare both candidates with a cost and property table before choosing, and to place a text copy of the WIBE paper under docs/ for the exact figures. | Q08's deliverable is the C09/C10 table (draft recorded); the window-granularity decision is deferred until the figures are in; the agent halts until the text copy's path is provided. |
| 18 | 2026-09-21: reviewed the latest notebook and supplied WIBE text; verified exact BB/BBG/BGW costs and corrected group encodings, BBG assumption, finite-depth security scope and several overbroad deductions. Derived C11 same-identity/same-attribute native-key randomization with constant transfer depth and public-parameter validity equations. | Q08 comparison completed at algebraic level. C11 is the leading issued-distinctness candidate; delivery composition Q09 and First Finder custody Q10 remain open. F1-F4 record alternatives; no custody concession or cryptographic suite has been adopted. Asked the user whether independent full-secret custodians are an acceptable First Finder trust boundary, while continuing independent proof analysis. |
| 19 | 2026-09-21: user asked why the First Finder cannot retain its already-known secret until escrow claim. Added F0 and its claim-handover relation; distinguished initial leak exposure from continuing availability and later-compromise exposure. Derived Q09's compact encrypted-key representation proof and inspected the new E45 WE preprint. | First Finder custody is mathematically viable. The pending choice is acceptance of its sole-custodian liveness until handover, or another explicit availability mechanism. Existing clients remain local; no threshold service is restored. The user's question is not treated as blanket approval to drop erasure/availability rules. |
| 20 | 2026-09-21: derived C11c's fixed-asset identity specialization, which gives two-element native keys and a smaller common capsule. Any compatible holder can supply a fresh credential for an authorized mint or claimant without the original master secret. Added public capsule well-formedness checks and their extra cost. | F0 plus holder assistance is a concrete conditional lifecycle. Asked whether publication may start with the First Finder alone or requires independent custody acknowledgement first. This is an availability-policy choice; the proof/implementation remains research and no specification has been adopted. |
| 21 | 2026-09-21: user clarified that initial creation necessarily has the First Finder as sole source; requiring an independent credential custodian first would make bootstrap fail. | P14 records sole-source bootstrap as settled. Additional credential sources improve availability after creation. Continue the cryptographic and lifecycle analysis without asking this question again. |
| 22 | 2026-09-21: user restated obligations — unbounded, on-demand minting to unknown bearers with no pre-bound count; no obligation to mint but no deniability over existing entitlements' use or transfer; publisher participation in a transfer acceptable iff non-deniable (a replacement mutates a member of the set without growing it); a bearer must lose the ability to decrypt at transfer, distinct from keeping plaintext; escrow claim must leave the claimant equivalent to an original publisher except for pricing escrow-era keys. | R06, R09, R10, R12 updated; X19 excludes setup-bound slot counts; interpretation 7 records three readings of loss-at-transfer and puts the choice to the user; R12 equivalence analyzed under F0/C11. |
| 23 | 2026-09-21: second maintainer re-read the reviewed notebook, independently re-derived C11a/C11c decryption, rerandomization and the Q09 relation, and recorded master-secret hiding and cross-identity non-convertibility under CDH, S19 buyer-enforceable freshness, the Q09 settlement ordering and residual race, and the R12 equivalence analysis. | Q09/Q10 advanced at the protocol-composition level; no implementation or formal proof. Next: interpretation 7 answer, then Q09's oracle-model composition and Q10's growth/handover state machine. |
| 24 | 2026-09-21: user answered interpretation 7 as an escalation chain — (a) conforming-protocol loss is the minimum viable construction; (a′) distinguishable stale material is potentially available and improves the protocol while still relying on client conformance; (b) cryptographic loss is the preference but unavailable under the common-ciphertext and no-TEE constraints. | Interpretation 7 settled; P15 records (b) as the unavailable preference; (a′) is the working target, which C11 meets and C09 does not. Next: Q09's oracle-model composition and Q10's growth/handover state machine. |
| 25 | 2026-09-21: user observed that a claimant can always produce a new volume under its own master key, so First Finder unavailability at claim is not a blocker; that a fresh secret-bearing component per read from an NFT method might reach interpretation 7(b); and that publisher involvement on the read path is deniable by unavailability even under a duty. | S20 and S21 recorded; F0, the custody section, C11's R11 paragraph and Q10 updated — claim never needs the First Finder, and escrow-era credentials follow later bodies via dual sidecars. The NFT-method idea is analyzed under interpretation 7 and recorded as C02's (b)-route: contracts cannot hold secrets, a public epoch attestation supplies freshness only, and per-epoch capsule sets plus the C02 private-witness relation are the cost and the gap. |
| 26 | 2026-09-21: a third agent proposed three additions, reviewed by Claude Fable 5.1 at the user's request: a time-lock/VDF delivery envelope to seal the Q09 reorg race, a puncturable KEM to make the (b)-route O(1) in storage, and a mandatory proof of knowledge of the rerandomization offset at each subsequent sale. All three premises matched the notebook; all three remedies failed against settled reasoning or duplicated the existing Q09 proof. | Recorded as X20-X22 with reopening conditions, pointers added at the Q09 second increment, the C02 (b)-route and S19, and the resume point restated. No candidate, requirement or queue order changed. Next: Q09's oracle-model composition, then Q10's growth/handover state machine. |
| 27 | 2026-09-21: at the user's request, separated current belief from provenance. Created [cryptography-critical-path.md](cryptography-critical-path.md) — the nine-step chain walk against C11, the construction stated once, open obligations by owner, accepted residuals, the do-not-re-propose index and the next unit of work — and [cryptography-requirements.md](cryptography-requirements.md) — R, P and the open interpretations 1, 4 and 5 at their current readings. This file becomes the append-only ledger. The chain walk exposed three items no queue entry owned: the C11a versus C11c choice, the verifier environment for a pairing-equation proof at settlement, and the concrete commitment scheme; it also re-homed interpretation 5's attempt semantics and the R11 registry rule. | Resume point replaced by a pointer and read rule; discipline item 1 revised and item 9 added; Q05, Q09 and Q10 rows note the proposed additions. No requirement, candidate or exclusion changed. The two new files are stamped "current as of log 26"; this row adds no belief, so bumping the stamp to 27 is their only pending re-sync. Next: Q09, the verifier environment first, then the oracle-model composition. |
| 28 | 2026-09-21: the user asked whether the F custody paths touch the publisher's minting right and whether F2 is preferable; on the corrected framing — the F paths govern first credentials of escrow-era entitlements, transfers are two-party under every option, and F2 excludes entitlement-level attribution by definition — the user accepted C11a with F0 custody and confirmed Q09 as the next unit. The Q09 third increment was then derived: the verifier environment resolves from cryptography.md to the EVM precompile interface (E47); the C11a layout is fixed in type-3 groups with a KEM-form capsule and an SXDH ElGamal envelope of four elements; the first increment's direct validity proof has a target-group first message and equation that precompiles cannot verify, and its source-group repair leaks the delivered key (X23); a chained source-group proof — the seller proves its new envelope is its decryption of the previous envelope shifted by a secret offset, with a mint base case proved from the master scalar — gives six scalars, or six scalars plus three `G2` elements on BN254, verified with source-group arithmetic and, on BN254, a batchable pairing-product check. | S22, X23, E46, E47 recorded; the F1 row notes the depth-two variant; Q05, Q08, Q09, Q10 rows updated; X22's correctness finding stands with its adoption-for-verifiability noted. Consequences: the buyer's envelope secrets persist for the interval as the next sale's witness; the separate commitment scheme drops out because the stored envelope is the binding; CPA plus proof of knowledge of the coins is the oracle-model target rather than CCA. Both current-state files are now stale at log 26 and need re-sync: C11a and F0 chosen, construction and next unit rewritten. Next: re-sync the critical path; then Q09's independent re-derivation and formal statement, and its settlement rules. |
| 29 | 2026-09-21: the user lifted the one-file-per-turn constraint for research and asked for continuation without halting except on genuine tensions. Recorded in one pass: the Q09 fourth increment (relations `R_M` and `R_T`, the Fiat–Shamir proof system, five composition claims under SXDH and the random-oracle model, the T1–T4 settlement protocol, data availability, the challenge context schema); the Q10 custody state machine S0–S4 with voluntary migration, rotation as recovery and the R11 sidecar-per-live-set rule; the Q05 conclusions settling interpretations 1, 3 and 5 (ownership interval as the window, the exact persistent credential, attempt semantics on the adapter's SOFT and HARD tiers, no offline attempts) and interpretation 4 by formula; and the first Q06 pass. No tension requiring user input was found: every choice had a determinant in the requirements, the user's earlier statements, or cryptography.md. | E48 added as a lead; Q05, Q06, Q08, Q09, Q10 rows updated. Q06 verdict: a complete construction at the protocol-composition level for every requirement under the recorded residuals; the remaining gap is proofs, source inspection (E46, E48), an independent re-derivation of the Q09 relations, implementation and measurement. The critical path and requirements files are re-synced to this row in the same pass. Next: the written random-oracle proofs of the Q09 claims, or, if a second maintainer is available, the independent re-derivation first. |
| 30 | 2026-09-21: Q09 fifth increment recorded — proof sketches of delivery soundness, ledger confidentiality and non-malleability, and the exact statement of Assumption A1 for non-holder content confidentiality, with the observation that the standard three-exponent DBDH reduction does not apply to the type-3 layout as written and that A1 is generic-group secure by a monomial argument. Source inspection attempted: E47 confirmed from the EIP texts (subgroup-check behaviour recorded on the E47 row, which discharges preliminary P3 for BN254 and narrows it for BLS12-381); E48 located at ePrint 2012/704 with a structural summary only; E46's PDF and the E48 PDF could not be text-extracted by the fetch tool, the PDF reader lacks its renderer, the browser pane cannot open ePrint PDFs, and terminal extraction is denied by the repo's policy. The user offered to supply LaTeX conversions. | Requested from the user: E46 (2004/172) and E48 (2012/704) in full; optionally Chatterjee–Menezes 2011 for the type-3 BB1 assumption. Blocked items: whether E46 or the type-3 literature gives A1 or A1 stands on its own; the exact simulation-soundness hypotheses and hash-input rule from E48. Not blocked but needing a second agent: the independent re-derivation of `R_M` and `R_T`. Everything else on the critical path is implementation, measurement or specification adoption. |
| 31 | 2026-09-21: the user supplied LaTeX texts of E46, E48 and Chatterjee–Menezes 2011 in docs/papers and asked for the re-derivation agent to be spawned; it was, and is running. Sources inspected (Q09 sixth increment): E46 is symmetric only, with Decision BDH as a 5-tuple and a tight selective-identity theorem; E48's Theorems 2 and 3 apply to the generalized Schnorr protocols for `R_M` and `R_T` because their responses are perfectly unique and first messages uniform, discharging (P2); Chatterjee–Menezes, recorded as E49, define BDH-3, 3b and 3c, and Assumption A1 reduces to decisional BDH-3b by setting the scheme's generator to the instance's `ĝ1^α` and the identity bases to powers of `ĝ1`, which corrects the fifth increment's claim that no standard reduction applied. | E46, E48 rows updated to reviewed; E49 added; E34 and the resume point re-pointed to docs/papers. Claim (iii) now stands under decisional BDH-3b; (P2) and, for BN254, (P3) discharged. Remaining in Q09: the agent's independent re-derivation, then the second-reader bookkeeping items. Both current-state files re-synced to this row. |
| 32 | 2026-09-21: while the re-derivation agent runs, the seventh Q09 increment wrote out the second-reader bookkeeping: index-guessing extraction for delivery soundness with a single rewinding and a `1/N` loss, and the concrete loss terms for the confidentiality hybrids, `m·(Adv_DDH,G1 + Adv_DDH,G2) + J·Adv_DBDH-3b + m·Q/p + Q/h`. | Q09's research-level obligations reduce to reconciling the agent's report. Both current-state files re-synced to this row. |
| 33 | 2026-09-21: the second agent's independent re-derivation returned and was reconciled (Q09 eighth increment). Every relation, the extraction, the Sigma protocol, the pairing form, the leak hazard, the DDH embeddings and the BDH-3b reduction were confirmed from the primitives; the agent reached the BDH-3b reduction independently. Five corrections adopted: pairing and multiplication counts are exact only for the hash-weighted batch (twelve pairings unweighted for a transfer, ten for a mint; sixteen scalar multiplications with `G2` arithmetic); the persistent credential is `(x, y, E_o, interval index)` with the sale base being the fresh decryption of `E_o`; proofs of possession are simulated in the confidentiality hybrid, coins rather than keys are independent, insider hybrids cover honest-recipient envelopes only; registration rejects identity keys and mint rejects an identity `F_I`; the master-scalar requirement of `R_M` is consistent with F0. | Q09 closed at the research level. Critical path corrected and both current-state files re-synced to this row. Remaining on the critical path: measurement on the chosen curve, then a Q06 second pass; specification adoption is separate work awaiting the user. |
| 34 | 2026-09-21: the user reframed measurement as a validation implementation belonging to the specifications and workplan, agreed the adoption order (cryptography.md, MVP Scope, Application Requirements, Execution Trace, contract draft, workplan) with the limit that no workplan nodes are authored yet, only the pre-WBS and To-Do sections; observed that the capsule sidecar resembles the notebook's "variant object"; and restated the requirement that every component be an adapter to an interface under dependency inversion so any part can be swapped. | R15 added to the requirements file. Terminology settled for adoption: the capsule sidecar is the universal instance of P02/P12's compact helper and fills the suite's existing "recipient-independent public parameters or global header" slot in cryptography.md; it is named the header sidecar in the specification, distinct from the schema's variant seed, which remains the separate V2 fingerprint overlay. Adoption maps C11a onto existing adapter slots: KEM as a deployment-encryption adapter, envelope as a key-agreement adapter, proof as a new delivery-proof adapter, curve as a pairing adapter resolved by the chain adapter. Correction: the payload cipher remains the schema's length-preserving adapter with Bao integrity; the notebook's mention of an AEAD is superseded. Adoption of cryptography.md begun. |
| 35 | 2026-09-21: adoption completed for cryptography.md, MVP Scope.md, MVP Application Requirements.md and MVP Execution Trace.md, and the workplan's pre-WBS note and To-Do list revised without authoring nodes. The construction is now the specification's MVP posture: native credentials, verified delivery, no provisioning committee, per-attempt authorization on the settlement adapter's tiers, parameter sets with the sidecar-coverage rule, F0 escrow custody with S21 claim. Three Open Problems are marked resolved in place; four To-Do entries are closed and two added. One tension was found with no determinant in the existing materials and is recorded as a To-Do decision rather than chosen: the custodian of the escrow salt, formerly the provisioning layer. The contract draft file was left untouched as an unrelated early sketch. | Specification adoption is no longer "separate work awaiting the user" except for the salt decision. Remaining on the critical path: the validation harness and measurement, then the Q06 second pass; the harness now has a home in the MVP documents (Scope, Cost Instrumentation; Requirements, CD-07 and AS-21). |
| 36 | 2026-09-22: the user reviewed the adopted documents and raised nine items; all answers were accepted with two amendments. Decisions: (1) the offline-First-Finder hole — a First Finder that escrows a package and vanishes leaves it dead under C11a-with-F0 — is closed by resolving the credential KEM's identity scope per suite: escrow suites fix the asset scope (C11c), so any current holder authors a grant's credential from its own envelope with the transfer proof and the conforming client serves pending grants automatically; explicit-publisher suites keep the entitlement scope (C11a) and its attribution; the alternative of admitting additional escrow deployments from other First Finders was rejected by the user because it splinters the ciphertext. (2) The common piece-group keys are accepted as a residual not resolvable with known practical methods; the adapter seam between decapsulation and the payload cipher is preserved so a multi-key PRF suite could replace it later, and the residual is now an Open Problem in cryptography.md rather than a To-Do decision. (3) Composable container objects by reference nesting, in the manner of BitTorrent v2 per-file trees, are noted as V2. (4) Asset ownership transfer follows the claim path's custody mechanics; publishers register envelope keys. (5) Counter layout and KDF, hash-to-scalar and encoding identifiers become explicit hash-card fields. (6) Ethereum is the launch ecosystem, network held; wallet standards EIP-1193, WalletConnect, EIP-712 and ERC-4337 named; default identity is a local key. (7) Abuse vectors recorded: relayer drain, lock griefing, RPC censorship, verifier-key compromise. (8) Swarm-native publication, the project seed host and site, archive visibility, test facilities and the license are added to Scope and Requirements. (9) Workplans carry no history: the resolved-items paragraph was deleted. | S22 unchanged: F2 and attribution remain exclusive, now applied per suite. F0 remains the escrow custody for the optional handover only. cryptography.md, Scope, Requirements and the workplan updated; current-state files re-synced. Remaining on the critical path: the validation harness and measurement, then the Q06 second pass. |

No implementation, benchmark run, or security proof was completed as part of creating this notebook.

Maintained by GPT-6 (created 2026-09-15; reviewed and extended 2026-09-21) and Claude Fable 5.1 Max (2026-09-16, 2026-09-20 and 2026-09-21 increments).


